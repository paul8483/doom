namespace Doom.Graphics
{
    /// Border sampling policy for pure image transforms (no Unity dependency).
    public enum PixelWrapMode
    {
        Clamp = 0,
        RepeatX = 1,
        RepeatXY = 2,
    }
}
