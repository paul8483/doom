# Ре-ролл BOSSN0 (барон, предпоследний кадр смерти) — эскиз v2 (GPT 5.6 SOL)

Долг трека монстров: старый меш N0 — компактный высокий курган с кровяной
лужей, вытянутой в глубину (длинная ось тела ушла в Z), а нативный кадр —
ШИРОКАЯ низкая россыпь (82×38). Рецепт TROOM0: «лежать ровно», вид
сверху, явная плоскостность, рваный контур.

## Как пользоваться (инструкция для нас, не для GPT)

1. Приложить к сессии ЧЕТЫРЕ файла — полные пути в промпте, порядок тот же.
2. Ожидаемый выход: ОДНА картинка, путь сохранения в промпте.
3. Дальше: TRELLIS.2 Space, seed 872506309, GLB в
   D:\Development\doom\Textures\Trellis2\GLB\ (имя BOSSN0_<timestamp>.glb),
   сказать ассистенту — пит TROOM0 (doomify палитра BOSSK0, lay_down
   flatten до аспекта 82/38, тон по нативному кропу + экранная
   калибровка), установка, панель, тесты.

---

## Текст промпта (отсюда и до конца — в GPT)

Нарисуй ОДИН conditioning-эскиз для 3D-реконструкции: предпоследний кадр
смерти барона ада из DOOM (Freedoom) — рухнувшая на пол широкая россыпь
останков. Кадр N0, между приложенными соседями M0 и O0 по оседанию.

**Приложенные файлы и их роли** (полные пути, читать оттуда):
1. `D:\Development\doom\Textures\Trellis2\NativeInputs\BOSSN0-trellis.png` —
   нативный спрайт кадра, ЦЕЛЕВАЯ КОМПОЗИЦИЯ: слева куча стальных
   пластин брони с рёбрами-шипами, в центре тёмный провал разорванной
   туши с зелёными мышцами, справа раскрытая красно-жёлтая пасть-рана;
   у левого и правого краёв зелёные обломки. Композицию (что где лежит
   и в каких долях ширины) сохранить один-в-один.
2. `D:\Development\doom\Textures\Trellis2\ShapeHints\3D\BOSSM0-depth-shapehint-v2.png` —
   принятый сосед-предшественник (кадр выше): те же материалы — светлая
   сталь, зелёная мускулатура, тёмно-красное мясо, жёлтые прожилки.
   Температура цвета и яркость — ровно как здесь.
3. `D:\Development\doom\Textures\Trellis2\ShapeHints\3D\BOSSO0-depth-shapehint-v2.png` —
   принятый сосед-последователь (финальная лепёшка): твой результат по
   высоте и распластанности должен сесть МЕЖДУ файлами 2 и 3 — ниже и
   шире, чем 2, чуть выше и собраннее, чем 3.
4. `D:\Development\doom\Textures\Trellis2\ShapeHints\3D\BOSSK0-depth-shapehint-v2.png` —
   эталон деталей барона для этого набора (панцирь, шипы, гор).

**Результат — одна картинка:** широкая НИЗКАЯ россыпь останков,
ЛЕЖАЩАЯ РОВНО НА ПОЛУ, вид с фронта чуть сверху (как соседи 2 и 3).
Правила формы, критичные для реконструкции:
- Масса ШИРОКАЯ и ПЛОСКАЯ: ширина примерно вдвое больше высоты
  (нативный аспект 82×38); никакого холма/кургана в центре — рельеф
  из отдельных бугров пластин и мяса, все ниже трети ширины.
- ДЛИННАЯ ОСЬ РОССЫПИ — СТРОГО ГОРИЗОНТАЛЬНО В ПЛОСКОСТИ КАРТИНКИ
  (слева направо), не в глубину; глубина россыпи меньше её ширины.
- РВАНЫЙ КОНТУР: внешний край — неровные обломки и потёки, не овал и
  не круг; крови МАЛО и только тонкой плёнкой ПОД останками, никаких
  луж, вытянутых к зрителю.
- Ничего не направлять в камеру; отдельные читаемые формы с боковой
  толщиной, объёмная светотень, жёсткий контур.

**Подача:** изолированная россыпь по центру на прозрачном/шахматном
фоне; без пола-текстуры, тени, окружения и текста. Единство с
приложенным набором: те же материалы, температура цвета и яркость.

**КУДА СОХРАНИТЬ РЕЗУЛЬТАТ (обязательно):** готовую картинку записать
РОВНО в этот файл, перезаписав существующий:
`D:\Development\doom\Textures\Trellis2\ShapeHints\3D\BOSSN0-depth-shapehint-v2.png`
Никаких других имён и папок. После записи проверь, что файл по этому
пути обновился (дата изменения — сегодняшняя).
