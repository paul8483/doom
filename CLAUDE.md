# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

Stages 0–8 are done (6a–6f including music; 7a–7e including E1 standalone; Stage 8 Classic/Enhanced graphics). Post-6c expansions: rocket launcher (slot 5, save schema v2), chainsaw (slot 3 / schema v3), plasma + BFG + cells (slots 6/7, schema v4), exploding barrels. **Stage 7e Task 15** closed 2026-07-12: full suites **413 EditMode + 67 PlayMode**, E1M1–E1M9 smoke, Windows build, interactive sign-off (standalone music via `OnAudioFilterRead`).

**Stage 8 ✅ closed 2026-07-12** on branch `enhanced` (spec/plan `2026-07-11-enhanced-graphics`). Tasks 1–15: Classic baseline; settings v2; URP **17.4.0** Linear; Classic shaders + hot-switch; HUD isolation; Enhanced materials/normals; runtime sector lights + save schema **v5**; bounded dynamic lights/shadows (pool 16); post/MSAA; WAD sky sphere + fluids cross-fade + fog boot fix; Enhanced sprites/Spectre; pooled particles/decals; SRP perf; Windows standalone ~128 MB; interactive Classic/Enhanced eyeball sign-off (pause HUD hide, sharp Point Enhanced, muzzle/lamp/sky/fluid/fog QA). Full suites at Task 14 close: **459 EditMode + 95 PlayMode**.

**Enhanced Texture Upscaling (branch `upscale`, 2026-07-12):** runtime Scale2x 2× for Enhanced world albedo/sky + normals-from-2× + hot-switch. Automation green (**505 EditMode + 109 PlayMode**, Windows build). **Interactive finding: Scale2x does not give a meaningful visible texture-quality improvement** on Freedoom E1 walls/flats (pipeline OK; algorithm too weak vs axis-aligned WAD art). Not closed as visual success. Spec/plan `2026-07-12-enhanced-texture-upscaling`; notes `Logs/enhanced-texture-upscale-baseline-notes.md`.

**Enhanced Texture Quality (branch `texquality`) ✅ closed 2026-07-24.** Spec/plan `2026-07-21-enhanced-texture-quality` (+ warm-perf `2026-07-22`). Stack: pattern-gated dedither → Super-xBR 4× (MIT) → sprites/weapon/HUD 4× + Sharpen 0.5 → texel-AA → multi-scale normals + POM; parallel warm + session store + SHA-256 disk pack. **Interactive visual gate: SUCCESS** (graphics improved vs Scale2x reject). Suites: EditMode **601/601**, PlayMode **144/144**; Windows build ~128 MB; E1M1 cold disk warm ~2.8–3.7 s / compute ~14 s / level transitions 0 compute. Limits: menus/intermission native; Freedoom dedither ≈ no-op; POM solid opaque only; neural upscale deferred. Notes `Logs/enhanced-texture-quality-baseline-notes.md`.

**Sprite EdgeMix 8× ✅ closed 2026-08-02, ❌ removed entirely 2026-08-08** (from `main`; plan/spec `2026-07-31-inventory-edge-mix`). Deterministic 8× with 2+2-pixel premultiplied-alpha boundary mix (`AAAAAA|MMMM|BBBBBB`) for Enhanced pickups, enemies (incl. Spectre), and first-person weapons/muzzle flash; Classic stays native; projectiles/effects keep Super-xBR 4×; world/HUD/menus unchanged. Interactive verdict: SUCCESS — edges preserved vs Super-xBR.

**EdgeMix stage 2: contrast gate ✅ closed 2026-08-03** (same plan/spec, «Этап 2»). The unconditional mix band blurred intentional high-contrast detail (medkit cross, armor gem). `Scale8XContrastGated`: mix weight ramps by weighted RGB distance (0.30/0.59/0.11) — full mix ≤16, hard nearest ≥64; alpha silhouette always mixes (accepted band preserved); corners take the weakest pair weight. Gate 0 PNG sweep → user picked aggressive 16→64; pipeline **v4** (cache invalidated). Also cherry-picked `bb7075e` from `esrgan` (texel-aware sprite sampling `DOOM_SPRITE_TEXEL_AA`, `AlphaToMask`, snapped UI placement, Enhanced-boot race fix in `MapLoader`) — visually neutral verdict, kept for non-visual wins. Standalone visual gate: SUCCESS. Suites: EditMode **616/616**, PlayMode **149/149**; Windows build ~122 MB. Warm-perf baseline v4 recorded 2026-08-03: parity with pre-v4 (E1M1 first switch ~9.5 s cold≈warm, repeat ~8–21 ms; E1M7 ~9.9 s / ~9 ms; sprites 213 MB; managed ~1.1 GB; no mitigation), disk pack ~719 MB. Preview tooling: `Tools > Doom > Dump EdgeMix Gate Preview`; interactive-verdict protocol: before each user visual check, send было/стало PNG panels (medkit/armor/monster/shotgun set).

**Enhanced Pickup Pixel Extrusion ❌ REJECT 2026-08-05** (branch `pixel-extrusion`; spec/plan `2026-08-05-inventory-pixel-extrusion`). Uniform texel-column extrusion failed interactive Gate 1 for non-box pickups; experiment stopped — no Stage 2+ runtime routing. Enhanced pickups stay flat EdgeMix 8× billboards.

**Enhanced Parametric Pickups ❌ REJECT 2026-08-06** (branch `parametric-pickups`; spec/plan `2026-08-05-inventory-parametric-pickups`). Stage 0 toolchain ✅; Stage 1 MEDIA0 brief approved and candidates/drawing iterated, but visual gate not reached — user stopped: per-item parametric loop too slow and not approaching needed result. No Stage 2+, no runtime mesh routing. Enhanced pickups stay flat EdgeMix 8× billboards.

**TRELLIS.2 Enhanced 3D rollout ✅ LIVE TEST SUCCESS 2026-08-07** (status `docs/superpowers/plans/2026-08-06-trellis2-pickups.md`). Enhanced now routes textured TRELLIS.2 meshes for `MEDIA0`, voxel-styled `BON1A0`, all six weapon pickups (`SHOTA0`/`MGUNA0`/`LAUNA0`/`PLASA0`/`CSAWA0`/`BFUGA0`), `COLUA0` floor lamp, `BAR1A0` exploding barrel, `TRE2A0` large tree (54), and `SMITA0` leafy tree (47); Classic keeps native billboards and gameplay/collision/save identity stay on the original thing roots. Pickup/lamp/barrel interactive editor/standalone gates passed («результат отличный»): Resources-loaded `Doom/ExperimentalPickupUnlit` at exposure 1.0, medikit cross pulse, health-bonus HDR emission + bounded green light, lamp's existing sticky light, and barrel fallback to the BEXP billboard death sequence. Tree runtime wiring + WAD palette albedo correct + SectorFog on unlit models: interactive standalone gate SUCCESS 2026-08-07. Automation/build baseline before trees: **617 EditMode + 155 PlayMode**, Windows standalone SUCCESS. Raw models remain ~94–99k triangles each, so production decimation/performance acceptance is still open; `ARM1A0` is the next accepted pickup candidate but is not yet routed in runtime. Generation archaeology: MEDIA native SUCCESS, EdgeMix conditioning REJECT, STIMA REJECT 3/3, BON round-shape gate REJECT 2/2 but live stylized result accepted.

**TRELLIS.2 monster conditioning 2026-08-07.** Native/front/45° source sets prepared for `POSSA1`, `SPOSA1`, `TROOA1`, `SARGA1` (demon+spectre) and `BOSSA1`; no monster GLB/runtime gate yet. Tree finding (for archaeology): a nominal 45° redraw still reconstructs flat unless the image contains explicit radial roots, circumference-distributed branches, foreshortening and front/rear overlap. Workflow is persisted in `.cursor/rules/trellis2-shape-hints.mdc`.

**AI-derived assets doctrine ✅ adopted 2026-08-07** (spec `2026-08-07-enhanced-ai-derived-assets-design.md`, brainstorm outcome). WAD stays the source of identity and runtime truth (no unpacking); Enhanced is a derived layer of AI assets keyed to WAD lumps. Pipeline: native sprite → conditioning redraw (currently GPT-5.6 SOL; shape-hint v2 with explicit 3D topology when TRELLIS reconstructs flat) → TRELLIS.2 → interactive gate. Central gate criterion is **authenticity balance**: improvement that fits the pixel-art world (over-detailed assets read as foreign — the "Heroes 3 3D-remake" failure mode; under-detailed ones disappoint); mitigations: Classic untouched + hot-switch, per-asset allowlist reversibility, stylization tools (voxel look, WAD-palette albedo remap, texture-size caps). Asset lifecycle Draft → Accepted → Finalized: decimation (~95k → 3–10k tris) is a **finalization wave** over stabilized Accepted assets, not a per-iteration step. ~~EdgeMix 8× is a transitional fallback~~ — superseded: EdgeMix removed entirely 2026-08-08 (see below), the Enhanced fallback for lumps without AI assets is now the **native sprite**. Monsters are a separate experimental track (need rigging/animation, strictest authenticity gate); AI music is a future track under the same doctrine.

**EdgeMix 8× removed entirely ❌ 2026-08-08** (branch `3d-toggle`, user decision). Reason: EdgeMix blurred the picture and made honest visual judgement of Enhanced 2D impossible («ошибки восприятия»); with display-grade redraws coming quickly, the transitional fallback was cut wholesale instead of per-lump. Enhanced fallback for lumps without AI assets is now the **native sprite (Point)**: cascade mesh→redraw→native in both 3D On/Off; projectiles/effects keep Super-xBR 4×; Classic untouched. Deleted: `EdgeMixUpscaler` (+contrast gate), `EnhancedJobKind` 4–6 (+codec cases), `WorldTextureVariant` 3–5, `ObjectPresentation.EdgeMixBillboard`, `Tools > Doom > Dump EdgeMix Gate Preview`; Gate-1 preview menu now native|redraw. Pipeline **v6** — stale v5 packs (~719 MB) are auto-dropped by `CleanupStalePacks`. Suites: EditMode **614/614**, PlayMode **160/160** (15 EdgeMix tests deleted, 8× expectations rewritten to native).

**Enhanced 3D Objects Toggle ✅ closed 2026-08-07** (branch `3d-toggle`; spec/plan `2026-08-07-enhanced-3d-objects-toggle`). Options «3D Objects» On/Off (default On, settings schema **v3**), visible only in Enhanced; hot-apply. Presentation cascade (post EdgeMix removal 2026-08-08): Classic native → Enhanced+3D On mesh→redraw→native → Enhanced+3D Off redraw→native. Gate 0 allowlist (Point): `ARM1A0`/`BAR1A0`/`BFUGA0`/`COLUA0`/`CSAWA0`/`LAUNA0`/`MGUNA0`/`PLASA0`/`SHOTA0` (`STIMA0-v3` REJECT; `BAR1` A0+B0 stays native until both frames covered; v2/45° never shown in-game — same for `ARM1` A0+B0, the frame-count gate in `SpriteCache` blocks partial redraws of animated lumps). Runtime: `Assets/Resources/EnhancedSprites/`, `ObjectPresentationResolver`, `ExperimentalPickupModel` third state, `SpriteCache` `EnhancedDisplayRedraw`. Gate 1 standalone SUCCESS. Suites: EditMode **629/629**, PlayMode **160/160**; Windows build ~182 MB.

- **Stage 0:** Unity project scaffolded at the repo root, Git initialized (branch `main`), Freedoom Phase 1 placed under `Assets/StreamingAssets/wads/freedoom1.wad`.
- **Stage 1:** WAD reader implemented in `Assets/Scripts/Wad/` (asmdef `Doom.Wad`, `noEngineReferences: true`). Public surface: `WadFile.Open(path)`, `Header`, `Directory`, `FindLump`, `ReadLump`, plus `WadMapNames.IsMapMarker`. Editor menu `Tools > Doom > Dump freedoom1.wad` confirms it reads the bundled WAD end-to-end. 30 EditMode tests (unit + integration) pass via Unity Test Framework.
- **Stage 2:** Map geometry pipeline implemented in `Assets/Scripts/Map/` (asmdef `Doom.Map`, `noEngineReferences: true`) and `Assets/Scripts/MapBuild/` (Unity glue). High-level API: `MapData.Load(wad, mapName)` → `MapGeometryBuilder.Build(map)` → per-sector `SectorMeshes`. Triangulation via vendored LibTessDotNet v1.1.15 (`Assets/ThirdParty/LibTessDotNet/`). `MapLoader` MonoBehaviour with auto-bootstrap renders E1M1 grey block-out on Play in `Stage2_MapPreview.unity`. 62 EditMode tests pass including freedoom1.wad integration coverage.
- **Stage 3:** Player + collisions. `MapLoader` spawns Player from `THINGS` Type 1 at scene start; `PlayerController` MonoBehaviour (`Doom.MapBuild`) uses the new Input System (`com.unity.inputsystem`, hand-built `InputActionMap`) for WASD + mouse-look + Shift-sprint. `worldScale = 1/32` plumbed through `MapGeometryBuilder` so the player is ~1.75 m tall and gravity = -9.81 m/s² works natively. A PlayMode test asserts the player lands on E1M1's floor without falling through.
- **Stage 4:** Palette and textures. New pure-C# assembly `Doom.Graphics` (`noEngineReferences`) decodes `PLAYPAL`, flats, patches, and `TEXTURE1`/`TEXTURE2`+`PNAMES` into `DecodedImage` (RGBA32). `Doom.Map` gained UV + DOOM pegging, per-sector vertex light, texture-grouped `WallSection`s, two-sided middle quads, and F_SKY1 ceiling skipping. `Doom.MapBuild` adds `TextureCache` (Texture2D Point+mipmap+anisotropic) and two Unlit shaders (`Doom/Unlit`, `Doom/UnlitCutout`); `MapLoader` assigns materials and writes UV + vertex colors. Sky and animation deferred.
- **Stage 5:** Sprite objects. New pure-C# assembly `Doom.Things` (`noEngineReferences`) ports the full DOOM `mobjinfo` table (`doomednum → sprite, frame, radius, height, flags`). `Doom.Graphics` gained `SpriteSet` (catalogs `S_START`/`S_END`, parses frame/rotation/mirror naming) and `Patch.ReadHeader` (size + draw offsets). `Doom.MapBuild` adds `SpriteCache` (sprite lump → cutout `Material`, pre-warmed while the WAD is open), `SpriteBillboard` (cylindrical facing + 8-rotation selection by view angle), and `ThingSpawner` (places billboards from `THINGS`, raycast floor/ceiling anchor, `CapsuleCollider` for SOLID things); `MapLoader` invokes it after geometry. Frame animation, AI, and item pickup deferred to Stage 6.
- **Stage 6a (doors & interactive sectors).** New pure-C# assembly `Doom.Specials` (`noEngineReferences`) ports the DOOM linedef-type table: `LineSpecialTable` (special number → `LineSpecial` kind/trigger/etc.) plus `SectorActions` (door/lift/floor target heights, stair builds) and `Neighbors` (sector adjacency via shared two-sided linedefs). `Doom.Map` gained `ISectorHeights`/`StaticSectorHeights` and a runtime-height `RebuildSector` (regenerates a sector's floor/ceiling/wall `MeshData` from live heights). `Doom.MapBuild` adds `RuntimeSectorHeights` (mutable per-sector heights), `SectorGeometry` (in-place mesh + collider rebuild registry), `SectorMover` (coroutine animating heights over time), and `LineActivator` (+`LineRef`) wiring Use/Walk/Switch triggers to sector actions; `PlayerController` gained a "Use" input (E key) that raycasts for a `LineRef`. Wall→linedef resolution for texture-grouped sections is geometric (sector-tagged `LineRef` + nearest-segment match) rather than a 1:1 mapping. Key checks deferred from 6a are done in 6e; Stage 7e added exits/teleports; Stage 8 Task 8 executes light specials. Still deferred from early 6a notes: crusher, scroll execution.
- **Stage 6b (player damage & HP).** New pure-C# assembly `Doom.Game` (`noEngineReferences`) holds `HealthModel` — HP/armor with DOOM armor absorption (green armor soaks 1/3 of damage, blue 1/2). `Doom.Specials` gained `SectorDamageTable` (sector special → damage-per-tic: nukage 5, slime 10, lava 20). `Doom.MapBuild` adds `SectorRef` (sector index on floor GOs), `PlayerHealth` (model wrapper + `Died` event), `FloorDamageSystem` (timed floor damage while grounded — downward raycast → `SectorRef`, every ~0.9 s), `PlayerDeathHandler` (freeze input + "You died" overlay + R-respawn), and `PlayerHud` (debug HP/armor OnGUI); all wired in `MapLoader.SpawnPlayer`. Deferred in 6b: radiation suit, special-11 level exit, secret sectors, armor/health pickups, crush damage, real HUD.
- **Stage 6c (weapons & shooting).** Four DOOM hitscan weapons (fist/pistol/shotgun/chaingun) with free aim. `Doom.Game` gained the pure rules: `DoomRandom` (P_Random port, 256-byte rndtable), `AmmoModel` (bullets/shells with DOOM start/max), `WeaponDef`/`WeaponTable` (frame/tic data from p_pspr.c/info.c state tables), `WeaponLoadout` (ownership, auto-select in P_CheckAmmo order), and `HitscanRules` (P_GunShot 5/10/15 and A_Punch 2–20 damage rolls, ±5.6° spread, 7-pellet shotgun volley). `Doom.Things` `ThingDef` gained `Health`/`CorpseFrame` for the E1 monsters (info.c). `Doom.MapBuild` adds `PlayerWeapons` (LMB + slot-keys 1–4 input, tic-based cooldowns, hitscan raycasts from the camera, ammo consumption + auto-downgrade, weapon/ammo pickups), `EnemyHealth` (HP; at zero swaps the billboard to its corpse frame and disables the collider), `HitEffect` (short-lived PUFF/BLUD billboards), `WeaponView` (OnGUI viewmodel on a virtual 320×200 screen per R_DrawPSprite, fire frames + muzzle flash + movement bob), and `ThingPickup` (trigger-sphere pickup for shotgun/chaingun/ammo things); `PlayerHud` shows AMMO; respawn resets the loadout. Notable fixes from the 6c review cycles: billboard quad meshes are destroyed with their GameObject (leak), `SpriteCache` caches negatives and warns on use after WAD close, the Use raycast ignores pickup triggers, shots originate at the camera (no point-blank dead zone), and weapon input freezes on death. Exploding barrels (2035) added post-6c (2026-07-11): HP 20, BEXP splash via `RadiusDamageRules`/`BarrelExplosion`, DSBAREXP. Still deferred from 6c: damage on the Nth tic of the fire state (applied on press instead), weapon-lower on death, keeping the loadout across respawn, queued weapon switch during cooldown.
- **Stage 6d (monster AI).** E1 roster (POSS/SPOS/TROO/SARG) wakes on sight/damage/gunfire noise, chases with DOOM 8-dir steps, opens doors, attacks (hitscan/melee/missile), infights, and dies with frame animation. `MonsterDef`/`MonsterTable` in `Doom.Things`; `MonsterRules`, `ChaseDir`, `MonsterBrain`+`IMonsterWorld` in `Doom.Game`; `NoiseAlert` in `Doom.Specials`. Unity glue — `SpriteBillboard.SetFrame`, `Projectile` (imp fireball), `MonsterController` (35 tics/s, capsule steps, sight, attacks, door use via `LineActivator`), `DamageSource`+infighting in `EnemyHealth`, `ThingSpawner`/`MapLoader` wiring, `NoiseAlertSystem` (gunfire wakes heard sectors); PlayMode `MonsterAiPlayTests` (5 tests, commit `14ae50f`). **Facing fix (2026-07-10):** `SpriteBillboard` rotation was `+202.5°` (180° off Stage 5's `+22.5°`), so a monster facing the camera always picked rot 4 (back) instead of rot 0 (front) — most visible on hitscan zombies; fixed to `+22.5°` with `MonsterController.Face()` each render frame while chasing. Visual sign-off (Task 12) confirmed interactively on E1M1. Deferred: XDEATH, flying monsters, sound (6f), A_Look chain-wake, ML_SOUNDBLOCK. Spec `docs/superpowers/specs/2026-07-03-monster-ai-design.md`; plan `docs/superpowers/plans/2026-07-03-monster-ai.md`.
- **Stage 6e (pickups).** Full E1 item set via pure `ItemRules` router: health (stim/medi/bonus/soul), armor (green/blue/bonus), six keys, backpack (×2 max + clip grant), berserk (heal-to-100, fist ×10, prefer fist), radiation suit (2100 tics). `HealthModel` gained GiveHealth/GiveArmor/GiveArmorBonus; new `KeyInventory`/`PlayerKey`, `PlayerPowers`, `DeathDropTable` (POSS→CLIP, SPOS→SHOTGUN); `AmmoModel` backpack. Unity: `PlayerInventory` facade, `ThingPickup`/`ThingSpawner` for all E1 pickups, `KeyMapping` + `LineActivator` key gate (card ≠ skull), ironfeet skip in `FloorDamageSystem`, `PickupFactory` corpse drops, HUD KEYS/BERSERK/SUIT. Respawn keeps keys, clears powers/backpack/ammo/weapons. PlayMode `PickupPlayTests` ×5. Deferred: invuln/invis/map/light-amp, rockets/cells/new weapons, pickup sound (6f), item spin anim, real HUD (Stage 7). Spec `docs/superpowers/specs/2026-07-10-pickups-design.md`; plan `docs/superpowers/plans/2026-07-10-pickups.md`.
- **Stage 6f (sound + music) ✅.** Pure-C# `Doom.Audio`: DMX `DS*` decoder; MUS + SMF MIDI parsers (`MusicScore` auto-detects — Freedoom `D_*` lumps are Type-1 MIDI, retail DOOM uses MUS); `GENMIDI` bank; `MusOplPlayer` (LittleMUS-derived sequencer) driving vendored managed Nuked OPL3 (`Assets/ThirdParty/NukedOpl/`, LGPL-2.1, commit `dc939ea`). `Doom.MapBuild`: `SoundCache`/`SoundSystem` (2D/3D pool), `MusicPlayer` (OPL via `OnAudioFilterRead` + silent carrier clip on a child of `PlayerCamera` — streaming `AudioClip` PCM callbacks are silent in Windows standalone), table-driven SFX pre-warm. Wired: weapons, pickups, player pain/death, monster FSM cues, projectile impact, doors/lifts/switches/key-deny, map music loop. Spec `docs/superpowers/specs/2026-07-10-sound-design.md`; plan `docs/superpowers/plans/2026-07-10-sound.md`. Known limits: no DMX channel stealing/pitch-shift, no sector occlusion.
- **Stage 7a (game session & level flow) ✅.** Pure `CampaignRoute`/`CampaignMap`/`SessionState`/`PlayerCarryState`; `GameSessionHost` (DontDestroyOnLoad); exit linedefs 11/51/52/124 + sector special 11; `LevelTransitionController` reloads `Stage2_MapPreview` with inventory carry-over (keys/powers cleared). Spec/plan: `2026-07-10-polish`.
- **Stage 7b (HUD & intermission) ✅.** WAD UI pipeline (`UiPatchCatalog`, `HudTextureCache`, `VirtualScreenRenderer`); pure `HudModel`/`FaceRules`/`FaceState`; `DoomHud` replaces debug `PlayerHud` (STBAR, tall/small digits, arms, keys, face); `LevelStats`/`LevelStatsTracker` + `IntermissionView` on exit. Fixes: health/armor `%` anchor (st_lib), ML_BLOCKING colliders on masked midtextures (grates). Interactive sign-off on E1M1. Spec/plan: `docs/superpowers/specs/2026-07-10-polish-design.md`, `docs/superpowers/plans/2026-07-10-polish.md`.
- **Stage 7c (menus & settings) ✅.** `GameFlowController` owns freeze/cursor/timescale (Playing/Paused/Dead/Intermission/MainMenu); WAD `MenuController` (main/pause); `GameSettingsData` + `SettingsStore`/`SettingsController` (SFX/music volume, sensitivity, invert Y, fullscreen); runtime volume + music pause/resume without sequencer restart. PlayMode `MenuPlayTests` ×7 + `SettingsPlayTests` ×3; EditMode `GameSettingsTests` ×5.
- **Stage 7d (full-world saves) ✅.** Pure `SaveGame`/`PlayerSnapshot`/`WorldSnapshot`/`SaveEntityId` + `SaveGameCodec`/`SaveSlotStore`; Unity `WorldStateRegistry`/`WorldSnapshotCapture`/`WorldSnapshotRestore`/`SaveGameController`; pause-menu Save and main/pause Load with WAD-identity preflight; scene reload then phased restore. PlayMode `WorldCapturePlayTests` ×2 + `SaveLoadPlayTests` ×3; EditMode snapshot/codec/store coverage in `Doom.Game.Tests` (179).
- **Stage 7e (E1 specials / perf / build) ✅.** Task 14: `TeleportRules`/`TeleportExecutor` (97/125/126), telefrag; spectre(58)+baron(3003); no E1 ProgressionBlockers; `E1SpecialsPlayTests` ×2. Task 15: `WalkLineIndex` + LineRef cache, `E1MapSmokePlayTests` (E1M1–E1M9), baseline `Logs/stage7e-baseline-notes.md`, `Tools > Doom > Build Windows Standalone` → `Builds/Windows/`, full suites **413 EditMode + 67 PlayMode** (2026-07-12), interactive sign-off (standalone music fix). Deferred visual: scroll 48, spectre MF_SHADOW translucency (linedef light 138 executed in Stage 8 Task 8).
- **Stage 8 (Classic/Enhanced graphics) ✅.** Spec/plan: `2026-07-11-enhanced-graphics`. Closed 2026-07-12: Tasks 1–15 including interactive Classic/Enhanced eyeball sign-off on standalone (URP 17.4.0, GraphicsMode settings v2, WAD sky/fluids/fog, lamps/particles, pause HUD isolation, Windows build). Suites at Task 14: **459 EditMode + 95 PlayMode**.

The master roadmap is in `docs/doom-unity-remake-plan.md` (written in Russian). Each stage has a paired design spec under `docs/superpowers/specs/` (`YYYY-MM-DD-<topic>-design.md`, the WHAT/WHY) and an implementation plan under `docs/superpowers/plans/` (`YYYY-MM-DD-<topic>.md`, task-by-task HOW): Stage 1 = `2026-05-28-wad-reader`, Stage 2 = `2026-05-28-geometry`, Stage 3 = `2026-05-29-player`, Stage 4 = `2026-05-30-textures`, Stage 6a = `2026-05-31-doors`, Stage 6b = `2026-05-31-player-damage`, Stage 6c = `2026-07-02-weapons`, Stage 6d = `2026-07-03-monster-ai`, Stage 6e = `2026-07-10-pickups`, Stage 6f = `2026-07-10-sound`, Stage 7 = `2026-07-10-polish` (spec adds the `-design` suffix), Stage 8 = `2026-07-11-enhanced-graphics`, post-8 texture quality = `2026-07-21-enhanced-texture-quality`, AI-derived assets doctrine = `2026-08-07-enhanced-ai-derived-assets`, 3D Objects toggle = `2026-08-07-enhanced-3d-objects-toggle`. **Stages 0–8 complete;** Enhanced Texture Quality ✅ closed 2026-07-24; 3D Objects toggle ✅ closed 2026-08-07 on `3d-toggle`.

## What this project is

A from-scratch reimplementation of DOOM in **Unity + C#** — a REMAKE, not just a port. The original WAD files supply the data (geometry, textures, sprites, sounds); all rendering, physics, and game logic are written anew on top of Unity. Two presentation modes: **Classic** (bit-faithful WAD rendering — nothing but WAD data on screen) and **Enhanced** (Unity-powered visuals that must preserve the original's style). Since 2026-08-07 Enhanced may additionally route **AI-derived presentation assets** (currently TRELLIS.2 3D meshes generated from WAD sprites); doctrine in `docs/superpowers/specs/2026-08-07-enhanced-ai-derived-assets-design.md`.

Development and distribution use **Freedoom** (free, format-compatible) rather than the commercial `DOOM.WAD`.

## Architectural ground rules

These decisions are load-bearing for the whole project — keep them in mind when designing any subsystem:

- **The original BSP data is intentionally discarded.** `NODES`, `SSECTORS`, `SEGS`, `BLOCKMAP`, `REJECT` are not read or used. Unity handles visibility and physics. Only the raw geometry lumps (`VERTEXES`, `LINEDEFS`, `SIDEDEFS`, `SECTORS`, `THINGS`) feed the level builder. Sector outlines are reconstructed by `SectorPolygonBuilder` (half-edge chaining with signed-area classification) — not by reading BSP segments.
- **Do not port the original C engine.** Port data formats and gameplay logic (sector behaviour, AI, weapon rules), not the renderer or BSP traversal.
- **WAD is the source of identity; Enhanced is a derived layer** (doctrine spec `2026-08-07-enhanced-ai-derived-assets-design.md`, supersedes the old "do not author custom content" rule). Classic renders only WAD data, bit-faithful, always. Maps, gameplay, collision, and save identity come from the WAD in every mode — AI never touches them. Enhanced may route AI-derived presentation assets, but each one is keyed to its source WAD lump, goes through the documented conditioning→generation→interactive-gate pipeline with provenance committed under `Textures/Trellis2/`, and stays presentation-only on the original thing root. The WAD is never unpacked into loose asset folders; the derived-asset registry lives on top of it. The central acceptance criterion is **аутентичность**: an asset must read as an object of the same pixel-art world — volume added to the original, not photorealism that outclasses the surroundings.
- **WAD binary format is little-endian.** Read with `BinaryReader` and stay consistent.
- **Wall texture pipeline is multi-step.** Wall textures are composed from patches via `TEXTURE1`/`TEXTURE2` + `PNAMES`, in column-major format with transparency. Flats (floor/ceiling) are raw 64×64 palette indices. Both go through `PLAYPAL` (256-color palette) before becoming `Texture2D`.
- **Sprites are camera-facing billboards**, with frame/angle encoded in the lump name.
- **World scale is `1 DOOM unit × worldScale = 1 Unity meter`**, with `worldScale = 1f / 32f` as the project default. Plumbed through `MapGeometryBuilder` so `MeshData` outputs are already in meters; `MapLoader` does not re-scale. Player height (56 DOOM units) becomes ~1.75 m; gravity is -9.81 m/s² natively.

## Roadmap shape

The plan (`docs/doom-unity-remake-plan.md`) is structured as stages 0–8, each ending in a visible, runnable milestone:

0. Environment setup → 1. WAD reader → 2. Grey block-out geometry of one map → 3. First-person player with collisions → 4. Palette and textures → 5. Sprite objects → 6. Gameplay (HP, weapons, AI, doors, sound) → 7. Polish (HUD, menus, saves, optimization) → 8. Classic/Enhanced graphics (URP).

Prefer landing each stage as its own visible milestone rather than building several layers in parallel — the plan explicitly calls out that most DOOM remakes die on textures, sprites, and AI, so each step should produce something you can see and run.

## Project layout

- `Assets/` — Unity assets (scripts, scenes, meshes — once we write them)
- `Assets/Materials/MapBlockOut/` — three Standard-shader materials (`Floor.mat`, `Ceiling.mat`, `Wall.mat`) for grey block-out rendering.
- `Assets/Scenes/Stage2_MapPreview.unity` — preview scene; auto-bootstrap creates `MapLoader` on Play, loads E1M1 from `freedoom1.wad`.
- `Assets/Scripts/Map/` — WAD geometry → C# `MeshData` (asmdef `Doom.Map`, `noEngineReferences: true`). Houses `MapData` (lump parsers + `Load`), `SectorPolygonBuilder` (half-edge chaining), `SectorTriangulator` (LibTessDotNet wrapper), `WallMeshBuilder`, and the `MapGeometryBuilder` orchestrator.
- `Assets/Scripts/MapBuild/` — Unity-coupled glue (asmdef `Doom.MapBuild`). `MapLoader` MonoBehaviour turns `MeshData` into `UnityEngine.Mesh`, instantiates per-sector `GameObject` hierarchy with `MeshFilter`/`MeshRenderer`/`MeshCollider`, and auto-bootstraps on scene load.
- `Assets/Scripts/Wad/` — WAD parser. Pure C# under asmdef `Doom.Wad` with `noEngineReferences: true` (the parser must not depend on `UnityEngine` — see architectural rules above).
- `Assets/Scripts/Wad/Editor/` — Editor-only tools (asmdef `Doom.Wad.Editor`, `includePlatforms: ["Editor"]`). Currently houses `WadInspectorMenu` (`Tools > Doom > Dump freedoom1.wad`).
- `Assets/StreamingAssets/wads/` — WAD files, copied verbatim into builds. **WADs live here, not in regular `Assets/`**, because anything else under `Assets/` goes through Unity's importer, which would try to interpret the binary as some asset type. `StreamingAssets` is the one Unity folder that ships files unchanged and is reachable at runtime via `Application.streamingAssetsPath`.
- `Assets/Tests/EditMode/Map/` — NUnit EditMode tests for the Map/MapBuild pipeline (asmdef `Doom.Map.Tests`): unit tests on `SyntheticMapBuilder`-built lumps plus `freedoom1.wad` integration/regression coverage (see the test-suite breakdown under "Build, run, test").
- `Assets/Tests/EditMode/Wad/` — NUnit EditMode tests (asmdef `Doom.Wad.Tests`). Unit tests use a `SyntheticWadBuilder`; integration tests run against `freedoom1.wad`.
- `Assets/Tests/PlayMode/` — Unity PlayMode tests (asmdef `Doom.Stage3.PlayTests`). Runs in Play context; required for verifying `CharacterController` + collider integration. Run WITHOUT `-nographics` (PhysX needs a real/null gfx device for collision callbacks).
- `Assets/Scripts/Audio/` — pure-C# WAD audio (`Doom.Audio`, `noEngineReferences`): DMX SFX, MUS/MIDI score parsers, GENMIDI, `MusOplPlayer`.
- `Assets/ThirdParty/LibTessDotNet/` — vendored LibTessDotNet v1.1.15 sources (SGI Free Software License B 2.0). Asmdef `LibTessDotNet` with `noEngineReferences: true`, `autoReferenced: false`.
- `Assets/ThirdParty/NukedOpl/` — vendored managed Nuked OPL3 (LGPL-2.1, SaxxonPike/NukedOpl `dc939ea`). Asmdef `NukedOpl`, `noEngineReferences: true`, `autoReferenced: false`.
- `Packages/`, `ProjectSettings/`, `UserSettings/` — Unity project metadata (committed; `UserSettings/` ignored per gitignore).
- `Library/`, `Temp/`, `Logs/`, `obj/` — Unity-generated, gitignored.
- `docs/` — design docs, including the stage-by-stage plan.

## Toolchain

- **Unity Editor:** `6000.4.8f1` at `C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe`. The version is pinned by `ProjectSettings/ProjectVersion.txt`; Unity Hub will auto-pick this editor when the project is opened.
- **Test WAD:** Freedoom v0.13.0 Phase 1 (`Assets/StreamingAssets/wads/freedoom1.wad`, ~28 MB), freely redistributable, so it is intentionally checked into Git — the repo should clone-and-run.

## Build, run, test

Day-to-day work happens inside the Unity Editor (open the repo root as a Unity project from Hub). No player build target is configured yet — that's a Stage 7 concern.

Useful CLI invocations:

```powershell
# Open the project in the editor (interactive)
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" -projectPath "D:\Development\doom"

# Headless: run EditMode tests (Unity Test Framework, NUnit-style)
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" `
    -batchmode -nographics -projectPath "D:\Development\doom" `
    -runTests -testPlatform EditMode `
    -testResults "D:\Development\doom\Logs\test-results.xml" `
    -logFile "D:\Development\doom\Logs\test-run.log"

# Headless: run PlayMode tests (slower; physics needs no -nographics)
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" `
    -batchmode -projectPath "D:\Development\doom" `
    -runTests -testPlatform PlayMode `
    -testResults "D:\Development\doom\Logs\playmode-results.xml" `
    -logFile "D:\Development\doom\Logs\playmode-run.log"

# Headless: execute the WAD dump menu without GUI
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" `
    -batchmode -nographics -projectPath "D:\Development\doom" `
    -executeMethod Doom.Wad.Editor.WadInspectorMenu.DumpFreedoom1 `
    -logFile "D:\Development\doom\Logs\dump.log" -quit
```

**Test CLI gotcha:** `-runTests` controls its own exit; do NOT add `-quit` with it (Unity exits before the runner starts). Per-test PASS/FAIL only lands in the `-testResults` XML, not the editor log. Use `-quit` only with `-executeMethod` or pure-compile runs.

The current test suite is **614 EditMode + 160 PlayMode** tests (EdgeMix removal, 2026-08-08). Historical breakdown below remains useful for archaeology; totals grew with 7a–7e, rockets/chainsaw/plasma-BFG, E1 smoke/specials, Stage 8, upscale, texquality, warm-perf, EdgeMix (later deleted), and the 3D Objects toggle:
- 30 from Stage 1 (WAD reader): 4 integration tests against `freedoom1.wad`, the rest unit tests on `SyntheticWadBuilder`-built blobs.
- 32 from Stage 2 (Map pipeline): 5 integration tests against `freedoom1.wad`, the rest unit tests on `SyntheticMapBuilder`-built lumps.
- 9 from Stage 3 EditMode (Player): THINGS parsing (+2), THINGS as required lump in Load (+2), worldScale plumbing (+4), Freedoom Player-1-start integration (+1), plus 1 PlayMode test asserting the player lands on E1M1's floor.
- 2 geometry-robustness regressions on `freedoom1.wad` E1M1: `SectorPolygonBuilder` must produce simple (non-self-intersecting) sector rings, and `MapGeometryBuilder` output must contain no degenerate (zero-area) triangles. These pin the fixes for self-intersecting sector contours (greedy chaining → angular face-tracing) and zero-area wall quads.
- 6 winding/orientation regressions (`SectorWindingTests` ×5, `WallUvPeggingTests` ×1). Unity draws the side a triangle's `Cross(p1-p0, p2-p0)` points toward, so orientation must be pinned ABSOLUTELY: floors face up and ceilings face down even for sectors made of several disjoint rings (LibTess auto-computes its projection normal from the NET signed area of all contours, and misclassified disjoint "holes" — alternating stair steps sharing one sector number, E1M1 74/85 — flipped whole sectors inside-out); wall quads face INTO their owning sector (`WallMeshBuilder.EmitQuad` used to wind every wall visible only from the wrong side of the line — rectangular DOOM architecture masked it by showing far-face backs); back-side wall U runs V2→V1 (DOOM draws back segs reversed; else textures mirror for the back-sector viewer). These fixed the "background-colored holes" the stair/crate areas showed from Stage 2 onward.
- 24 from Stage 4 (palette & textures): 13 in `Doom.Graphics.Tests` (Palette, Flat, Patch, TextureSet unit tests on `SyntheticGfxBuilder` blobs + `freedoom1.wad` integration) and 11 added to `Doom.Map.Tests` (MeshData UV/Colors, flat UV+light, wall UV+pegging). The 2nd PlayMode test is the textured-E1M1 build smoke test.
- 14 from Stage 5 (sprites): 8 in `Doom.Things.Tests` (ThingDef flags, ThingTable lookups, every-Doom-1-sprite-resolves-in-freedoom1.wad regression + the Doom-2-only-gap guard) and 6 in `Doom.Graphics.Tests` (Patch header offsets, SpriteSet name/rotation/mirror parsing + freedoom integration). 2 added PlayMode tests assert E1M1 spawns billboards with renderers/colliders and that player starts spawn no sprite.
- 13 EditMode + 1 PlayMode from Stage 6a (doors & interactive sectors): 12 in `Doom.Specials.Tests` (`LineSpecialTests` ×1, `LineSpecialTableTests` ×5, `LineSpecialCoverageTests` ×1 — every E1M1 linedef special resolves in the table, `NeighborsTests` ×1, `SectorActionsTests` ×4) and `RuntimeHeightRebuildTests` ×1 in `Doom.Map.Tests` (rebuilding a sector from runtime heights moves its floor/ceiling mesh). The added PlayMode test is `SectorActionPlayTests` (a door's ceiling rises programmatically on activation).
- 13 EditMode + 3 PlayMode from Stage 6b (player damage & HP): 7 in `Doom.Game.Tests` (`HealthModel` — HP clamp/death, green/blue armor absorption + depletion) and 6 in `Doom.Specials.Tests` (`SectorDamageTable` — nukage/slime/lava damage-per-tic mapping + non-damaging specials). The 3 added PlayMode tests (`PlayerDamagePlayTests`) assert floor damage drains HP while grounded, death fires + freezes the player, and R-respawn restores HP.
- 19 EditMode + 3 PlayMode from Stage 6c (weapons & shooting): 17 in `Doom.Game.Tests` (`DoomRandomTests` ×3 — rndtable values/wrap/byte range, `AmmoModelTests` ×3 — start/max clamp, consume, reset, `WeaponTableTests` ×2 — def consistency + p_pspr.c cadence, `WeaponLoadoutTests` ×5 — start/give/select/P_CheckAmmo/reset, `HitscanRulesTests` ×4 — damage rolls, shotgun volley, first-shot accuracy) and 2 in `Doom.Things.Tests` (`MonsterDataTests` — E1 monster health/corpse frames + corpse sprites resolve in freedoom1.wad). The 3 PlayMode tests (`WeaponPlayTests`) assert the pistol kills a synthetic enemy and drains ammo, empty ammo auto-switches to fist, and a shotgun pickup gives the weapon + shells. NOTE: the `StairCaptureTests` harness renders via `Camera.Render()`, which excludes OnGUI/IMGUI — the viewmodel/HUD never appear in its PNGs, so `WeaponView` placement is not eyeball-verifiable headlessly. It was verified interactively in the editor instead, which caught a horizontal offset bug in the R_DrawPSprite port (a dropped `-160` term in `DrawPatch`; fixed).
- 4 PlayMode from the post-6b fix wave: `SectorTranslatePlayTests` (door ceiling is translated, not re-created — same mesh instance), `SectorRetriggerPlayTests` (lift re-triggers after a full cycle), `PlayerLiftRiderPlayTests` (player rides a rising floor), and `StairCaptureTests` (headless render-capture harness → `Logs/stair-spawn.png` + `Logs/stair-capture.png`; a diagnostic tool, re-run it to eyeball rendering changes).
- 31 EditMode from Stage 6d Tasks 1–6 (monster AI, pure C#): `MonsterTableTests` ×3, `MonsterRulesTests` ×4, `ChaseDirTests` ×6, `MonsterBrainWakeTests` ×7, `MonsterBrainCombatTests` ×8, `NoiseAlertTests` ×3 in `Doom.Game.Tests` / `Doom.Things.Tests` / `Doom.Specials.Tests`. **5 PlayMode** from Stage 6d Task 11: `MonsterAiPlayTests` (noise wake, melee/hitscan damage, imp fireball, death animation, infighting retarget).
- 22 EditMode + 5 PlayMode from Stage 6e (pickups): `HealthModel` give-API (+4), `KeyInventoryTests` ×3, `AmmoModelBackpackTests` ×3, `PlayerPowersTests` ×2, `ItemRulesTests` ×8, `DeathDropTableTests` ×1, berserk punch in `HitscanRulesTests` (+1). PlayMode `PickupPlayTests` ×5 (stim, green armor, locked door key gate on E1M2, POSS→CLIP drop, ironfeet blocks floor damage).
