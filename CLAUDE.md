# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

Stages 0–8 are done (6a–6f including music; 7a–7e including E1 standalone; Stage 8 Classic/Enhanced graphics). Post-6c expansions: rocket launcher (slot 5, save schema v2), chainsaw (slot 3 / schema v3), plasma + BFG + cells (slots 6/7, schema v4), exploding barrels. **Stage 7e Task 15** closed 2026-07-12: full suites **413 EditMode + 67 PlayMode**, E1M1–E1M9 smoke, Windows build, interactive sign-off (standalone music via `OnAudioFilterRead`).

**Stage 8 ✅ closed 2026-07-12** on branch `enhanced` (spec/plan `2026-07-11-enhanced-graphics`). Tasks 1–15: Classic baseline; settings v2; URP **17.4.0** Linear; Classic shaders + hot-switch; HUD isolation; Enhanced materials/normals; runtime sector lights + save schema **v5**; bounded dynamic lights/shadows (pool 16); post/MSAA; WAD sky sphere + fluids cross-fade + fog boot fix; Enhanced sprites/Spectre; pooled particles/decals; SRP perf; Windows standalone ~128 MB; interactive Classic/Enhanced eyeball sign-off (pause HUD hide, sharp Point Enhanced, muzzle/lamp/sky/fluid/fog QA). Full suites at Task 14 close: **459 EditMode + 95 PlayMode**.

**Enhanced Texture Upscaling (branch `upscale`, 2026-07-12):** runtime Scale2x 2× for Enhanced world albedo/sky + normals-from-2× + hot-switch. Automation green (**505 EditMode + 109 PlayMode**, Windows build). **Interactive finding: Scale2x does not give a meaningful visible texture-quality improvement** on Freedoom E1 walls/flats (pipeline OK; algorithm too weak vs axis-aligned WAD art). Not closed as visual success. Spec/plan `2026-07-12-enhanced-texture-upscaling`; notes `Logs/enhanced-texture-upscale-baseline-notes.md`.

**Enhanced Texture Quality (branch `texquality`) ✅ closed 2026-07-24.** Spec/plan `2026-07-21-enhanced-texture-quality` (+ warm-perf `2026-07-22`). Stack: pattern-gated dedither → Super-xBR 4× (MIT) → sprites/weapon/HUD 4× + Sharpen 0.5 → texel-AA → multi-scale normals + POM; parallel warm + session store + SHA-256 disk pack. **Interactive visual gate: SUCCESS** (graphics improved vs Scale2x reject). Suites: EditMode **601/601**, PlayMode **144/144**; Windows build ~128 MB; E1M1 cold disk warm ~2.8–3.7 s / compute ~14 s / level transitions 0 compute. Limits: menus/intermission native; Freedoom dedither ≈ no-op; POM solid opaque only; neural upscale deferred. Notes `Logs/enhanced-texture-quality-baseline-notes.md`.

**Sprite EdgeMix 8× ✅ closed 2026-08-02, ❌ removed entirely 2026-08-08** (from `main`; plan/spec `2026-07-31-inventory-edge-mix`). Deterministic 8× with 2+2-pixel premultiplied-alpha boundary mix (`AAAAAA|MMMM|BBBBBB`) for Enhanced pickups, enemies (incl. Spectre), and first-person weapons/muzzle flash; Classic stays native; projectiles/effects keep Super-xBR 4×; world/HUD/menus unchanged. Interactive verdict: SUCCESS — edges preserved vs Super-xBR.

**EdgeMix stage 2: contrast gate ✅ closed 2026-08-03** (same plan/spec, «Этап 2»). The unconditional mix band blurred intentional high-contrast detail (medkit cross, armor gem). `Scale8XContrastGated`: mix weight ramps by weighted RGB distance (0.30/0.59/0.11) — full mix ≤16, hard nearest ≥64; alpha silhouette always mixes (accepted band preserved); corners take the weakest pair weight. Gate 0 PNG sweep → user picked aggressive 16→64; pipeline **v4** (cache invalidated). Also cherry-picked `bb7075e` from `esrgan` (texel-aware sprite sampling `DOOM_SPRITE_TEXEL_AA`, `AlphaToMask`, snapped UI placement, Enhanced-boot race fix in `MapLoader`) — visually neutral verdict, kept for non-visual wins. Standalone visual gate: SUCCESS. Suites: EditMode **616/616**, PlayMode **149/149**; Windows build ~122 MB. Warm-perf baseline v4 recorded 2026-08-03: parity with pre-v4 (E1M1 first switch ~9.5 s cold≈warm, repeat ~8–21 ms; E1M7 ~9.9 s / ~9 ms; sprites 213 MB; managed ~1.1 GB; no mitigation), disk pack ~719 MB. Preview tooling: `Tools > Doom > Dump EdgeMix Gate Preview`; interactive-verdict protocol: before each user visual check, send было/стало PNG panels (medkit/armor/monster/shotgun set).

**Enhanced Pickup Pixel Extrusion ❌ REJECT 2026-08-05** (branch `pixel-extrusion`; spec/plan `2026-08-05-inventory-pixel-extrusion`). Uniform texel-column extrusion failed interactive Gate 1 for non-box pickups; experiment stopped — no Stage 2+ runtime routing. Enhanced pickups stay flat EdgeMix 8× billboards.

**Enhanced Parametric Pickups ❌ REJECT 2026-08-06** (branch `parametric-pickups`; spec/plan `2026-08-05-inventory-parametric-pickups`). Stage 0 toolchain ✅; Stage 1 MEDIA0 brief approved and candidates/drawing iterated, but visual gate not reached — user stopped: per-item parametric loop too slow and not approaching needed result. No Stage 2+, no runtime mesh routing. Enhanced pickups stay flat EdgeMix 8× billboards.

**TRELLIS.2 Enhanced 3D rollout ✅ LIVE TEST SUCCESS 2026-08-07** (status `docs/superpowers/plans/2026-08-06-trellis2-pickups.md`). Enhanced now routes textured TRELLIS.2 meshes for `MEDIA0`, voxel-styled `BON1A0`, all six weapon pickups (`SHOTA0`/`MGUNA0`/`LAUNA0`/`PLASA0`/`CSAWA0`/`BFUGA0`), ammo (`CLIPA0`/`SBOXA0`/`AMMOA0`/`CELLA0`/`CELPA0`/`ROCKA0`/`SHELA0`), `COLUA0` floor lamp, `BAR1A0` exploding barrel, `TRE2A0` large tree (54), and `SMITA0` leafy tree (47); Classic keeps native billboards and gameplay/collision/save identity stay on the original thing roots. Pickup/lamp/barrel interactive editor/standalone gates passed («результат отличный»): Resources-loaded `Doom/ExperimentalPickupUnlit` at exposure 1.0, medikit cross pulse, health-bonus HDR emission + bounded green light, lamp's existing sticky light, and barrel fallback to the BEXP billboard death sequence. Tree runtime wiring + WAD palette albedo correct + SectorFog on unlit models: interactive standalone gate SUCCESS 2026-08-07. **Ammo `AMMOA0`/`CELLA0`/`CELPA0`/`ROCKA0`/`SHELA0` interactive standalone SUCCESS 2026-08-09** (3D mesh + 2D redraw). Automation/build baseline before trees: **617 EditMode + 155 PlayMode**, Windows standalone SUCCESS. Raw models remain ~94–99k triangles each, so production decimation/performance acceptance is still open; `ARM1A0` is the next accepted pickup candidate but is not yet routed in runtime. Generation archaeology: MEDIA native SUCCESS, EdgeMix conditioning REJECT, STIMA REJECT 3/3, BON round-shape gate REJECT 2/2 but live stylized result accepted.

**TRELLIS.2 monster conditioning 2026-08-07.** Native/front/45° source sets prepared for `POSSA1`, `SPOSA1`, `TROOA1`, `SARGA1` (demon+spectre) and `BOSSA1`; no monster GLB/runtime gate yet. Tree finding (for archaeology): a nominal 45° redraw still reconstructs flat unless the image contains explicit radial roots, circumference-distributed branches, foreshortening and front/rear overlap. Workflow is persisted in `.cursor/rules/trellis2-shape-hints.mdc`.

**AI-derived assets doctrine ✅ adopted 2026-08-07** (spec `2026-08-07-enhanced-ai-derived-assets-design.md`, brainstorm outcome). WAD stays the source of identity and runtime truth (no unpacking); Enhanced is a derived layer of AI assets keyed to WAD lumps. Pipeline: native sprite → conditioning redraw (currently GPT-5.6 SOL; shape-hint v2 with explicit 3D topology when TRELLIS reconstructs flat) → TRELLIS.2 → interactive gate. Central gate criterion is **authenticity balance**: improvement that fits the pixel-art world (over-detailed assets read as foreign — the "Heroes 3 3D-remake" failure mode; under-detailed ones disappoint); mitigations: Classic untouched + hot-switch, per-asset allowlist reversibility, stylization tools (voxel look, WAD-palette albedo remap, texture-size caps). Asset lifecycle Draft → Accepted → Finalized: decimation (~95k → 3–10k tris) is a **finalization wave** over stabilized Accepted assets, not a per-iteration step. ~~EdgeMix 8× is a transitional fallback~~ — superseded: EdgeMix removed entirely 2026-08-08 (see below), the Enhanced fallback for lumps without AI assets is now the **native sprite**. Monsters are a separate experimental track (need rigging/animation, strictest authenticity gate); AI music is a future track under the same doctrine.

**EdgeMix 8× removed entirely ❌ 2026-08-08** (branch `3d-toggle`, user decision). Reason: EdgeMix blurred the picture and made honest visual judgement of Enhanced 2D impossible («ошибки восприятия»); with display-grade redraws coming quickly, the transitional fallback was cut wholesale instead of per-lump. Enhanced fallback for lumps without AI assets is now the **native sprite (Point)**: cascade mesh→redraw→native in both 3D On/Off; projectiles/effects keep Super-xBR 4×; Classic untouched. Deleted: `EdgeMixUpscaler` (+contrast gate), `EnhancedJobKind` 4–6 (+codec cases), `WorldTextureVariant` 3–5, `ObjectPresentation.EdgeMixBillboard`, `Tools > Doom > Dump EdgeMix Gate Preview`; Gate-1 preview menu now native|redraw. Pipeline **v6** — stale v5 packs (~719 MB) are auto-dropped by `CleanupStalePacks`. Suites: EditMode **614/614**, PlayMode **160/160** (15 EdgeMix tests deleted, 8× expectations rewritten to native).

**Enhanced lamp flicker ✅ SUCCESS 2026-08-12** (branch `3d-toggle`; spec/plan `2026-08-11-enhanced-tlite-per-bulb-flicker`). Enhanced-only per-fixture shader flicker of light surfaces (ceiling flats `TLITE*`/`FLAT2`/`CEIL3_4`/Freedoom `AQF` panels, wall textures `AQLITE*`/`LITE*`) via `_LampFlickerParams` MPB + UV-cell hash + luma gate; sector light / thinkers stay WAD-static, Classic untouched. Two launch bugs fixed 2026-08-12 (`6a49f99`): (1) packed params passed via `SetColor` were sRGB→linear-crushed in the Linear project — grid collapsed to one shared phase, amp 0.30→0.07 (flicker invisible); **packed shader data must be `Vector` + `SetVector`** (see memory `packed-shader-params-setvector`); found via GPU pixel-diff probe (`Camera.Render` + PNG diff), CPU-side MPB asserts can't catch it. (2) Freedoom `CEIL3_4` is a lamp panel (vanilla Doom: plain ceiling) — added to the allowlist. Interactive gate SUCCESS on slot-0 E1M5 standalone (round TLITE bulbs + square CEIL3_4 panels, independent phases).

**Doomify 3D ✅ closed 2026-08-13** (branch `3d-toggle`; spec/plan `2026-08-12-doomify3d`). Pipeline pivot: TRELLIS conditioning answers only for **geometry** (silhouette fidelity is the sole hard rule); doom style is enforced by a unified deterministic post-process `Tools/doomify3d.py` — weld voxel micro-cracks (`merge_close_vertices`; half the verts were crack dupes, else decimation tears slits open), pymeshlab quadric-with-texture decimation ~96k → 40k tris with the UV atlas preserved (boundary lock OFF or it stalls at ~50k), albedo 256px quantized to the native sprite lump palette, import side Point + uncompressed (bilinear+DXT ate the texel look). Emission masks stay valid (same UV space). Accepted preset **A (40k/256/native)** via ARM1A0 in-game gate; green armor 2018 routes the **ARM1B0** mesh (user preference; gem mask derived from red-dominant albedo texels; discrete blink glow is albedo-tinted, not white). All 24 meshes done: EditMode **624/624**, PlayMode **163/163**, Windows build **183 MB** (−69 MB vs raw meshes). Interactive verdict SUCCESS («объекты стали однороднее... единый стиль»). Tooling gotchas recorded in the plan: pymeshlab save clobbers same-name textures (decimate before writing albedo); ARM1A0/B0 gate found 10–13k tris tears thin shells (pauldrons) — 20k+ needed. Follow-up next stage: regenerate BAR1A0 (baked pixel art mushed by 256px cap), COLUA0 (lamp top), BON1A0 (low-quality source). This closes the raw-mesh decimation debt from the TRELLIS.2 rollout.

**Enhanced 3D monster pilot (POSS) ✅ closed 2026-08-14** (branch `3d-toggle`; spec/plan `2026-08-13-enhanced-3d-monster`). First fully functional stop-motion 3D monster: zombieman walks/attacks/pains as 7 doomified TRELLIS.2 meshes (frames A–G, one per live animation frame, swapped on the 35 Hz brain tick, Voxel-Doom style); death/gibs/corpse revert to the native billboard forever (barrel BEXP pattern); mesh yaw replaces the 8 sprite rotations. Runtime: `ExperimentalMonsterModel` (all-or-nothing frame coverage, per-frame patch-height normalization, pose interp, Enhanced±3D cascade, hot-switch) + seams in `MonsterController`/`ThingSpawner`; Classic untouched. Pipeline findings: (1) the 2026-08-10 «mid-style» shape-hint refresh made monsters unreadable for TRELLIS — restored the original volumetric v2 renders from `d908fcd` (doctrine: conditioning serves geometry only, style comes from Doomify); (2) frame recipe: per-frame GPT redraw (identity ref = A's v2 render + native frame sprite for pose) → TRELLIS seed 872506309 → Doomify preset A with `--palette-lump POSSA1` (ONE shared palette for all frames or they flicker); (3) TRELLIS still drifts helmet/collar/texture tone between generations — torso shimmer fixed by `Tools/tone_match_albedo.py` (masked mean/std transfer from frame A, shirt-hue band only; global transfer rejected — browned the green pants); (4) PowerShell `&` does not wait for GUI Unity — `Start-Process -Wait` only. New tools: `dump_trellis_native_inputs.py`, `tone_match_albedo.py`; doomify3d gained `--src`/`--palette-lump`. Suites: EditMode **624/624**, PlayMode **167/167** (4 new monster tests); Windows build 188 MB. Interactive verdict: «переливание ушло, выглядит хорошо». **SPOS ✅ added 2026-08-14** on the same recipe (7 meshes, targeted PlayMode 4/4, build Success, interactive «выглядит хорошо»): user picked frame **G** (dark armor + blue chest insets, closest to the v2 hint) as the color anchor — `tone_match_albedo.py` gained multi-region masks (`--mask armor,pants`) and G's armor/pants statistics were transferred onto A–F. Known SPOS limits (re-roll candidates): blue chest insets live only on G, frame F carries a gray baked muzzle flash. **SPOS color saga resolved 2026-08-14 («теперь выглядит хорошо»): statistical tone-matching can NEVER fix stop-motion flicker (TRELLIS bakes highlights onto different body parts per frame — histograms converge, placement wanders); accepted fix is `Tools/project_hint_texture.py` — the TRELLIS texture bake is discarded entirely and every frame mesh is painted by front-projecting its own v2 hint render (one consistent set), with row-by-row silhouette alignment, projection at native 1024 atlas res (at 256 texels are contended by 40k tris), 2px rim erosion, below-waist skin suppression + luminance cap, and strict-blue visor clusters feeding per-frame `_EmissionMask` steady glow in `ExperimentalMonsterModel`. Future monsters: project hints directly, skip statistics.** **SARG ✅ added 2026-08-14** (8 meshes A–H — melee spans E–F–G, pain H; targeted PlayMode 4/4, build Success, interactive «выглядит хорошо»): the spectre (58) intentionally keeps its MF_SHADOW billboard; recipe upgrade — the tone anchor is now the **native WAD sprite**, not frame A (mesh albedos were 3.5× flatter, std 14 vs 52; masked mean/std transfer restores the deep-red edge shading over the pink body; new `flesh` mask with hue wrap 330–25°). SARG re-roll candidates: F≈G near-identical (attack reads two-phase), E darkest. **TROO ✅ added 2026-08-14** (8 meshes A–H, first monster fully on hint projection; targeted PlayMode 4/4, build Success, «теперь выглядит хорошо»): projection gained `--side-accent-fix` (frontal-only gold via face normals + signed-volume orientation check), `--head-from-bake` (crest smear read as two heads while walking), raw-bake eye boost (eyes died in quantization — the imp was twice reported «walking backwards» though yaw was correct; all monsters share offset 0), `--lower-skin-fix` now opt-in. **BOSS ✅ added 2026-08-15, monster track CLOSED**: baron 8 meshes A–H via hint projection (`--head-from-bake` skull), steel armor + green muscle consistent first-pass; targeted PlayMode 4/4; closure suites EditMode **624/624** + PlayMode **167/167**, Windows build **250 MB**. **All five unique E1 monster presentations now route stop-motion 3D** (spectre intentionally billboard). Track debts: 3D death frames, re-rolls (SARG F≈G, SPOS blue insets), per-monster mesh memory (5 × 7–8 × 40k tris), XDEATH.

**Enhanced 3D monster death frames ✅ closed 2026-08-19** (branch `3d-toggle`; accepted by the user — the baron on the panel, the other four in game). All five E1 monsters now run stop-motion 3D from the stand to the body on the floor; only XDEATH gibs and the Spectre (intentionally MF_SHADOW) stay on the sprite. Closure: full suites EditMode **630/630** + PlayMode **175/175**, Windows build Success **316 MB**, 66 frame meshes / 2.64M tris / 31 MB runtime textures, death frames lazy. Carried debts: geometry re-roll of the live SARG F (F≈G, the attack reads two-phase), BOSS N0 (a mound where the sprite spreads), SPOS blue chest insets living only on frame G, and 3D XDEATH gibs. Spec/plan `2026-08-15-enhanced-3d-monster-death`. **SPOS death chain ✅ SHIPPED AND ACCEPTED** in standalone («сержант падает сейчас хорошо»): H0–K0 fall plus the L0 corpse heap route stop-motion meshes, so the sergeant is 3D from stand to body on the floor; only xdeath gibs hand back to the sprite. Runtime: the frame table declares each monster's whole death chain and `TryAttach` covers it as far as the meshes on disk reach (contiguous prefix, `CoveredDeathFramesForTest`), so frames can be added one at a time without the finished ones regressing; death meshes instantiate lazily; gibs always revert. New tools: `lay_down_mesh.py` (TRELLIS reads conditioning as the front view of an UPRIGHT subject, so any lying pose comes back standing — SPOSK0 measured Y/X 1.02; the pitch that matches the sprite's own aspect is the right one, −75° for K0, −90° for the L0 heap), `validate_shape_hint.py`, `global_tone_match.py`, `head_from_old_texture.py`. Suites: EditMode `MonsterModelFrameTableTests` 4/4 (table pinned to MonsterTable/ThingTable + WAD patch headers for all 28 frames), PlayMode `ExperimentalMonsterModelPlayTests` 6/6. Windows build Success ~258 MB.

**Enhanced 3D светильники (факелы) ✅ закрыто 2026-08-19** («принимается, выглядит хорошо») (branch `3d-toggle`; spec/plan `2026-08-19-enhanced-3d-torches`). Точка входа — сохранение игрока в слоте 1: E1M5, DOOM (1326, 2060), взгляд ~165°, то есть прямо на высокий синий факел `TBLU` в 144 юнитах; E1M5 — единственная карта E1 с полным набором светильников (по одному факелу каждого цвета плюс канделябр, в 350–550 юнитах вокруг). **Обе половины факела — тела вращения, поэтому вся волна прошла без Space; позже стойки всё же подняли до TRELLIS ради качества (см. следующий абзац), а токарная версия осталась фолбэком**: и пламя, и стойка — тела вращения, поэтому весь объект считается из спрайта арифметически, как файербол. Стойка — **токарное тело** (нога, кольца, чаша, основание), её силуэт, провёрнутый вокруг оси вещи, и есть её форма, а затенение радиальное по той же причине, что у цилиндра; пламя мешем-с-бейком быть не может (горячее ядро внутри объёма — вывод волны файербола), поэтому цвет пересчитывает шейдер `Doom/ExperimentalTorch`: `t` — проекционное расстояние от оси части, `v` — высота, таблица набита собственными текселями лампа. `Tools/split_torch_sprite.py` режет спрайт по измеренной строке верха головы (38/44/38/38/44/38); межкадровый diff для разделения НЕ годится — пламя перекрашивает латунь каждый тик (`TBLU`: 535 изменяющихся текселей из 873). `Tools/make_torch_model.py` строит токарную стойку и **свой плюм на каждый кадр A–D** — две более простые формы отвергнуты на превью (вращение вокруг оси вещи даёт симметричную «вазу» без сужения; огибающая четырёх кадров такая же толстая), а меш здесь вычисляется, а не генерируется, поэтому stop-motion бесплатен и силуэт огня анимируется. **Баг волны, стоивший целой панели гейта:** таблица `_spine` (смещение оси по высоте) импортировалась как sRGB, и в Linear-проекте нейтральное 128 читалось как 0.216 — шейдер считал ось сдвинутой на 0.28 высоты, факел рисовался верно ровно с того ракурса, где сдвиг смотрит в камеру, и разваливался со всех прочих; данные-таблицы импортировать `sRGBTexture: 0` (тот же урок, что `SetColor` в памяти `packed-shader-params-setvector`). Второй дефект той же панели: меш доходит до края вокселя, а тексели биннились по центрам, поэтому внешнее полтекселя было прозрачным и вся наружная оболочка отсекалась — крайний бакет теперь тянется до 15. Побочно починена **бит-верность Classic**: `PickupAnimationTable` не покрывала декорации, и порт держал все шесть факелов на кадре A — новая `DecorationAnimationTable` крутит A–D по 4 тика (info.c `S_TBLUETORCH…`). Рантайм `ExperimentalTorchModel` + шов в `ThingSpawner`; высоты частей берутся из WAD-патча и высот таблиц, схема сейва не менялась. Панель: `Tools > Doom > Dump Torch Gate Preview` → `Logs/torch-gate/` (нужен графический девайс). Suites: EditMode **644/644** (`TorchAssetTests` ×8), PlayMode **185/185** (`ExperimentalTorchModelPlayTests` ×5 на E1M5 через реальный спавнер). Ловушка тестового харнесса: `GameFlowController.ResetForTests()` идёт через `GameSessionHost`, который **обнуляет `MapLoader.MapNameOverride`** — карту задавать ПОСЛЕ сброса, иначе сцена молча строит E1M1.

**Стойки факелов через TRELLIS.2 ✅ приняты 2026-08-19** (та же волна). Пламя гейт прошло сразу, а стойки по решению пользователя подняли до генерации ради качества: токарное тело держит силуэт точно, но фронтальное украшение головы у него становится кольцом. Шесть GLB (`Textures/Trellis2/GLB/<ЛАМП>_*.glb`) → doomify preset A (92–99k → 40k tris, 256 px) → `ExperimentalTorches/<BASE>/<BASE>_stand_mesh`; `ExperimentalTorchModel` предпочитает его токарному сам, а PlayMode-тест пиннит ПРАВИЛО (`UsesGeneratedStandForTest == HasGeneratedStand(sprite)`), а не сегодняшнее состояние ассетов. **Находка: бейк TRELLIS всегда площе пиксель-арта** — альбедо пришли со std 16–27 против нативных 40–55, высокие ещё и на ~30 светлее оригинала, короткие на 10–20 темнее, у `SMGT` пропала латунная теплота (warm 27 против 56); рядом с соседними пикселями это читается как выбеленный гипс. Лечится новым ключом `doomify3d --tone-image` — перенос mean/std по каналам с НАТИВНОГО кропа самого ассета до квантования (инструмент монстров, наведённый на спрайт вместо соседнего кадра); после него все шесть в пределах пары единиц от спрайта и по яркости, и по теплоте. Второй ключ волны, `--palette-image`: стойка делит лампу с пламенем, и квантование по `TBLUA0` предложило бы латуни все синие и белые цвета огня. Ловушка установки: doomify пишет `<lump>.obj.mtl`, а OBJ ссылается на `<lump>.mtl` — при копировании в Resources переименовывать, иначе Unity подставит дефолтный материал и объект будет белым (та же беда, что с трупами POSS 2026-08-16); теперь это пиннит EditMode-тест. Итог: EditMode **645/645**, PlayMode **185/186** (падение — чужой плавающий `HudPlayTests`, в изоляции 4/4), Windows build Success **321 МБ**.

**Enhanced 3D зелёные колонны ✅ закрыто 2026-08-20 («выглядит хорошо»)** (branch `3d-toggle`; spec/plan `2026-08-20-enhanced-3d-pillars`). Точка входа — слот 1: E1M5, DOOM (1276, 2155), взгляд ~264° прямо на колонны первого зала; E1M5 — единственная карта E1 с колоннами (одна `COL1` 30, две `COL2` 31; красная `COL3` 32 — вне волны по решению пользователя, как и анимируемая `COL5` 36 с E1M7). **Правило проекта, зафиксированное этой волной: все 3D-объекты делаются через TRELLIS.2; вычисленная геометрия (токарка/воксели) — редкое исключение, которое выбирает ПОЛЬЗОВАТЕЛЬ, а не ассистент.** Первая сборка волны была сделана токаркой без согласования («колонна же тело вращения») и откатана целиком — та же ошибка, что с уже не одобренными токарными стойками факелов; вычисляемым остаётся только огонь (ядро градиента внутри объёма не запекается). Пит стандартный, как у лампы/деревьев: нативные канвы + кропы → эскизы v2 одним набором с привязкой «файл → результат» (черепа и каннелюры по всей окружности, эллипс верха) → Space (обе GLB с первого прогона) → `doomify3d` preset A с `--tone-image` по нативному кропу (96–97k → 40k tris, 256 px/18 цветов; черепа переживают децимацию) → `Assets/Resources/ExperimentalPickups/COL1A0|COL2A0/` (meta клоном с `COLUA0`). Рантайм — ни одного нового компонента: два кейса в `ExperimentalPickupModel` + визуальные высоты пином к патчу (56/41 px против коллизии 48/36); `ThingSpawner` не менялся; коллизия и схема сейва не тронуты. Suites: EditMode **651/651** (`PillarAssetTests` ×3: OBJ→MTL→albedo, Resources, высоты против WAD), PlayMode **187/187** (колонны в табличном тесте декораций `PickupPlayTests`), Windows build Success **326 MB**. Панель было/стало `Logs/pillar-gate/pillar-bylo-stalo.png`; интерактивный гейт в standalone по слоту 1 — SUCCESS. Красная `COL3` добрана 2026-08-20 тем же питом (кейс 32, высота 55 px, гейт «выглядит хорошо»); урок: unlit-панель doomify показывает только альбедо-бейк — шум тёмного мрамора был принят за комковатую геометрию, меш был хорошим; геометрию судить по ламберт-рендеру без текстуры. **`COL5` «сердце» добрана 2026-08-21 (гейт «выглядит хорошо») — все колонны эпизода теперь 3D.** Freedoom-версия — кровавый алтарь (пьедестал с черепами, идол, струя крови); меш ОДИН, кадра A: кадры A/B различаются двумя текселями глаз, второй прогон TRELLIS дал бы дрожание геометрии (урок монстров). Кейс 36, высота 49 px; попутно закрыт пробел бит-верности — `DecorationAnimationTable` получила `S_HEARTCOL` (A/B по 14 тиков), раньше порт держал COL5 замороженной на кадре A (та же беда, что у факелов). Suites: EditMode **652/652**, PlayMode **186/187** (падение — плавающий `GraphicsResourceLifetimePlayTests.Hot_switch…` на E1M1, +1 Doom/-материал — тот самый «+1 known Stage-8 flake», который E1M7-двойник теста допускает явно; в изоляции 1/1); Windows build Success **329.8 MB**.

**Перегенерация бочки BAR1 ✅ закрыта 2026-08-21 («выглядит хорошо»)** (branch `3d-toggle`; первый закрытый долг Doomify-волны — остались COLUA0/BON1A0). Старые эскизы A/B расходились габаритами и несли выдуманные эмблемы «!»/«?» (нативы идентичны 23×32 — между кадрами меняются только огни верхнего кольца), поэтому пары 2D и 3D перерисованы заново одним сетом; в рантайм пошёл **BAR1B0**-меш (выбор пользователя — лампы и полоса читаются лучше), doomify **texcap 512** (смаз запечённого пиксель-арта был от капа 256; дешёвый путь из плана вместо полной регенерации подтвердился), старый BAR1A0-меш из Resources удалён. **Шейдерное мерцание:** ARM1-механизм discrete blink обобщён (`blinkAnimation`+`brightFrame`) — лампы и зелёная полоса вспыхивают на кадре B в родном ритме S_BAR1 (6+6 тиков), в тёмной фазе гаснут; маска эмиссии зонируется по **высоте меша**, т.к. UV-атлас не пространственный (красный знак эмблемы делит оттенок с лампами, но живёт в средней зоне и не светится) — `Tools/make_bar1_emission.py`. Пойманный баг: каденция бочки живёт в `BarrelRules`, НЕ в `PickupAnimationTable` — табличный поиск молча замораживал вспышку в яркой фазе (пин в `BarrelAssetTests` со сторожем от второго источника). **Две находки тона:** (1) `tone_match` считал статистику по всему атласу — тёмная набивка между UV-островами утягивала среднее и подгонка ВЫСВЕТЛЯЛА видимые тексели; теперь статистика только по UV-покрытым (`uv_coverage_mask` в doomify3d). (2) **Тон судить в экранном пространстве:** при уже сошедшихся текстельных статистиках рендер был на 44% ярче натива (88 vs 61) — яркие UV-острова кромок занимают на модели больше видимой площади, чем их тонкие блики в спрайте; `Tools/calibrate_screen_tone.py` итерирует глобальный гейн через unlit-рендер до нативной экранной яркости (тут 0.676× → 61.2 = натив). 2D-редравы: новая согласованная пара — «дыхание» размером при мигании ушло. Suites: полные EditMode **655/655** + PlayMode **187/187** (без флейков; `BarrelAssetTests` ×3 новых), импорт 27/27, Windows build **330.9 MB**.

**Перегенерация лампы COLUA0 ✅ закрыта 2026-08-21 («выглядит хорошо»)** (branch `3d-toggle`; второй долг Doomify-волны — остался BON1A0). Старая голова меша рассыпалась «в набор пикселей» (чанки-эскиз 2026-08-10 нёс слишком мелкую деталировку для TRELLIS); новый эскиз — по бочковому рецепту: чистый объёмный рендер без пикселизации с наказом «голова крупными раздельными формами» (купол, иллюминатор в латунном кольце с глубиной, порты-огоньки) — и реконструкция удалась с первого прогона. Пит бочки целиком: texcap 512 + `tone_match` по UV-покрытию + `calibrate_screen_tone.py` (инструмент научился сохранять ЛУЧШУЮ итерацию — на 41-цветной палитре отклик гейна немонотонный, осцилляция 85→77→87 вокруг цели 82). **Постоянная янтарная подсветка шейдером** (запрос пользователя): маска эмиссии по высоте меша — колба + порты купола, латунь/линза/медальон тёмные (`Tools/make_colu_emission.py`, маджента-проба); свечение едет по discrete-blink-пути БЕЗ анимации — blink навсегда в яркой фазе, ноль новых шейдеров; родной sticky point light не менялся. Тесты: `LampAssetTests` ×3 (цепочка+маска, Resources, сторож «2028 не появится в PickupAnimationTable — иначе постоянное свечение замигает»). Suites: полные EditMode **658/658** + PlayMode **186/187** (падение — `FloorAnchorPlayTests.Pickup_follows_a_lowering_floor`, тайминговый флейк полного прогона: в изоляции 2/2, волна якорей не касалась); Windows build **332.1 MB**.

**3D XDEATH-трупы ✅ закрыто 2026-08-22 («выглядит хорошо») — долг гибсов закрыт** (branch `3d-toggle`). Решение по паттерну файербола/бочки: **анимация разрыва остаётся спрайтовой** (спрей рассыпанных пикселей телом не смоделировать), а финальная лужа останков — кадр **U0** — получает меш. Рантайм — «гибс-интерлюдия» в `ExperimentalMonsterModel`: `NotifyDeathStarted(extreme)` при наличии U0-меша прячет меш и отдаёт брызги билборду БЕЗ постоянного revert, `NotifyFrame(20)` (индекс = `XDeathCorpseFrame` таблицы = 'U'−'A') лениво инстанцирует лужу с flat-нормализацией по ширине патча (POSS/SPOS 67, TROO 66); у SARG/BOSS XDEATH нет — жёсткий revert как раньше (сторож в тестах). Заодно **вещи 10/12** («кровавое месиво» карт — рисуют разорванного игрока) поехали на новый **PLAYW0**-меш (ширина 53) через трупный шов; папка `ExperimentalMonsters/PLAY/` заведена. Пит: один промпт на 4 лужи с явной привязкой файлов, все GLB вернулись лежащими (толщина 0.08–0.13); **известный дефект** — POSSU0/SPOSU0 экструдированы прямоугольными плитами с тёмными бортами (ре-ролл кандидаты с наказом «рваный контур»), в игре гейт прошли. Suites: полные EditMode **662/662** + PlayMode **188/188** (без флейков; пины xdeath к WAD + 10/12 в тесте декораций; PlayMode: интерлюдия→меш на POSS, revert без меша на SARG), Windows build **342.2 MB** (+8 MB за четыре лужи). Оба открытых хвоста закрыты 2026-08-23 (см. ниже): перерисовка TROOM0 и PLAYN0.

**Перегенерация TROOM0 ✅ закрыта 2026-08-23 («выглядит хорошо»)** (branch `3d-toggle`, `4066f57`). Труп импа перерисован по промпту «лежать ровно» (вид сверху, явная плоскостность + рваный контур) — GLB вернулся уже лежащим (Y 0.37 против кургана 0.72), днище чистое (старый «парящий» отросток не повторился). Пит: doomify preset A c `--palette-lump TROOA1`, затем `lay_down_mesh.py --flatten-to-aspect 2.1923` — растяжка Y ×1.245 до нативного аспекта 57×26, так что height-правило рантайма ложится на размер спрайта БЕЗ правок кода/таблицы. Тон: `--tone-image` по нативному кропу дал +37% экранной яркости — `calibrate_screen_tone.py` (бочковый урок, впервые на трупе) свёл 55.3 → 40.7 при цели 40.3 (gain 0.729). Целевые сьюты EditMode 8/8 + PlayMode 13/13, Windows build Success **342.3 MB**.

**Мёртвый игрок PLAYN0 ✅ закрыт 2026-08-23 — трупная волна без долгов** (branch `3d-toggle`, `61f040d`, интерактивный гейт пройден). Вещь 15 (мёртвый морпех карт) — последняя трупная декорация на спрайте — получила меш: GLB вернулся лежащим (Y 0.30 при длине 1.0, тело вдоль X, доворот не нужен), doomify preset A на собственной 40-цветной палитре PLAYN0, тон по нативному кропу + экранная калибровка (92.3 → 87.8, err 0.0). Рантайм — прямой кейс в `TryDescribeCorpse` рядом с лужами 10/12 (width-правило, патч 37 px); пин декораций в `MonsterModelFrameTableTests` расширен на 15, негативный сторож «у 15 нет меша» удалён. Целевые сьюты EditMode 8/8 + PlayMode 13/13, Windows build Success **344.1 MB**. Оставшиеся ре-ролл кандидаты трека: POSSU0/SPOSU0 (плиты с тёмными бортами), SARG F≈G, SPOS синие вставки только на G, BOSS N0.

**POSS: смертная цепочка перерисована единым сетом + 3D трупы-декорации ✅ закрыто 2026-08-22 («оставляем так»)** (branch `3d-toggle`). Повод — три находки пользователя в игре: трупы парили и были сплющены (L0), заранее мёртвые солдаты карт оставались спрайтами, а у старого сета H0–L0 **дрейфовала личность** (полевой китель с карманами вместо живой рваной куртки-«второй кожи», разрыв позы K0-на-спине → L0-на-боку, выдуманный взрыв головы — в нативах ранение в ГРУДЬ, шлем цел). Новый сет H0–L0 нарисован ОДНИМ промптом с эталоном одежды из POSSA1 и связной инерцией падения (грудная рана на всех кадрах, K0 — перекат на тот же бок, где лежит L0; правило «две ноги, два сапога» в промпте явно — первая генерация L0 дала три ноги). Пит: doomify c `--palette-lump POSSA1`, ориентация каждого кадра развёрсткой pitch×yaw (fall-правило по нативному аспекту: I0 pitch 90 — натив 38×42 почти вертикален, J0 pitch 40; H0 пришёл стоя), **проекция хинта пропущена осознанно** — видимую сторону несёт думифицированный бейк, эскиз рисован с той стороны, что в игре скрыта. **Два рантайм-урока:** (1) лежачий кадр, нормализованный по ВЫСОТЕ, растягивается в гигантский блин (L0 по 19 px высоты) — K0/L0 переведены на flat-правило ширины (48/50 px по патчу); (2) flat-ширина меряется по оси X, а TRELLIS возвращает длину тела по Z — длину доворачивать на X, иначе за «ширину» берётся поперечина и труп раздувает в 3.6 м. **Трупы-декорации**: вещи 18/19/20/21 (E1M1 в одиночку размещает девять) маршрутизируются на трупные меши конца смертных цепочек через новый шов `ExperimentalMonsterModel.TryDescribeCorpse` + width-нормализация в `ExperimentalPickupModel`; мёртвый игрок (15) остаётся спрайтом до генерации PLAY-меша. Курган TROOM0 парил из-за отростка-мусора под днищем (0.2% вершин в нижних 2%) — клампован к линии днища. Suites: `MonsterModelFrameTableTests` 7/7 (новый пин трупов-декораций), PlayMode 19/19, Windows build **334.1 MB**. Открыто: новый труп игрока PLAYN0 — промпт выдан, эскиз уже сгенерирован (TROOM0 перерисован 2026-08-23, см. выше).

**Перегенерация бонуса BON1A0 ✅ закрыта 2026-08-22 («выглядит хорошо») — ВСЕ долги Doomify-волны закрыты** (branch `3d-toggle`). Слабый воксельный исходник заменён: эскиз по бочковому рецепту (объёмный рендер; крошечный 11×15 натив прочитан как круглая фляжка — шарообразное зелёное стекло, железные скобы-ленты по окружности, воротник, зелёная пробка; наказ «не запекать звёздный блик — это кадры B–D»), Space с первого прогона. Пит: texcap 512 + тон по покрытию + экранная калибровка (ошибка 0.4 lum). **Две правки поверх пита:** (1) TRELLIS вернул фляжку боком — стеклянное окно смотрело в yaw 90; OBJ довёрнут на 90° скриптом (правильный фронт ищется развёрткой по углам, не предполагается); (2) классовый тон — зелёные тексели подогнаны к статистике стекла натива, серые к металлу, затем реквантование (глобальная статистика уже сходилась, а скобы всё равно были светлее натива). **Свечение**: глобальный буст 0.65 старого воксель-меша убран — маска эмиссии только на стеклянное тело (ламповый steady-путь); тонкость кейса: 2014 ЕСТЬ в `PickupAnimationTable` (звёздный блик билборда), но мешу анимация мигания сознательно не отдана — `blinkAnimation` теперь выдаётся только 2018/2035 явно, блик остаётся 2D-делом; зелёный point light не менялся. Тесты: `BonusAssetTests` ×2; полные сьюты EditMode **660/660** + PlayMode **186/187** (падение — известный плавающий `HudPlayTests`, в изоляции зелёный); Windows build **334.3 MB**.

**Enhanced 3D файербол импа ✅ закрыт: интерактивный гейт SUCCESS 2026-08-21 («всё отлично»)** (branch `3d-toggle`; собран 2026-08-19, spec/plan `2026-08-19-enhanced-3d-fireball`). The imp's missile was the last moving object still flat in Enhanced+3D. **No TRELLIS here, and deliberately so**: `BAL1A0`/`BAL1B0` is a 15×15 ball with no internal structure — conditioning would return a lumpy potato for something on screen a fraction of a second — while a sphere's shape follows from the sprite arithmetically. `Tools/make_fireball_mesh.py` builds a voxel ball on the sprite's own pixel grid (15³, r 7.28 px, outer faces only, 2124 tris) plus a **radius × variant** 16×16 colour table baked from BAL1's own texels; no drawing, no Space run. **Wave finding: a fireball's gradient cannot be baked onto a solid ball's surface** — the white-hot core lives INSIDE the sphere, so any bake yields a flat orange marble and loses the sprite's defining feature. The split that works: the mesh carries only shape, and `Doom/ExperimentalFireball` re-derives the radial coordinate per fragment in OBJECT space (the ball is a unit sphere there, so no radius uniform and no scale correction), sampling `(t, uv.y)` — core reads white from every angle while the thing occludes, spins and takes SectorFog. Mesh UVs carry only the variant row (`u = 0.5`), assigned by a position hash so a spinning ball keeps the sprite's boiling instead of a smooth ramp; the profile PNG imports Point / no-mip / **Clamp** (it is a LUT: a mip would average the core away at range, Repeat would wrap the rim back into the core at the silhouette). Coverage is fly frames only — impact reverts to the billboard forever (barrel BEXP pattern), because the explosion `BAL1C0`–`E0` is a spray of loose pixels with no body to model. Runtime `ExperimentalProjectileModel` + seams in `Projectile`; size and centre come from the WAD patch (`Width`, `TopOffset − Height/2`), not from measuring the mesh; save schema untouched. Suites: EditMode **636/636** (`FireballAssetTests` ×6), PlayMode **179/180** (`ExperimentalProjectileModelPlayTests` ×5; the one failure is the pre-existing flaky `HudPlayTests` face-patch assert — green 4/4 in isolation), Windows build Success **302 MB**. Panel: `Tools > Doom > Dump Fireball Gate Preview` → `Logs/fireball-gate/` (needs a graphics device — headless without `-nographics`). Test-harness trap worth remembering: `SceneManager.LoadScene` applies at the end of the frame, so `FindFirstObjectByType<MapLoader>()` right after the call hands back the PREVIOUS loader and a `SpriteCache` whose WAD is about to close — wait for `GameFlowState.Playing`, not for a non-null cache.

**POSS re-roll ❌ REVERTED 2026-08-15, front/back問題 open.** Re-projecting POSS onto the accepted hint-projection recipe failed its in-game gate twice: first the colour pulsed through the walk cycle (the old POSS hint set spanned 3× in luminance — projection is only as consistent as the hint set behind it; `global_tone_match.py` closed 40% → 5% but could not touch the painted wear or the per-frame helmet), then, with a freshly redrawn consistent set and seven regenerated meshes, the zombie read as «передом и задом одновременно». Root cause found and measured: **a single frontal projection paints the back with the front** — 41–51% of visor triangles face backwards on POSS and SPOS alike, so chest wounds and a glowing visor reappear on the spine and skull. **Front/back ✅ SOLVED 2026-08-16, POSS live re-roll ACCEPTED in-game** («в игре сейчас всё выглядит нормально»): two measurable causes, no principled obstacle. (1) The axis was inverted — settle it by rendering the mesh with its own (geometry-correct) TRELLIS bake from both sides: `doomify3d.render` at yaw 0, which keeps the LARGER z, shows face/pistol/chest wounds, yaw 180 a clean back, so **front is +Z** and the hint projects from there; the parked flags assumed −Z, i.e. repainted the face with the back. Normals play no part. (2) Occlusion was binned in hint pixels, but the row-by-row alignment stretches front and back of a row differently, so 22% of rear texels sat alone in a cell with nothing to occlude them — binning moved to mesh space (`--back-grid`, 256 ≈ 16 texels/cell; 128 over-marks thin limbs, 512 re-orphans texels). Result: 0% orphans, 0% blue on the rear half, bake-sourced texels 43% → 52–63%. Leftover: TRELLIS wraps the visor onto the skull as a dark navy strap that palette quantization lifts to visor-blue — those texels are dimmed and re-snapped after quantization, and the emission mask is cut by the back side (glow belongs to the visor, not the skull). `--visor-front-only` deleted (subsumed); `--dump-back-mask` added — it writes the split as a texture, which is what exposed cause (2). POSS now ships the seven regenerated meshes on the consistent hint set plus its first `_emission` visor masks; targeted EditMode 4/4 + PlayMode 6/6, Windows build Successful ~259 MB. SPOS keeps its accepted textures (same two-faced defect, re-projection available but not run). Also recorded in `.cursor/rules/trellis2-shape-hints.mdc`: **never draw a shape aimed at the camera** (the POSS pistol drawn muzzle-on reconstructed as a blob every time), and conditioning sets must be drawn as one set. Remaining death frames to author: BOSS I0–O0.

**SARG death chain I0–N0 built 2026-08-17 (branch `3d-toggle`, awaiting the in-game gate).** Same pit (doomify with `--palette-lump SARGA1`, hint projection with `--back-from-bake --back-grid 256`, lay-down last); the demon has no blue, so its six empty `_emission` masks were deleted — the live frames carry none either. Projection pitches: I0/J0 0, K0 +90, L0/M0/N0 +30 (each pile is drawn from a low camera, and Space returned K0–N0 already laid down). **Pose rule for lying frames, caught in game (it hit the imp too, fixed the same way: TROO K0 −80° ×2.86, L0 −90° ×2.90): if a mesh is flat — has an axis it is 2–3× thinner along — the frame lies down along that axis and its proportions are fixed by scaling Y, never by tilting; tilt is only for volumetric meshes where the tilt IS the pose (fall frames).** The fall-frame rule — pick the pitch whose width at patch height matches the sprite — was applied to the piles too (K0 +90, L0 +60, M0 +45, N0 +30) and produced «кучи не лежат на полу»: a flat mass stood on edge reads as meat propped against a wall. **A pile must lie flat, and its proportions are fixed by scaling Y, not by tilting** — K0–N0 were rotated back to horizontal and stretched vertically with the same `--flatten-to-aspect` that squashed the imp's mound (here running the other way: ×3.18/×2.98/×2.79/×1.35). Widths now match the sprite by construction: 36/41/43/44/49/48 vs 38/40/43/43/48/49; and it reads closer to the original, since native K0/L0 are fountains of chunks rather than puddles. Note for BOSS: Space returned only I0/J0 standing and K0–N0 already flat (Y 0.22–0.44) — the closer a frame is to a pile, the more likely it comes back laid down, so the per-frame pitch sweep beside the hint is mandatory. The authenticity risk flagged before generation landed softly: I0/J0 read as a body (pink carcass with the top torn off), K0–N0 as volumetric meat piles; if the piles fail the gate, coverage trims to I0–J0 by deleting files, no code change. Suites: EditMode `MonsterModelFrameTableTests` 5/5, PlayMode `ExperimentalMonsterModelPlayTests` 9/9 (chain test now covers POSS/SPOS/TROO/SARG; the uncovered-tail case moved to BOSS, the last monster without death meshes); Windows build Successful ~288 MB.

**BOSS death chain I0–O0 built 2026-08-19 (branch `3d-toggle`, conditionally accepted on the panel — the baron lives on E1M8, so the real in-game verdict rides along with the Task 5 fight) — all five monsters now route 3D from stand to corpse.** Same pit (doomify preset A, hint projection with `--back-from-bake --back-grid 256`, 57–69% of texels from the bake, lay-down last). Space returned the poses nearly finished: I0 upright (y 1.003), J0 buckling (mesh x/y 1.14 vs the sprite's 78×69 = 1.13), K0 collapsed (y 0.679), and L0–O0 already flat (thickness 0.12–0.31 at width 1.0) — confirming the demon's note that the closer a frame is to a pile, the more likely Space lays it down itself. Projection pitches from the silhouette sweep: I0/J0/K0 0, L0 −15, M0 +15, N0 +30, O0 +15, each undone after projection. **Wave finding: the palette for death frames comes from a DEATH lump, not from frame A.** The first pass used `--palette-lump BOSSA1` like every earlier monster and the gore came out brown — the live baron's palette holds 6 red-ish colors against `BOSSK0`'s 36, while BOSSK0 covers the same steel and green (39 greys vs 38, 14 greens vs 13, 8 golds vs 3), i.e. strictly richer; re-projecting on BOSSK0 restored the red with one shared palette across the chain. Flat frames flagged L0–O0 (native widths 80/82/82/90); K0 stays on the height rule (0.68 thickness is still volume). The uncovered-death-tail test moved to a new `DeathCoverageCapForTest` seam — every routed monster now ships its whole chain, so no asset gap is left to exercise that path. Suites: EditMode `MonsterModelFrameTableTests` 6/6, PlayMode `ExperimentalMonsterModelPlayTests` 12/12 (chain test now covers all five monsters). **Task 5 automation closed 2026-08-19: full suites EditMode 630/630 + PlayMode 175/175, Windows build Success 316 MB** (death chains cost ~66 MB over the 250 MB live-frame baseline); 66 frame meshes / 2.64M tris / 31 MB of runtime textures across the roster, with death frames instantiated lazily so a live population still holds only its 7–8 live frames. The interactive Gate Final — a fight with all five monsters in Enhanced+3D On, carrying the baron's deferred verdict — is the one thing left.

**Кучи стояли столбом: нормализация по ширине ✅ 2026-08-19** (branch `3d-toggle`). The 2026-08-17 rule («a pile lies flat, proportions come from scaling Y») fixed the tilt but re-erected the piles by the other route: Y is a lying mass's THICKNESS, so ×2.8–3.2 turned each blin into a cube (`TROOK0` 0.83×0.99×1.00, `SARGK0` 1.00×1.00×1.00) and the gate came back «всё ещё не на земле». Root cause is the runtime, not the meshes: `NormalizeFrame` scaled EVERY frame so its bounds height equals the native patch height — true for a figure on its feet, false for a pile, whose patch height comes from DEPTH (DOOM draws the floor from a raised viewpoint). Fix: the frame table gained `flatFrames` (lump → native patch WIDTH); those frames scale by width and keep their own thickness, bottom still on the floor, XZ centred, and bounds are measured in the yaw pivot's space (`Renderer.bounds` is a world AABB and would ride the corpse's facing — death frames are built lazily, so a turned corpse is the normal case). Flagged: `SARG K0–N0` (43/43/48/49 px), `TROO K0–L0` (45/42); `TROO M0` stays on the height rule (its mound was pre-squashed to the sprite aspect and lands on the native 57×26); POSS/SPOS untouched (accepted in game). Meshes returned to shape with the exact inverse (`lay_down_mesh.py --scale-y`, new): TROO ×1/2.86, ×1/2.90; SARG ×1/3.18, ×1/2.98, ×1/2.79, ×1/1.35 → thickness 0.22–0.44 at width ~1.0. Known cost: a flat pile projects LOWER than the sprite draws it — the sprite's height is DOOM's fixed raised viewpoint, which a 3D pile at eye level cannot reproduce; if the gate calls it too flat, the safe knob is a thickness multiplier on flat frames. Suites: EditMode `MonsterModelFrameTableTests` 6/6, PlayMode `ExperimentalMonsterModelPlayTests` 11/11 (new: widths pinned to WAD patch headers + flat frames measured at yaw 0 and 90).

**TROO death chain I0–M0 built 2026-08-16 (branch `3d-toggle`, awaiting the in-game gate).** Same pit as POSS (doomify preset A with `--palette-lump TROOA1`, hint projection with `--back-from-bake --back-grid 256`, lay-down last); the imp has no emission, so its masks stay empty like the live frames. Projection and pose pitches coincided per frame: I0 0°, J0 0°, K0 +80°, L0 −90° (Space returned K0/L0 already flat). Widths at patch height 36/67/45/42 px vs native 49/54/45/42. **Wave finding: the silhouette metric that picked POSS' angles is useless on thin-limbed monsters** — every pitch scored 0.49–0.67 and the "best" angles were noise (85°, −125°, 150°), because the mesh mask is built from vertices and TROO's claws and tendrils carry almost no weight in it; the angle was found by rendering a pitch sweep beside the hint instead. Rule of thumb: metric for mass-shaped monsters, render sweep for thin-limbed ones. **The M0 corpse took a re-roll and produced a second, separate finding.** The first generation was unusable: TRELLIS read the wide (3.8-aspect) corpse drawing as a body extending in DEPTH — X 0.27, Y 0.21, Z 1.00, i.e. 124 px wide at the 26 px patch height against a 57 px sprite. Redrawn as a compact top-down mass (the accepted `SPOSL0`/`POSSL0` shape) it came back as a **rounded mound** — 0.97 × 0.72 × 0.98 — because a corpse is a body PRESSED to the floor and a top-down drawing carries no thickness. Rotation cannot fix a mound (no thin axis to turn toward the camera): the best silhouette over all pitches was 35 px against the sprite's 57, so the corpse would shrink on the very frame the sprite spreads widest (L0 42 → M0 35 instead of 42 → 57). Fixed with a new `lay_down_mesh.py --flatten-to-aspect`, which squashes Y until the silhouette matches the native patch aspect — applied AFTER projection, so UVs and textures stay valid, and normals are only rotated (scaling a normal is not the inverse-transpose it would need, and the shader is unlit). M0 flattened ×0.61 lands exactly on 57×26; the same tool is ready for `SARG N0` and `BOSS O0` if they come back as mounds. Re-roll candidates after the gate: J0 and K0 read as a tangle of limbs, because the imp's arms were drawn reaching toward the camera (the "never point a shape at the camera" rule applied to a whole pose). Suites: EditMode `MonsterModelFrameTableTests` 5/5, PlayMode `ExperimentalMonsterModelPlayTests` 8/8 (the chain test is now table-driven over POSS/SPOS/TROO — it derives the frame numbers from the table instead of hardcoding the zombies' 7–11); Windows build Successful ~278 MB.

**POSS death chain H0–L0 ✅ gate SUCCESS 2026-08-16** («в игре выглядит хорошо»), branch `3d-toggle`. Five drawings + five GLBs came as one set, so the whole pit ran in one pass — doomify preset A with `--palette-lump POSSA1`, hint projection with `--back-from-bake --back-grid 256`, lay-down last; tone matched the accepted live frames first try (one hint set, no statistics). Wave finding: **TRELLIS lays SOME lying frames down by itself, so the projection angle must be measured per frame, not assumed** — H0/K0 came back upright, I0/L0 already flat (L0 Y/X 0.33/0.91). The hint only paints correctly in the orientation where the mesh's +Z view matches it, found by comparing hint and mesh silhouettes over a pitch sweep with aspect dropped (the projection normalizes y globally, x per row): H0/I0/K0 at 0, J0 at −35°, L0 at +20°. Lay-down pitches follow the width-at-patch-height rule: J0 −30° (46 px vs 44), K0 −60° (50 vs 48); H0/I0/L0 need none (L0 52 vs 50). The «physically right» top-view projection for L0 (+80°, where the bake matches the drawing one-to-one) tested WORSE — the drawing foreshortens the body and a global linear y map can't reproduce that. **Accepted with a known defect:** near-white stripes along the undersides of the lying frames, worst on the L0 corpse. A source-mask probe (recolor the bake magenta and render it on the mesh) traced them to the **hint**, not the bake: on a body lying flat, top and bottom separate along the mesh's Y, not along the view axis, so the occlusion grid drops them into different cells, calls both of them front-facing and paints the underside with the jacket. Standing frames never show it, and the top-view projection that would fix the pairing tests worse overall. Fix candidates for the TROO/SARG/BOSS wave (none tried): bin occlusion along the body slab's normal instead of the view axis; a flat-frame mode that takes the underside from the bake unconditionally; or a luminance cap on the lower half, like `--lower-skin-fix` does for boots. At the 50×19 game footprint the stripes read as a highlight. **Import gotcha found the hard way:** the first build showed the corpses pure WHITE — pymeshlab writes `mtllib ./<lump>.obj.mtl` while the repo ships `<lump>.mtl`, so Unity imported the frames with its default material and `mainTexture` null; nothing in the suites noticed (attach, frame swaps and visibility are all correct on a white mesh). Closed at three levels: `doomify3d.decimate_with_uv` rewrites the `mtllib` line to the shipped name, a new EditMode test pins OBJ → MTL → albedo for every frame on disk, and the PlayMode death-chain test asserts a non-null `mainTexture` on every instantiated frame. Targeted suites: EditMode `MonsterModelFrameTableTests` 5/5, PlayMode `ExperimentalMonsterModelPlayTests` 7/7 (the covered-chain test now runs over POSS and SPOS; the uncovered-tail case moved to TROO); Windows build Successful ~268 MB.

**Enhanced 3D Objects Toggle ✅ closed 2026-08-07** (branch `3d-toggle`; spec/plan `2026-08-07-enhanced-3d-objects-toggle`). Options «3D Objects» On/Off (default On, settings schema **v3**), visible only in Enhanced; hot-apply. Presentation cascade (post EdgeMix removal 2026-08-08): Classic native → Enhanced+3D On mesh→redraw→native → Enhanced+3D Off redraw→native. Gate 0 allowlist (Point): `ARM1A0`/`BAR1A0`/`BFUGA0`/`COLUA0`/`CSAWA0`/`LAUNA0`/`MGUNA0`/`PLASA0`/`SHOTA0` (`STIMA0-v3` REJECT; v2/45° never shown in-game). **2026-08-08 (verdict «результат отличный»):** `ARM1B0`/`BAR1B0` redraws added → allowlist **11 lumps**; the `SpriteCache` animation gate now passes lumps whose **every** frame is covered (blocks partial coverage like BON1). Barrel routed through the pickup/redraw path with vanilla S_BAR1/S_BAR2 idle blink (A 6 → B 6 loop; the port previously always showed frame A); BEXP explosion stays on the generic effects path. Suites: EditMode **614/614**, PlayMode **161/161**. **2026-08-09 SUCCESS:** ammo `AMMOA0`/`CELLA0`/`CELPA0`/`ROCKA0`/`SHELA0` mesh+redraw (+ prior `CLIPA0`/`SBOXA0`) → display-redraw allowlist **21**. Runtime: `Assets/Resources/EnhancedSprites/`, `ObjectPresentationResolver`, `ExperimentalPickupModel` third state, `SpriteCache` `EnhancedDisplayRedraw`. Gate 1 standalone SUCCESS; Windows build ~182 MB. **2026-08-10 review fixes:** rotation-aware redraw coverage gate, tree heights in tests pinned to WAD patch headers, event-driven `ExperimentalPickupModel` visibility, single-open WAD in the import menu. Suites re-measured: EditMode **615/615**, PlayMode **162/162**.

- **Stage 0:** Unity project scaffolded at the repo root, Git initialized (branch `main`), Freedoom Phase 1 placed under `Assets/StreamingAssets/wads/freedoom1.wad`.
- **Stage 1:** WAD reader implemented in `Assets/Scripts/Wad/` (asmdef `Doom.Wad`, `noEngineReferences: true`). Public surface: `WadFile.Open(path)`, `Header`, `Directory`, `FindLump`, `ReadLump`, plus `WadMapNames.IsMapMarker`. Editor menu `Tools > Doom > Dump freedoom1.wad` confirms it reads the bundled WAD end-to-end. 30 EditMode tests (unit + integration) pass via Unity Test Framework.
- **Stage 2:** Map geometry pipeline implemented in `Assets/Scripts/Map/` (asmdef `Doom.Map`, `noEngineReferences: true`) and `Assets/Scripts/MapBuild/` (Unity glue). High-level API: `MapData.Load(wad, mapName)` → `MapGeometryBuilder.Build(map)` → per-sector `SectorMeshes`. Triangulation via vendored LibTessDotNet v1.1.15 (`Assets/ThirdParty/LibTessDotNet/`). `MapLoader` MonoBehaviour with auto-bootstrap renders E1M1 grey block-out on Play in `Stage2_MapPreview.unity`. 62 EditMode tests pass including freedoom1.wad integration coverage.
- **Stage 3:** Player + collisions. `MapLoader` spawns Player from `THINGS` Type 1 at scene start; `PlayerController` MonoBehaviour (`Doom.MapBuild`) uses the new Input System (`com.unity.inputsystem`, hand-built `InputActionMap`) for WASD + mouse-look + Shift-sprint. `worldScale = 1/32` plumbed through `MapGeometryBuilder` so the player is ~1.75 m tall and gravity = -9.81 m/s² works natively. A PlayMode test asserts the player lands on E1M1's floor without falling through.
- **Stage 4:** Palette and textures. New pure-C# assembly `Doom.Graphics` (`noEngineReferences`) decodes `PLAYPAL`, flats, patches, and `TEXTURE1`/`TEXTURE2`+`PNAMES` into `DecodedImage` (RGBA32). `Doom.Map` gained UV + DOOM pegging, per-sector vertex light, texture-grouped `WallSection`s, two-sided middle quads, and F_SKY1 ceiling skipping. `Doom.MapBuild` adds `TextureCache` (Texture2D Point+mipmap+anisotropic) and two Unlit shaders (`Doom/Unlit`, `Doom/UnlitCutout`); `MapLoader` assigns materials and writes UV + vertex colors. Sky and animation deferred.
- **Stage 5:** Sprite objects. New pure-C# assembly `Doom.Things` (`noEngineReferences`) ports the full DOOM `mobjinfo` table (`doomednum → sprite, frame, radius, height, flags`). `Doom.Graphics` gained `SpriteSet` (catalogs `S_START`/`S_END`, parses frame/rotation/mirror naming) and `Patch.ReadHeader` (size + draw offsets). `Doom.MapBuild` adds `SpriteCache` (sprite lump → cutout `Material`, pre-warmed while the WAD is open), `SpriteBillboard` (cylindrical facing + 8-rotation selection by view angle), and `ThingSpawner` (places billboards from `THINGS`, raycast floor/ceiling anchor, `CapsuleCollider` for SOLID things); `MapLoader` invokes it after geometry. Frame animation, AI, and item pickup deferred to Stage 6.
- **Stage 6a (doors & interactive sectors).** New pure-C# assembly `Doom.Specials` (`noEngineReferences`) ports the DOOM linedef-type table: `LineSpecialTable` (special number → `LineSpecial` kind/trigger/etc.) plus `SectorActions` (door/lift/floor target heights, stair builds) and `Neighbors` (sector adjacency via shared two-sided linedefs). `Doom.Map` gained `ISectorHeights`/`StaticSectorHeights` and a runtime-height `RebuildSector` (regenerates a sector's floor/ceiling/wall `MeshData` from live heights). `Doom.MapBuild` adds `RuntimeSectorHeights` (mutable per-sector heights), `SectorGeometry` (in-place mesh + collider rebuild registry), `SectorMover` (coroutine animating heights over time), and `LineActivator` (+`LineRef`) wiring Use/Walk/Switch triggers to sector actions; `PlayerController` gained a "Use" input (E key) that raycasts for a `LineRef`. Wall→linedef resolution for texture-grouped sections is geometric (sector-tagged `LineRef` + nearest-segment match) rather than a 1:1 mapping. Key checks deferred from 6a are done in 6e; Stage 7e added exits/teleports; Stage 8 Task 8 executes light specials. Still deferred from early 6a notes: crusher, scroll execution.
- **Stage 6b (player damage & HP).** New pure-C# assembly `Doom.Game` (`noEngineReferences`) holds `HealthModel` — HP/armor with DOOM armor absorption (green armor soaks 1/3 of damage, blue 1/2). `Doom.Specials` gained `SectorDamageTable` (sector special → damage-per-tic: nukage 5, slime 10, lava 20). `Doom.MapBuild` adds `SectorRef` (sector index on floor GOs), `PlayerHealth` (model wrapper + `Died` event), `FloorDamageSystem` (timed floor damage while grounded — downward raycast → `SectorRef`, every ~0.9 s), `PlayerDeathHandler` (freeze input + "You died" overlay + R-respawn), and `PlayerHud` (debug HP/armor OnGUI); all wired in `MapLoader.SpawnPlayer`. Deferred in 6b: radiation suit, special-11 level exit, secret sectors, armor/health pickups, crush damage, real HUD.
- **Stage 6c (weapons & shooting).** Four DOOM hitscan weapons (fist/pistol/shotgun/chaingun) with free aim. `Doom.Game` gained the pure rules: `DoomRandom` (P_Random port, 256-byte rndtable), `AmmoModel` (bullets/shells with DOOM start/max), `WeaponDef`/`WeaponTable` (frame/tic data from p_pspr.c/info.c state tables), `WeaponLoadout` (ownership, auto-select in P_CheckAmmo order), and `HitscanRules` (P_GunShot 5/10/15 and A_Punch 2–20 damage rolls, ±5.6° spread, 7-pellet shotgun volley). `Doom.Things` `ThingDef` gained `Health`/`CorpseFrame` for the E1 monsters (info.c). `Doom.MapBuild` adds `PlayerWeapons` (LMB + slot-keys 1–4 input, tic-based cooldowns, hitscan raycasts from the camera, ammo consumption + auto-downgrade, weapon/ammo pickups), `EnemyHealth` (HP; at zero swaps the billboard to its corpse frame and disables the collider), `HitEffect` (short-lived PUFF/BLUD billboards), `WeaponView` (OnGUI viewmodel on a virtual 320×200 screen per R_DrawPSprite, fire frames + muzzle flash + movement bob), and `ThingPickup` (trigger-sphere pickup for shotgun/chaingun/ammo things); `PlayerHud` shows AMMO; respawn resets the loadout. Notable fixes from the 6c review cycles: billboard quad meshes are destroyed with their GameObject (leak), `SpriteCache` caches negatives and warns on use after WAD close, the Use raycast ignores pickup triggers, shots originate at the camera (no point-blank dead zone), and weapon input freezes on death. Exploding barrels (2035) added post-6c (2026-07-11): HP 20, BEXP splash via `RadiusDamageRules`/`BarrelExplosion`, DSBAREXP. Still deferred from 6c: damage on the Nth tic of the fire state (applied on press instead), weapon-lower on death, keeping the loadout across respawn, queued weapon switch during cooldown.
- **Stage 6d (monster AI).** E1 roster (POSS/SPOS/TROO/SARG) wakes on sight/damage/gunfire noise, chases with DOOM 8-dir steps, opens doors, attacks (hitscan/melee/missile), infights, and dies with frame animation. `MonsterDef`/`MonsterTable` in `Doom.Things`; `MonsterRules`, `ChaseDir`, `MonsterBrain`+`IMonsterWorld` in `Doom.Game`; `NoiseAlert` in `Doom.Specials`. Unity glue — `SpriteBillboard.SetFrame`, `Projectile` (imp fireball), `MonsterController` (35 tics/s, capsule steps, sight, attacks, door use via `LineActivator`), `DamageSource`+infighting in `EnemyHealth`, `ThingSpawner`/`MapLoader` wiring, `NoiseAlertSystem` (gunfire wakes heard sectors); PlayMode `MonsterAiPlayTests` (5 tests, commit `14ae50f`). **Facing fix (2026-07-10):** `SpriteBillboard` rotation was `+202.5°` (180° off Stage 5's `+22.5°`), so a monster facing the camera always picked rot 4 (back) instead of rot 0 (front) — most visible on hitscan zombies; fixed to `+22.5°` with `MonsterController.Face()` each render frame while chasing. Visual sign-off (Task 12) confirmed interactively on E1M1. Deferred: XDEATH, flying monsters, sound (6f), A_Look chain-wake, ML_SOUNDBLOCK. Spec `docs/superpowers/specs/2026-07-03-monster-ai-design.md`; plan `docs/superpowers/plans/2026-07-03-monster-ai.md`.
- **Stage 6e (pickups).** Full E1 item set via pure `ItemRules` router: health (stim/medi/bonus/soul), armor (green/blue/bonus), six keys, backpack (×2 max + clip grant), berserk (heal-to-100, fist ×10, prefer fist), radiation suit (2100 tics). `HealthModel` gained GiveHealth/GiveArmor/GiveArmorBonus; new `KeyInventory`/`PlayerKey`, `PlayerPowers`, `DeathDropTable` (POSS→CLIP, SPOS→SHOTGUN); `AmmoModel` backpack. Unity: `PlayerInventory` facade, `ThingPickup`/`ThingSpawner` for all E1 pickups, `KeyMapping` + `LineActivator` key gate (card ≠ skull), ironfeet skip in `FloorDamageSystem`, `PickupFactory` corpse drops, HUD KEYS/BERSERK/SUIT. Respawn keeps keys, clears powers/backpack/ammo/weapons. PlayMode `PickupPlayTests` ×5. Deferred: invuln/invis/map/light-amp, rockets/cells/new weapons, pickup sound (6f), item spin anim, real HUD (Stage 7). Spec `docs/superpowers/specs/2026-07-10-pickups-design.md`; plan `docs/superpowers/plans/2026-07-10-pickups.md`.
- **Stage 6f (sound + music) ✅.** Pure-C# `Doom.Audio`: DMX `DS*` decoder; MUS + SMF MIDI parsers (`MusicScore` auto-detects — Freedoom `D_*` lumps are Type-1 MIDI, retail DOOM uses MUS); `GENMIDI` bank; `MusOplPlayer` (LittleMUS-derived sequencer) driving vendored managed Nuked OPL3 (`Assets/ThirdParty/NukedOpl/`, LGPL-2.1, commit `dc939ea`). `Doom.MapBuild`: `SoundCache`/`SoundSystem` (2D/3D pool), `MusicPlayer` (OPL via `OnAudioFilterRead` + silent carrier clip on a child of `PlayerCamera` — streaming `AudioClip` PCM callbacks are silent in Windows standalone), table-driven SFX pre-warm. Wired: weapons, pickups, player pain/death, monster FSM cues, projectile impact, doors/lifts/switches/key-deny, map music loop. Spec `docs/superpowers/specs/2026-07-10-sound-design.md`; plan `docs/superpowers/plans/2026-07-10-sound.md`. Known limits: no DMX channel stealing/pitch-shift, no sector occlusion.
- **Stage 7a (game session & level flow) ✅.** Pure `CampaignRoute`/`CampaignMap`/`SessionState`/`PlayerCarryState`; `GameSessionHost` (DontDestroyOnLoad); exit linedefs 11/51/52/124 + sector special 11; `LevelTransitionController` reloads `Stage2_MapPreview` with inventory carry-over (keys/powers cleared). Spec/plan: `2026-07-10-polish`.
- **Stage 7b (HUD & intermission) ✅.** WAD UI pipeline (`UiPatchCatalog`, `HudTextureCache`, `VirtualScreenRenderer`); pure `HudModel`/`FaceRules`/`FaceState`; `DoomHud` replaces debug `PlayerHud` (STBAR, tall/small digits, arms, keys, face); `LevelStats`/`LevelStatsTracker` + `IntermissionView` on exit. Fixes: health/armor `%` anchor (st_lib), ML_BLOCKING colliders on masked midtextures (grates). Interactive sign-off on E1M1. Spec/plan: `docs/superpowers/specs/2026-07-10-polish-design.md`, `docs/superpowers/plans/2026-07-10-polish.md`.
- **Stage 7c (menus & settings) ✅.** `GameFlowController` owns freeze/cursor/timescale (Playing/Paused/Dead/Intermission/MainMenu); WAD `MenuController` (main/pause); `GameSettingsData` + `SettingsStore`/`SettingsController` (SFX/music volume, sensitivity, invert Y, fullscreen); runtime volume + music pause/resume without sequencer restart. PlayMode `MenuPlayTests` ×7 + `SettingsPlayTests` ×3; EditMode `GameSettingsTests` ×5.
- **Stage 7d (full-world saves) ✅.** Pure `SaveGame`/`PlayerSnapshot`/`WorldSnapshot`/`SaveEntityId` + `SaveGameCodec`/`SaveSlotStore`; Unity `WorldStateRegistry`/`WorldSnapshotCapture`/`WorldSnapshotRestore`/`SaveGameController`; pause-menu Save and main/pause Load with WAD-identity preflight; scene reload then phased restore. PlayMode `WorldCapturePlayTests` ×2 + `SaveLoadPlayTests` ×3; EditMode snapshot/codec/store coverage in `Doom.Game.Tests` (179).
- **Stage 7e (E1 specials / perf / build) ✅.** Task 14: `TeleportRules`/`TeleportExecutor` (97/125/126), telefrag; spectre(58)+baron(3003); no E1 ProgressionBlockers; `E1SpecialsPlayTests` ×2. Task 15: `WalkLineIndex` + LineRef cache, `E1MapSmokePlayTests` (E1M1–E1M9), baseline `Logs/stage7e-baseline-notes.md`, `Tools > Doom > Build Windows Standalone` → `Builds/Windows/`, full suites **413 EditMode + 67 PlayMode** (2026-07-12), interactive sign-off (standalone music fix). Deferred visual: scroll 48, spectre MF_SHADOW translucency (linedef light 138 executed in Stage 8 Task 8).
- **Stage 8 (Classic/Enhanced graphics) ✅.** Spec/plan: `2026-07-11-enhanced-graphics`. Closed 2026-07-12: Tasks 1–15 including interactive Classic/Enhanced eyeball sign-off on standalone (URP 17.4.0, GraphicsMode settings v2, WAD sky/fluids/fog, lamps/particles, pause HUD isolation, Windows build). Suites at Task 14: **459 EditMode + 95 PlayMode**.

The master roadmap is in `docs/doom-unity-remake-plan.md` (written in Russian). Each stage has a paired design spec under `docs/superpowers/specs/` (`YYYY-MM-DD-<topic>-design.md`, the WHAT/WHY) and an implementation plan under `docs/superpowers/plans/` (`YYYY-MM-DD-<topic>.md`, task-by-task HOW): Stage 1 = `2026-05-28-wad-reader`, Stage 2 = `2026-05-28-geometry`, Stage 3 = `2026-05-29-player`, Stage 4 = `2026-05-30-textures`, Stage 6a = `2026-05-31-doors`, Stage 6b = `2026-05-31-player-damage`, Stage 6c = `2026-07-02-weapons`, Stage 6d = `2026-07-03-monster-ai`, Stage 6e = `2026-07-10-pickups`, Stage 6f = `2026-07-10-sound`, Stage 7 = `2026-07-10-polish` (spec adds the `-design` suffix), Stage 8 = `2026-07-11-enhanced-graphics`, post-8 texture quality = `2026-07-21-enhanced-texture-quality`, AI-derived assets doctrine = `2026-08-07-enhanced-ai-derived-assets`, 3D Objects toggle = `2026-08-07-enhanced-3d-objects-toggle`. **Stages 0–8 complete;** Enhanced Texture Quality ✅ closed 2026-07-24; 3D Objects toggle ✅ closed 2026-08-07 on `3d-toggle`.

## What this project is

A from-scratch reimplementation of DOOM in **Unity + C#** — a REMAKE, not just a port. The original WAD files supply the data (geometry, textures, sprites, sounds); all rendering, physics, and game logic are written anew on top of Unity. Two presentation modes: **Classic** (bit-faithful WAD rendering — nothing but WAD data on screen) and **Enhanced** (Unity-powered visuals that must preserve the original's style). Since 2026-08-07 Enhanced may additionally route **AI-derived presentation assets** (currently TRELLIS.2 3D meshes generated from WAD sprites); doctrine in `docs/superpowers/specs/2026-08-07-enhanced-ai-derived-assets-design.md`.

Development and distribution use **Freedoom** (free, format-compatible) rather than the commercial `DOOM.WAD`.

## Architectural ground rules

These decisions are load-bearing for the whole project — keep them in mind when designing any subsystem:

- **The original BSP data is intentionally discarded.** `NODES`, `SSECTORS`, `SEGS`, `BLOCKMAP`, `REJECT` are not read or used. Unity handles visibility and physics. Only the raw geometry lumps (`VERTEXES`, `LINEDEFS`, `SIDEDEFS`, `SECTORS`, `THINGS`) feed the level builder. Sector outlines are reconstructed by `SectorPolygonBuilder` (half-edge chaining with signed-area classification) — not by reading BSP segments.
- **Do not port the original C engine.** Port data formats and gameplay logic (sector behaviour, AI, weapon rules), not the renderer or BSP traversal.
- **WAD is the source of identity; Enhanced is a derived layer** (doctrine spec `2026-08-07-enhanced-ai-derived-assets-design.md`, supersedes the old "do not author custom content" rule). Classic renders only WAD data, bit-faithful, always. Maps, gameplay, collision, and save identity come from the WAD in every mode — AI never touches them. Enhanced may route AI-derived presentation assets, but each one is keyed to its source WAD lump, goes through the documented conditioning→generation→interactive-gate pipeline with provenance committed under `Textures/Trellis2/`, and stays presentation-only on the original thing root. The WAD is never unpacked into loose asset folders; the derived-asset registry lives on top of it. The central acceptance criterion is **аутентичность**: an asset must read as an object of the same pixel-art world — volume added to the original, not photorealism that outclasses the surroundings.
- **WAD binary format is little-endian.** Read with `BinaryReader` and stay consistent.
- **Wall texture pipeline is multi-step.** Wall textures are composed from patches via `TEXTURE1`/`TEXTURE2` + `PNAMES`, in column-major format with transparency. Flats (floor/ceiling) are raw 64×64 palette indices. Both go through `PLAYPAL` (256-color palette) before becoming `Texture2D`.
- **Sprites are camera-facing billboards**, with frame/angle encoded in the lump name.
- **World scale is `1 DOOM unit × worldScale = 1 Unity meter`**, with `worldScale = 1f / 32f` as the project default. Plumbed through `MapGeometryBuilder` so `MeshData` outputs are already in meters; `MapLoader` does not re-scale. Player height (56 DOOM units) becomes ~1.75 m; gravity is -9.81 m/s² natively.

## Roadmap shape

The plan (`docs/doom-unity-remake-plan.md`) is structured as stages 0–8, each ending in a visible, runnable milestone:

0. Environment setup → 1. WAD reader → 2. Grey block-out geometry of one map → 3. First-person player with collisions → 4. Palette and textures → 5. Sprite objects → 6. Gameplay (HP, weapons, AI, doors, sound) → 7. Polish (HUD, menus, saves, optimization) → 8. Classic/Enhanced graphics (URP).

Prefer landing each stage as its own visible milestone rather than building several layers in parallel — the plan explicitly calls out that most DOOM remakes die on textures, sprites, and AI, so each step should produce something you can see and run.

## Project layout

- `Assets/` — Unity assets (scripts, scenes, meshes — once we write them)
- `Assets/Materials/MapBlockOut/` — three Standard-shader materials (`Floor.mat`, `Ceiling.mat`, `Wall.mat`) for grey block-out rendering.
- `Assets/Scenes/Stage2_MapPreview.unity` — preview scene; auto-bootstrap creates `MapLoader` on Play, loads E1M1 from `freedoom1.wad`.
- `Assets/Scripts/Map/` — WAD geometry → C# `MeshData` (asmdef `Doom.Map`, `noEngineReferences: true`). Houses `MapData` (lump parsers + `Load`), `SectorPolygonBuilder` (half-edge chaining), `SectorTriangulator` (LibTessDotNet wrapper), `WallMeshBuilder`, and the `MapGeometryBuilder` orchestrator.
- `Assets/Scripts/MapBuild/` — Unity-coupled glue (asmdef `Doom.MapBuild`). `MapLoader` MonoBehaviour turns `MeshData` into `UnityEngine.Mesh`, instantiates per-sector `GameObject` hierarchy with `MeshFilter`/`MeshRenderer`/`MeshCollider`, and auto-bootstraps on scene load.
- `Assets/Scripts/Wad/` — WAD parser. Pure C# under asmdef `Doom.Wad` with `noEngineReferences: true` (the parser must not depend on `UnityEngine` — see architectural rules above).
- `Assets/Scripts/Wad/Editor/` — Editor-only tools (asmdef `Doom.Wad.Editor`, `includePlatforms: ["Editor"]`). Currently houses `WadInspectorMenu` (`Tools > Doom > Dump freedoom1.wad`).
- `Assets/StreamingAssets/wads/` — WAD files, copied verbatim into builds. **WADs live here, not in regular `Assets/`**, because anything else under `Assets/` goes through Unity's importer, which would try to interpret the binary as some asset type. `StreamingAssets` is the one Unity folder that ships files unchanged and is reachable at runtime via `Application.streamingAssetsPath`.
- `Assets/Tests/EditMode/Map/` — NUnit EditMode tests for the Map/MapBuild pipeline (asmdef `Doom.Map.Tests`): unit tests on `SyntheticMapBuilder`-built lumps plus `freedoom1.wad` integration/regression coverage (see the test-suite breakdown under "Build, run, test").
- `Assets/Tests/EditMode/Wad/` — NUnit EditMode tests (asmdef `Doom.Wad.Tests`). Unit tests use a `SyntheticWadBuilder`; integration tests run against `freedoom1.wad`.
- `Assets/Tests/PlayMode/` — Unity PlayMode tests (asmdef `Doom.Stage3.PlayTests`). Runs in Play context; required for verifying `CharacterController` + collider integration. Run WITHOUT `-nographics` (PhysX needs a real/null gfx device for collision callbacks).
- `Assets/Scripts/Audio/` — pure-C# WAD audio (`Doom.Audio`, `noEngineReferences`): DMX SFX, MUS/MIDI score parsers, GENMIDI, `MusOplPlayer`.
- `Assets/ThirdParty/LibTessDotNet/` — vendored LibTessDotNet v1.1.15 sources (SGI Free Software License B 2.0). Asmdef `LibTessDotNet` with `noEngineReferences: true`, `autoReferenced: false`.
- `Assets/ThirdParty/NukedOpl/` — vendored managed Nuked OPL3 (LGPL-2.1, SaxxonPike/NukedOpl `dc939ea`). Asmdef `NukedOpl`, `noEngineReferences: true`, `autoReferenced: false`.
- `Packages/`, `ProjectSettings/`, `UserSettings/` — Unity project metadata (committed; `UserSettings/` ignored per gitignore).
- `Library/`, `Temp/`, `Logs/`, `obj/` — Unity-generated, gitignored.
- `docs/` — design docs, including the stage-by-stage plan.

## Toolchain

- **Unity Editor:** `6000.4.8f1` at `C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe`. The version is pinned by `ProjectSettings/ProjectVersion.txt`; Unity Hub will auto-pick this editor when the project is opened.
- **Test WAD:** Freedoom v0.13.0 Phase 1 (`Assets/StreamingAssets/wads/freedoom1.wad`, ~28 MB), freely redistributable, so it is intentionally checked into Git — the repo should clone-and-run.

## Build, run, test

Day-to-day work happens inside the Unity Editor (open the repo root as a Unity project from Hub). No player build target is configured yet — that's a Stage 7 concern.

Useful CLI invocations:

```powershell
# Open the project in the editor (interactive)
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" -projectPath "D:\Development\doom"

# Headless: run EditMode tests (Unity Test Framework, NUnit-style)
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" `
    -batchmode -nographics -projectPath "D:\Development\doom" `
    -runTests -testPlatform EditMode `
    -testResults "D:\Development\doom\Logs\test-results.xml" `
    -logFile "D:\Development\doom\Logs\test-run.log"

# Headless: run PlayMode tests (slower; physics needs no -nographics)
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" `
    -batchmode -projectPath "D:\Development\doom" `
    -runTests -testPlatform PlayMode `
    -testResults "D:\Development\doom\Logs\playmode-results.xml" `
    -logFile "D:\Development\doom\Logs\playmode-run.log"

# Headless: execute the WAD dump menu without GUI
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" `
    -batchmode -nographics -projectPath "D:\Development\doom" `
    -executeMethod Doom.Wad.Editor.WadInspectorMenu.DumpFreedoom1 `
    -logFile "D:\Development\doom\Logs\dump.log" -quit
```

**Test CLI gotcha:** `-runTests` controls its own exit; do NOT add `-quit` with it (Unity exits before the runner starts). Per-test PASS/FAIL only lands in the `-testResults` XML, not the editor log. Use `-quit` only with `-executeMethod` or pure-compile runs.

The current test suite is **615 EditMode + 162 PlayMode** tests (2026-08-10, post-ammo review fixes; measured by the headless CLI runs above). Historical breakdown below remains useful for archaeology; totals grew with 7a–7e, rockets/chainsaw/plasma-BFG, E1 smoke/specials, Stage 8, upscale, texquality, warm-perf, EdgeMix (later deleted), and the 3D Objects toggle:
- 30 from Stage 1 (WAD reader): 4 integration tests against `freedoom1.wad`, the rest unit tests on `SyntheticWadBuilder`-built blobs.
- 32 from Stage 2 (Map pipeline): 5 integration tests against `freedoom1.wad`, the rest unit tests on `SyntheticMapBuilder`-built lumps.
- 9 from Stage 3 EditMode (Player): THINGS parsing (+2), THINGS as required lump in Load (+2), worldScale plumbing (+4), Freedoom Player-1-start integration (+1), plus 1 PlayMode test asserting the player lands on E1M1's floor.
- 2 geometry-robustness regressions on `freedoom1.wad` E1M1: `SectorPolygonBuilder` must produce simple (non-self-intersecting) sector rings, and `MapGeometryBuilder` output must contain no degenerate (zero-area) triangles. These pin the fixes for self-intersecting sector contours (greedy chaining → angular face-tracing) and zero-area wall quads.
- 6 winding/orientation regressions (`SectorWindingTests` ×5, `WallUvPeggingTests` ×1). Unity draws the side a triangle's `Cross(p1-p0, p2-p0)` points toward, so orientation must be pinned ABSOLUTELY: floors face up and ceilings face down even for sectors made of several disjoint rings (LibTess auto-computes its projection normal from the NET signed area of all contours, and misclassified disjoint "holes" — alternating stair steps sharing one sector number, E1M1 74/85 — flipped whole sectors inside-out); wall quads face INTO their owning sector (`WallMeshBuilder.EmitQuad` used to wind every wall visible only from the wrong side of the line — rectangular DOOM architecture masked it by showing far-face backs); back-side wall U runs V2→V1 (DOOM draws back segs reversed; else textures mirror for the back-sector viewer). These fixed the "background-colored holes" the stair/crate areas showed from Stage 2 onward.
- 24 from Stage 4 (palette & textures): 13 in `Doom.Graphics.Tests` (Palette, Flat, Patch, TextureSet unit tests on `SyntheticGfxBuilder` blobs + `freedoom1.wad` integration) and 11 added to `Doom.Map.Tests` (MeshData UV/Colors, flat UV+light, wall UV+pegging). The 2nd PlayMode test is the textured-E1M1 build smoke test.
- 14 from Stage 5 (sprites): 8 in `Doom.Things.Tests` (ThingDef flags, ThingTable lookups, every-Doom-1-sprite-resolves-in-freedoom1.wad regression + the Doom-2-only-gap guard) and 6 in `Doom.Graphics.Tests` (Patch header offsets, SpriteSet name/rotation/mirror parsing + freedoom integration). 2 added PlayMode tests assert E1M1 spawns billboards with renderers/colliders and that player starts spawn no sprite.
- 13 EditMode + 1 PlayMode from Stage 6a (doors & interactive sectors): 12 in `Doom.Specials.Tests` (`LineSpecialTests` ×1, `LineSpecialTableTests` ×5, `LineSpecialCoverageTests` ×1 — every E1M1 linedef special resolves in the table, `NeighborsTests` ×1, `SectorActionsTests` ×4) and `RuntimeHeightRebuildTests` ×1 in `Doom.Map.Tests` (rebuilding a sector from runtime heights moves its floor/ceiling mesh). The added PlayMode test is `SectorActionPlayTests` (a door's ceiling rises programmatically on activation).
- 13 EditMode + 3 PlayMode from Stage 6b (player damage & HP): 7 in `Doom.Game.Tests` (`HealthModel` — HP clamp/death, green/blue armor absorption + depletion) and 6 in `Doom.Specials.Tests` (`SectorDamageTable` — nukage/slime/lava damage-per-tic mapping + non-damaging specials). The 3 added PlayMode tests (`PlayerDamagePlayTests`) assert floor damage drains HP while grounded, death fires + freezes the player, and R-respawn restores HP.
- 19 EditMode + 3 PlayMode from Stage 6c (weapons & shooting): 17 in `Doom.Game.Tests` (`DoomRandomTests` ×3 — rndtable values/wrap/byte range, `AmmoModelTests` ×3 — start/max clamp, consume, reset, `WeaponTableTests` ×2 — def consistency + p_pspr.c cadence, `WeaponLoadoutTests` ×5 — start/give/select/P_CheckAmmo/reset, `HitscanRulesTests` ×4 — damage rolls, shotgun volley, first-shot accuracy) and 2 in `Doom.Things.Tests` (`MonsterDataTests` — E1 monster health/corpse frames + corpse sprites resolve in freedoom1.wad). The 3 PlayMode tests (`WeaponPlayTests`) assert the pistol kills a synthetic enemy and drains ammo, empty ammo auto-switches to fist, and a shotgun pickup gives the weapon + shells. NOTE: the `StairCaptureTests` harness renders via `Camera.Render()`, which excludes OnGUI/IMGUI — the viewmodel/HUD never appear in its PNGs, so `WeaponView` placement is not eyeball-verifiable headlessly. It was verified interactively in the editor instead, which caught a horizontal offset bug in the R_DrawPSprite port (a dropped `-160` term in `DrawPatch`; fixed).
- 4 PlayMode from the post-6b fix wave: `SectorTranslatePlayTests` (door ceiling is translated, not re-created — same mesh instance), `SectorRetriggerPlayTests` (lift re-triggers after a full cycle), `PlayerLiftRiderPlayTests` (player rides a rising floor), and `StairCaptureTests` (headless render-capture harness → `Logs/stair-spawn.png` + `Logs/stair-capture.png`; a diagnostic tool, re-run it to eyeball rendering changes).
- 31 EditMode from Stage 6d Tasks 1–6 (monster AI, pure C#): `MonsterTableTests` ×3, `MonsterRulesTests` ×4, `ChaseDirTests` ×6, `MonsterBrainWakeTests` ×7, `MonsterBrainCombatTests` ×8, `NoiseAlertTests` ×3 in `Doom.Game.Tests` / `Doom.Things.Tests` / `Doom.Specials.Tests`. **5 PlayMode** from Stage 6d Task 11: `MonsterAiPlayTests` (noise wake, melee/hitscan damage, imp fireball, death animation, infighting retarget).
- 22 EditMode + 5 PlayMode from Stage 6e (pickups): `HealthModel` give-API (+4), `KeyInventoryTests` ×3, `AmmoModelBackpackTests` ×3, `PlayerPowersTests` ×2, `ItemRulesTests` ×8, `DeathDropTableTests` ×1, berserk punch in `HitscanRulesTests` (+1). PlayMode `PickupPlayTests` ×5 (stim, green armor, locked door key gate on E1M2, POSS→CLIP drop, ironfeet blocks floor damage).
