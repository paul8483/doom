# Stage 6e: Pickups — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Полный E1-набор подбираемых предметов: здоровье/броня/ключи/рюкзак/берсерк/радиокостюм; ключи запирают двери; POSS/SPOS дропают CLIP/SHOTGUN; узкий подбор 6c остаётся рабочим через единый `ItemRules`.

**Architecture:** Чистые правила (`HealthModel` give-API, `KeyInventory`, `PlayerPowers`, `AmmoModel` backpack, `ItemRules`) — в `Doom.Game`; `KeyKind` уже в `Doom.Specials`; Unity-глю (`PlayerInventory`, расширенный `ThingPickup`, `LineActivator` key check, ironfeet в `FloorDamageSystem`, corpse drops) — в `Doom.MapBuild`. Спека: `docs/superpowers/specs/2026-07-10-pickups-design.md`.

**Статус:** реализован 2026-07-10; EditMode 215 + PlayMode 25 зелёные.

**Tech Stack:** Unity 6000.4.8f1, Unity Test Framework (EditMode + PlayMode), freedoom1.wad.

**Запуск тестов** (из CLAUDE.md; фильтр меняется по задаче):

```powershell
# EditMode
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" `
    -batchmode -nographics -projectPath "C:\Development\doom" `
    -runTests -testPlatform EditMode -testFilter "<FILTER>" `
    -testResults "C:\Development\doom\Logs\t.xml" -logFile "C:\Development\doom\Logs\t.log"
# PlayMode — то же, но БЕЗ -nographics и -testPlatform PlayMode
```

Готчи: не добавлять `-quit` к `-runTests`; результаты только в XML; код 198 «no valid license» или «project already open» → BLOCKED; таймаут 600000 мс; на каждый прогон — своё имя XML/лога.

**Все DOOM-числа ниже выверены по linuxdoom-1.10 (`p_inter.c`, `p_mobj.c`, `p_user.c`) — копировать из плана, не из исходников.**

---

## Таблица предметов (источник истины)

| Doomednum | Sprite | Эффект | Reject если |
|-----------|--------|--------|-------------|
| 2011 | STIM | `GiveHealth(10, 100)` | HP ≥ 100 |
| 2012 | MEDI | `GiveHealth(25, 100)` | HP ≥ 100 |
| 2014 | BON1 | `GiveHealth(1, 200)` | HP ≥ 200 |
| 2013 | SOUL | `GiveHealth(100, 200)` | HP ≥ 200 |
| 2018 | ARM1 | `GiveArmor(Green)` → Armor=100 | Armor ≥ 100 |
| 2019 | ARM2 | `GiveArmor(Blue)` → Armor=200 | Armor ≥ 200 |
| 2015 | BON2 | `GiveArmorBonus(1)` | Armor ≥ 200 |
| 5 | BKEY | `Keys.Give(BlueCard)` | never |
| 40 | BSKU | `Keys.Give(BlueSkull)` | never |
| 13 | RKEY | `Keys.Give(RedCard)` | never |
| 38 | RSKU | `Keys.Give(RedSkull)` | never |
| 6 | YKEY | `Keys.Give(YellowCard)` | never |
| 39 | YSKU | `Keys.Give(YellowSkull)` | never |
| 8 | BPAK | `Ammo.GiveBackpack()` + clip grant on every pickup | never (всегда accepted) |
| 2023 | PSTR | berserk + heal-to-100 if HP&lt;100 + select Fist | never |
| 2025 | SUIT | `IronFeetTics = 60*35` (2100) | never |
| 2001 | SHOT | Shotgun + 8 shells | already have + shells full |
| 2002 | MGUN | Chaingun + 20 bullets | already have + bullets full |
| 2007 | CLIP | +10 bullets | bullets full |
| 2048 | AMMO | +50 bullets | bullets full |
| 2008 | SHEL | +4 shells | shells full |
| 2049 | SBOX | +20 shells | shells full |

**Death drops (`P_KillMobj`):**

| Monster doomednum | Drop doomednum |
|-------------------|----------------|
| 3004 (POSS) | 2007 (CLIP) |
| 9 (SPOS) | 2001 (SHOT) |

**Backpack clip grants:** +10 Bullets, +4 Shells на каждом подборе (включая повторные; ракеты/ячейки — нет).
**Backpack max:** Bullets 400, Shells 100.
**Berserk fist:** обычный A_Punch `2*(1d10)`, при berserk ещё `×10`.
**IronFeet:** 2100 tics; berserk не тикает (`pw_strength` в DOOM не декрементится).

---

### Task 1: `HealthModel` — GiveHealth / GiveArmor / GiveArmorBonus

**Files:**
- Modify: `Assets/Scripts/Game/HealthModel.cs`
- Test: дополнить `Assets/Tests/EditMode/Game/HealthModelTests.cs` или создать `HealthModelGiveTests.cs`

- [x] **Step 1: Write the failing tests**

```csharp
[Test]
public void GiveHealth_stim_caps_at_100()
{
    var h = new HealthModel(90, 0, ArmorKind.None);
    Assert.That(h.GiveHealth(10, cap: 100), Is.True);
    Assert.That(h.Health, Is.EqualTo(100));
    Assert.That(h.GiveHealth(10, cap: 100), Is.False);
    Assert.That(h.Health, Is.EqualTo(100));
}

[Test]
public void GiveHealth_bonus_allows_up_to_200()
{
    var h = new HealthModel(100, 0, ArmorKind.None);
    Assert.That(h.GiveHealth(1, cap: 200), Is.True);
    Assert.That(h.Health, Is.EqualTo(101));
}

[Test]
public void GiveArmor_green_and_blue_rules()
{
    var h = new HealthModel();
    Assert.That(h.GiveArmor(ArmorKind.Green), Is.True);
    Assert.That(h.Armor, Is.EqualTo(100));
    Assert.That(h.ArmorType, Is.EqualTo(ArmorKind.Green));
    Assert.That(h.GiveArmor(ArmorKind.Green), Is.False);

    Assert.That(h.GiveArmor(ArmorKind.Blue), Is.True);
    Assert.That(h.Armor, Is.EqualTo(200));
    Assert.That(h.ArmorType, Is.EqualTo(ArmorKind.Blue));
    Assert.That(h.GiveArmor(ArmorKind.Green), Is.False);
    Assert.That(h.GiveArmor(ArmorKind.Blue), Is.False);
}

[Test]
public void GiveArmorBonus_sets_green_if_none()
{
    var h = new HealthModel();
    Assert.That(h.GiveArmorBonus(1), Is.True);
    Assert.That(h.Armor, Is.EqualTo(1));
    Assert.That(h.ArmorType, Is.EqualTo(ArmorKind.Green));
}
```

- [x] **Step 2: Run to verify FAIL** — filter новых тестов.

- [x] **Step 3: Implement**

```csharp
/// P_GiveBody: add up to cap; false if already at cap.
public bool GiveHealth(int amount, int cap)
{
    if (amount <= 0 || Health >= cap) return false;
    Health = System.Math.Min(Health + amount, cap);
    return true;
}

/// P_GiveArmor: hits = kind rank * 100; false if Armor >= hits.
public bool GiveArmor(ArmorKind kind)
{
    if (kind == ArmorKind.None) return false;
    int hits = kind == ArmorKind.Green ? 100 : 200;
    if (Armor >= hits) return false;
    ArmorType = kind;
    Armor = hits;
    return true;
}

/// Armor bonus +N ≤ MaxArmor; if no type yet → Green.
public bool GiveArmorBonus(int amount)
{
    if (amount <= 0 || Armor >= MaxArmor) return false;
    if (ArmorType == ArmorKind.None) ArmorType = ArmorKind.Green;
    Armor = System.Math.Min(Armor + amount, MaxArmor);
    return true;
}
```

- [x] **Step 4: Run to verify PASS.**
- [x] **Step 5: Commit** — `git commit -m "Stage 6e: HealthModel GiveHealth/GiveArmor/GiveArmorBonus"`.

---

### Task 2: `KeyInventory`

**Files:**
- Create: `Assets/Scripts/Game/KeyInventory.cs`, `Assets/Scripts/Game/PlayerKey.cs`
- Test: `Assets/Tests/EditMode/Game/KeyInventoryTests.cs`

**Решение:** `KeyInventory` в `Doom.Game` со своим enum `PlayerKey` (без зависимости Game→Specials). Маппинг `KeyKind` ↔ `PlayerKey` — в `Doom.MapBuild` (`KeyMapping`). `KeyKind.Any` → `HasAny()`.

- [x] **Step 1: Failing tests**

```csharp
[Test]
public void Give_and_Has_distinguish_card_and_skull()
{
    var k = new KeyInventory();
    k.Give(PlayerKey.RedCard);
    Assert.That(k.Has(PlayerKey.RedCard), Is.True);
    Assert.That(k.Has(PlayerKey.RedSkull), Is.False);
}

[Test]
public void HasAny_true_if_any_key()
{
    var k = new KeyInventory();
    Assert.That(k.HasAny(), Is.False);
    k.Give(PlayerKey.BlueSkull);
    Assert.That(k.HasAny(), Is.True);
}

[Test]
public void Give_idempotent_returns_false_if_owned()
{
    var k = new KeyInventory();
    Assert.That(k.Give(PlayerKey.YellowCard), Is.True);
    Assert.That(k.Give(PlayerKey.YellowCard), Is.False);
}
```

Для ключей `ItemRules` всегда accepted (GO исчезает), даже если `Give` вернул false.

- [x] **Step 2: Run to verify FAIL.**
- [x] **Step 3: Implement** bitset/`HashSet<PlayerKey>`; `Give`/`Has`/`HasAny`/`Reset`.
- [x] **Step 4: PASS.**
- [x] **Step 5: Commit** — `Stage 6e: KeyInventory - player key bits`.

---

### Task 3: `AmmoModel` backpack + `PlayerPowers`

**Files:**
- Modify: `Assets/Scripts/Game/AmmoModel.cs`
- Create: `Assets/Scripts/Game/PlayerPowers.cs`
- Test: `AmmoModelBackpackTests.cs`, `PlayerPowersTests.cs`

- [x] **Step 1: Tests**

```csharp
[Test]
public void GiveBackpack_doubles_max_and_grants_clips()
{
    var a = new AmmoModel(); // 50 bullets, 0 shells
    Assert.That(a.GiveBackpack(), Is.True);
    Assert.That(a.HasBackpack, Is.True);
    Assert.That(a.GetMax(AmmoType.Bullets), Is.EqualTo(400));
    Assert.That(a.GetMax(AmmoType.Shells), Is.EqualTo(100));
    Assert.That(a.Get(AmmoType.Bullets), Is.EqualTo(60)); // 50+10
    Assert.That(a.Get(AmmoType.Shells), Is.EqualTo(4));
}

[Test]
public void GiveBackpack_again_grants_more_clips()
{
    // DOOM: повторный backpack снова даёт клипы; max уже ×2.
    var a = new AmmoModel();
    a.GiveBackpack();
    int b = a.Get(AmmoType.Bullets);
    a.GiveBackpack();
    Assert.That(a.Get(AmmoType.Bullets), Is.EqualTo(b + 10));
    Assert.That(a.GetMax(AmmoType.Bullets), Is.EqualTo(400));
}

[Test]
public void Reset_clears_backpack()
{
    var a = new AmmoModel();
    a.GiveBackpack();
    a.Reset();
    Assert.That(a.HasBackpack, Is.False);
    Assert.That(a.GetMax(AmmoType.Bullets), Is.EqualTo(200));
}

[Test]
public void Berserk_does_not_tick_down()
{
    var p = new PlayerPowers();
    p.GiveBerserk();
    p.Advance(10_000);
    Assert.That(p.Berserk, Is.True);
}

[Test]
public void IronFeet_ticks_to_zero()
{
    var p = new PlayerPowers();
    p.GiveIronFeet(10);
    p.Advance(4);
    Assert.That(p.IronFeetTics, Is.EqualTo(6));
    p.Advance(100);
    Assert.That(p.IronFeetTics, Is.EqualTo(0));
}
```

- [x] **Step 2: FAIL.**
- [x] **Step 3: Implement** — `HasBackpack`; `GetMax` ×2; `GiveBackpack` (+10/+4); `Reset` clears flag. `PlayerPowers`: `Berserk`, `IronFeetTics`, `Give*`, `Advance`, `Reset`. `GiveBackpack` всегда true для ItemRules.
- [x] **Step 4: PASS.**
- [x] **Step 5: Commit** — `Stage 6e: AmmoModel backpack + PlayerPowers`.

---

### Task 4: `ItemRules` + berserk punch в `HitscanRules`

**Files:**
- Create: `Assets/Scripts/Game/ItemRules.cs`
- Modify: `Assets/Scripts/Game/HitscanRules.cs` (player punch path)
- Test: `ItemRulesTests.cs`, доп. к `HitscanRulesTests.cs`

```csharp
public sealed class PickupContext
{
    public HealthModel Health;
    public AmmoModel Ammo;
    public WeaponLoadout Loadout;
    public KeyInventory Keys;
    public PlayerPowers Powers;
    public bool PreferFist; // out: berserk → caller selects Fist
}

public static class ItemRules
{
    /// true = предмет поглощён (уничтожить GO).
    public static bool TryPickup(int doomedNum, PickupContext ctx) { /* таблица */ }
}
```

Оружейные кейсы — перенос логики из `PlayerWeapons.Pickup` (числа 8/20/10/50/4/20).

- [x] **Step 1: Tests** — по строке на ключевые doomednum (stim reject at 100, armor, key always, backpack, weapon regress); berserk punch:

```csharp
[Test]
public void Berserk_multiplies_punch_by_10()
{
    var rng = new DoomRandom(0);
    int normal = HitscanRules.PunchDamage(rng, berserk: false);
    rng = new DoomRandom(0);
    int strong = HitscanRules.PunchDamage(rng, berserk: true);
    Assert.That(strong, Is.EqualTo(normal * 10));
}
```

Если punch зашит в `FireVolley` — вынести чистую функцию с флагом `berserk`.

- [x] **Step 2: FAIL.**
- [x] **Step 3: Implement** `ItemRules` switch по таблице; berserk heal-to-100 + `PreferFist=true`.
- [x] **Step 4: PASS.**
- [x] **Step 5: Commit** — `Stage 6e: ItemRules router + berserk punch`.

---

### Task 5: `PlayerInventory` фасад + расширить `ThingPickup` / `ThingSpawner`

**Files:**
- Create: `Assets/Scripts/MapBuild/PlayerInventory.cs`
- Modify: `ThingPickup.cs`, `ThingSpawner.cs`, `PlayerWeapons.cs`, `PlayerHealth.cs`, `MapLoader.cs`, respawn wiring

- [x] **Step 1:** `PlayerInventory` на игроке:
  - ссылки Health / Weapons / Keys / Powers;
  - `bool TryPickup(int doomedNum)` → `ItemRules` + если `PreferFist` → select Fist;
  - `AdvancePowers` из `Update` (35 Hz accumulator).
- [x] **Step 2:** `ThingPickup.OnTriggerEnter` → `PlayerInventory.TryPickup`.
- [x] **Step 3:** `ThingSpawner` — вешать `ThingPickup` на **все** doomednum из таблицы (не только шесть 6c).
- [x] **Step 4:** `PlayerWeapons.Pickup` делегирует в `ItemRules` / inventory (регрессия shotgun PlayMode).
- [x] **Step 5:** Respawn: `Powers.Reset()`, ammo/weapons reset; **ключи не Reset**.
- [x] **Step 6: Commit** — `Stage 6e: PlayerInventory + ThingPickup for all E1 items`.

---

### Task 6: `LineActivator` key check + `KeyMapping`

**Files:**
- Create: `Assets/Scripts/MapBuild/KeyMapping.cs`
- Modify: `Assets/Scripts/MapBuild/LineActivator.cs`

- [x] **Step 1:** Убрать лог «opening anyway».
- [x] **Step 2:**

```csharp
if (sp.Key != KeyKind.None)
{
    if (inventory == null || !KeyMapping.HasRequired(inventory.Keys, sp.Key))
    {
        Debug.Log($"need key {sp.Key}");
        return;
    }
}
```

`KeyKind.Any` → `Keys.HasAny()`; иначе точный `PlayerKey`.
- [x] **Step 3:** Прокинуть `PlayerInventory` из `MapLoader` в `LineActivator`.
- [x] **Step 4: Commit** — `Stage 6e: LineActivator enforces key inventory`.

---

### Task 7: IronFeet в `FloorDamageSystem` + HUD

**Files:**
- Modify: `FloorDamageSystem.cs`, `PlayerHud.cs`

- [x] **Step 1:** Перед уроном: если `IronFeetTics > 0` → skip.
- [x] **Step 2:** HUD: `KEYS` (R/Y/B card|skull), `SUIT` / `BERSERK` если активны.
- [x] **Step 3: Commit** — `Stage 6e: ironfeet skips floor damage + HUD keys/powers`.

---

### Task 8: Death drops POSS/SPOS

**Files:**
- Create: `Assets/Scripts/Game/DeathDropTable.cs`
- Modify: `EnemyHealth.cs` и/или `MonsterController.cs`; helper spawn (рядом с `ThingSpawner`)

```csharp
public static class DeathDropTable
{
    public static bool TryGet(int monsterDoomEdNum, out int dropDoomEdNum);
    // 3004 → 2007, 9 → 2001
}
```

- [x] **Step 1:** EditMode тест таблицы.
- [x] **Step 2:** На смерти (один раз) — создать GO с билбордом + `ThingPickup` в позиции трупа через `SpriteCache`.
- [x] **Step 3: Commit** — `Stage 6e: POSS/SPOS death drops CLIP/SHOTGUN`.

---

### Task 9: EditMode — полный прогон

- [x] **Step 1:** Весь EditMode без фильтра; починить регрессии (`AmmoModelTests` Reset/GetMax и т.п.).
- [x] **Step 2:** Commit fixup при необходимости.

---

### Task 10: PlayMode `PickupPlayTests`

**Files:**
- Create: `Assets/Tests/PlayMode/PickupPlayTests.cs`

Минимум 5 тестов:

1. **Stim_heals_and_destroys** — HP 50 → 2011 → HP 60, GO gone; at HP 100 → GO remains.
2. **Green_armor_absorbs** — ARM1 → Armor 100; `TakeDamage(30)` теряет меньше HP, чем без брони.
3. **Key_gates_locked_door** — locked linedef без ключа не двигает ceiling; после Give — двигает.
4. **Poss_drops_clip** — смерть POSS → в сцене `ThingPickup` 2007.
5. **IronFeet_blocks_floor_damage** — suit + урон-сектор → HP не падает за тик урона.

- [x] **Step 1: Write tests.**
- [x] **Step 2: Green.**
- [x] **Step 3: Commit** — `Stage 6e Task 10: PickupPlayTests`.

---

### Task 11: визуальная проверка + документация + финал

**Files:**
- Modify: `CLAUDE.md`, `docs/doom-unity-remake-plan.md`, этот план (чекбоксы)

- [x] **Step 1: Глазная проверка.** E1M1: стимпаки, броня, дроп; E1M2: ключ/дверь; берсерк; костюм в нукаже.
- [x] **Step 2: `CLAUDE.md`** — абзац Stage 6e; счётчики тестов.
- [x] **Step 3: `docs/doom-unity-remake-plan.md`** — «Подбираемые предметы ✅».
- [x] **Step 4: Чекбоксы в этом плане.**
- [x] **Step 5: Финальный прогон** EditMode + PlayMode.
- [x] **Step 6: Commit** — `Stage 6e done: pickups - docs, plan checkboxes`.

---

## Порядок и зависимости

```
Task 1 (HealthModel give)
  → Task 2 (KeyInventory)
  → Task 3 (Backpack + Powers)
  → Task 4 (ItemRules + berserk punch)   [нужны 1–3]
  → Task 5 (Unity inventory + spawner)   [нужен 4]
  → Task 6 (LineActivator keys)          [нужны 2, 5]
  → Task 7 (IronFeet + HUD)              [нужны 3, 5]
  → Task 8 (Death drops)                 [нужен 5]
  → Task 9 (EditMode full)
  → Task 10 (PlayMode)
  → Task 11 (docs)
```

Tasks 6–8 после Task 5 можно частично параллелить (разные файлы), но не коммитить сломанную сборку.

## Известные упрощения (зафиксированы в спеке)

- Нет invuln / invis / map / light-amp.
- Нет ракет/ячеек и нового оружия.
- Нет звука подбора (6f).
- Нет анимации вращения бонусов.
- Ключи: точное совпадение card/skull (не «один цвет = оба»).
- Берсерк на весь уровень (не тикает), как `pw_strength` в DOOM.
- Респавн сохраняет ключи, сбрасывает powers/backpack/ammo/weapons.
