# Stage 6c: Weapons & Shooting — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Четыре hitscan-оружия DOOM (кулак/пистолет/дробовик/пулемёт) со свободным прицелом: враги получают HP и умирают в труп-кадр, патроны тратятся и подбираются, viewmodel анимирует выстрел, попадания дают PUFF/BLUD.

**Architecture:** Чистые правила (ГПСЧ, урон, боезапас, арсенал, таблица оружия) — в pure-C# `Doom.Game`; данные монстров (HP, кадр трупа) — в `Doom.Things`; Unity-глю (ввод+raycast, здоровье врага, viewmodel, эффекты, подбор) — в `Doom.MapBuild`. Спека: `docs/superpowers/specs/2026-07-02-weapons-design.md`.

**Tech Stack:** Unity 6000.4.8f1, Unity Test Framework (EditMode + PlayMode), Input System, freedoom1.wad.

**Запуск тестов** (из CLAUDE.md; фильтр меняется по задаче):

```powershell
# EditMode
& "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" `
    -batchmode -nographics -projectPath "D:\Development\doom" `
    -runTests -testPlatform EditMode -testFilter "<FILTER>" `
    -testResults "D:\Development\doom\Logs\t.xml" -logFile "D:\Development\doom\Logs\t.log"
# PlayMode — то же, но БЕЗ -nographics и -testPlatform PlayMode
```

Готчи: не добавлять `-quit` к `-runTests`; результат смотреть в XML; если Unity падает с кодом 198 «no valid license» — запустить Unity Hub, дождаться обновления лицензии, закрыть авто-открытый редактор (см. memory `unity-headless-license`).

**Ссылка на DOOM-источники:** все числа ниже уже выверены по linuxdoom-1.10 (`m_random.c`, `info.c`, `p_pspr.c`, `p_inter.c`) — переписывать из исходников не нужно, копировать из плана.

---

### Task 1: `DoomRandom` — порт P_Random

**Files:**
- Create: `Assets/Scripts/Game/DoomRandom.cs`
- Test: `Assets/Tests/EditMode/Game/DoomRandomTests.cs`

- [x] **Step 1: Write the failing test**

```csharp
using NUnit.Framework;
using Doom.Game;

namespace Doom.Game.Tests
{
    public class DoomRandomTests
    {
        [Test]
        public void First_values_match_doom_rndtable()
        {
            var r = new DoomRandom();
            // rndtable[1..8] — P_Random инкрементирует индекс ПЕРЕД чтением.
            Assert.That(r.Next(), Is.EqualTo(8));
            Assert.That(r.Next(), Is.EqualTo(109));
            Assert.That(r.Next(), Is.EqualTo(220));
            Assert.That(r.Next(), Is.EqualTo(222));
            Assert.That(r.Next(), Is.EqualTo(241));
            Assert.That(r.Next(), Is.EqualTo(149));
            Assert.That(r.Next(), Is.EqualTo(107));
            Assert.That(r.Next(), Is.EqualTo(75));
        }

        [Test]
        public void Wraps_after_256_and_seed_offsets_start()
        {
            var r = new DoomRandom();
            for (int i = 0; i < 256; i++) r.Next();
            Assert.That(r.Next(), Is.EqualTo(8), "после 256 значений индекс заворачивается");

            var seeded = new DoomRandom(seed: 1); // индекс 1 → первое значение rndtable[2]
            Assert.That(seeded.Next(), Is.EqualTo(109));
        }

        [Test]
        public void Values_are_bytes()
        {
            var r = new DoomRandom();
            for (int i = 0; i < 512; i++)
            {
                int v = r.Next();
                Assert.That(v, Is.InRange(0, 255));
            }
        }
    }
}
```

- [x] **Step 2: Run test to verify it fails**

Filter: `Doom.Map.Tests` не подходит — используй `Doom.Game.Tests.DoomRandomTests`.
Expected: FAIL (компиляция: `DoomRandom` не существует). Компиляционная ошибка в логе = «красный» для Unity; допустимо проверить красноту одним прогоном вместе со Step 3, если цикл слишком дорог, но по умолчанию — отдельный прогон.

- [x] **Step 3: Write minimal implementation**

`Assets/Scripts/Game/DoomRandom.cs`:

```csharp
namespace Doom.Game
{
    /// Порт P_Random из m_random.c: фиксированная 256-байтовая таблица + индекс.
    /// Детерминирован — юнит-тесты сверяют конкретные значения.
    public sealed class DoomRandom
    {
        static readonly byte[] RndTable =
        {
            0, 8, 109, 220, 222, 241, 149, 107, 75, 248, 254, 140, 16, 66, 74, 21,
            211, 47, 80, 242, 154, 27, 205, 128, 161, 89, 77, 36, 95, 110, 85, 48,
            212, 140, 211, 249, 22, 79, 200, 50, 28, 188, 52, 140, 202, 120, 68, 145,
            62, 70, 184, 190, 91, 197, 152, 224, 149, 104, 25, 178, 252, 182, 202, 182,
            141, 197, 4, 81, 181, 242, 145, 42, 39, 227, 156, 198, 225, 193, 219, 93,
            122, 175, 249, 0, 175, 143, 70, 239, 46, 246, 163, 53, 163, 109, 168, 135,
            2, 235, 25, 92, 20, 145, 138, 77, 69, 166, 78, 176, 173, 212, 166, 113,
            94, 161, 41, 50, 239, 49, 111, 164, 70, 60, 2, 37, 171, 75, 136, 156,
            11, 56, 42, 146, 138, 229, 73, 146, 77, 61, 98, 196, 135, 106, 63, 197,
            195, 86, 96, 203, 113, 101, 170, 247, 181, 113, 80, 250, 108, 7, 255, 237,
            129, 226, 79, 107, 112, 166, 103, 241, 24, 223, 239, 120, 198, 58, 60, 82,
            128, 3, 184, 66, 143, 224, 145, 224, 81, 206, 163, 45, 63, 90, 168, 114,
            59, 33, 159, 95, 28, 139, 123, 98, 125, 196, 15, 70, 194, 253, 54, 14,
            109, 226, 71, 17, 161, 93, 186, 87, 244, 138, 20, 52, 123, 251, 26, 36,
            17, 46, 52, 231, 232, 76, 31, 221, 84, 37, 216, 165, 212, 106, 197, 242,
            98, 43, 39, 175, 254, 145, 190, 84, 118, 222, 187, 136, 120, 163, 236, 249
        };

        int index;

        public DoomRandom(int seed = 0) { index = seed & 0xFF; }

        /// P_Random: rndindex = (rndindex+1)&0xff; return rndtable[rndindex].
        public int Next()
        {
            index = (index + 1) & 0xFF;
            return RndTable[index];
        }
    }
}
```

- [x] **Step 4: Run test to verify it passes** — filter `Doom.Game.Tests.DoomRandomTests`, expected 3/3 PASS.

- [x] **Step 5: Commit** — `git add Assets/Scripts/Game/DoomRandom.cs* Assets/Tests/EditMode/Game/DoomRandomTests.cs*` (включая .meta, которые сгенерирует Unity), `git commit -m "Stage 6c: DoomRandom - P_Random port with rndtable"`.

---

### Task 2: `AmmoType`, `WeaponId`, `AmmoModel`

**Files:**
- Create: `Assets/Scripts/Game/AmmoType.cs`, `Assets/Scripts/Game/WeaponId.cs`, `Assets/Scripts/Game/AmmoModel.cs`
- Test: `Assets/Tests/EditMode/Game/AmmoModelTests.cs`

- [x] **Step 1: Write the failing test**

```csharp
using NUnit.Framework;
using Doom.Game;

namespace Doom.Game.Tests
{
    public class AmmoModelTests
    {
        [Test]
        public void Starts_with_doom_loadout_and_clamps_to_max()
        {
            var a = new AmmoModel();
            Assert.That(a.Get(AmmoType.Bullets), Is.EqualTo(50));
            Assert.That(a.Get(AmmoType.Shells), Is.EqualTo(0));

            Assert.That(a.Add(AmmoType.Bullets, 500), Is.True);
            Assert.That(a.Get(AmmoType.Bullets), Is.EqualTo(200), "кламп к maxammo");
            Assert.That(a.Add(AmmoType.Bullets, 10), Is.False, "уже полно — не подобрано");
        }

        [Test]
        public void TryConsume_spends_or_refuses()
        {
            var a = new AmmoModel();
            Assert.That(a.TryConsume(AmmoType.Shells, 1), Is.False, "дроби нет");
            a.Add(AmmoType.Shells, 4);
            Assert.That(a.TryConsume(AmmoType.Shells, 1), Is.True);
            Assert.That(a.Get(AmmoType.Shells), Is.EqualTo(3));
        }

        [Test]
        public void None_ammo_is_always_available_and_reset_restores_start()
        {
            var a = new AmmoModel();
            Assert.That(a.TryConsume(AmmoType.None, 1), Is.True, "кулак патронов не требует");
            while (a.TryConsume(AmmoType.Bullets, 1)) { }
            a.Reset();
            Assert.That(a.Get(AmmoType.Bullets), Is.EqualTo(50));
            Assert.That(a.Get(AmmoType.Shells), Is.EqualTo(0));
        }
    }
}
```

- [x] **Step 2: Run to verify FAIL** (filter `Doom.Game.Tests.AmmoModelTests`).

- [x] **Step 3: Implement**

`AmmoType.cs`:
```csharp
namespace Doom.Game
{
    /// Типы боезапаса 6c. None — оружие без патронов (кулак).
    public enum AmmoType { None, Bullets, Shells }
}
```

`WeaponId.cs`:
```csharp
namespace Doom.Game
{
    public enum WeaponId { Fist, Pistol, Shotgun, Chaingun }
}
```

`AmmoModel.cs`:
```csharp
namespace Doom.Game
{
    /// Счётчики патронов игрока. Значения DOOM: старт 50 пуль,
    /// максимумы maxammo = 200 пуль / 50 дроби (p_inter.c, без рюкзака).
    public sealed class AmmoModel
    {
        public const int MaxBullets = 200;
        public const int MaxShells = 50;
        public const int StartBullets = 50;

        int bullets = StartBullets;
        int shells;

        public int Get(AmmoType t) => t switch
        {
            AmmoType.Bullets => bullets,
            AmmoType.Shells => shells,
            _ => 0,
        };

        public int GetMax(AmmoType t) => t switch
        {
            AmmoType.Bullets => MaxBullets,
            AmmoType.Shells => MaxShells,
            _ => 0,
        };

        /// true — что-то добавилось; false — уже полно (вещь не подбирается).
        public bool Add(AmmoType t, int n)
        {
            int cur = Get(t), max = GetMax(t);
            if (t == AmmoType.None || cur >= max) return false;
            Set(t, System.Math.Min(cur + n, max));
            return true;
        }

        public bool TryConsume(AmmoType t, int n)
        {
            if (t == AmmoType.None) return true;
            if (Get(t) < n) return false;
            Set(t, Get(t) - n);
            return true;
        }

        public void Reset() { bullets = StartBullets; shells = 0; }

        void Set(AmmoType t, int v)
        {
            if (t == AmmoType.Bullets) bullets = v;
            else if (t == AmmoType.Shells) shells = v;
        }
    }
}
```

- [x] **Step 4: Run to verify PASS.**
- [x] **Step 5: Commit** — `git commit -m "Stage 6c: AmmoModel with DOOM start/max values"`.

---

### Task 3: `WeaponDef` + `WeaponTable`

**Files:**
- Create: `Assets/Scripts/Game/WeaponDef.cs`, `Assets/Scripts/Game/WeaponTable.cs`
- Test: `Assets/Tests/EditMode/Game/WeaponTableTests.cs`

- [x] **Step 1: Write the failing test**

```csharp
using NUnit.Framework;
using Doom.Game;

namespace Doom.Game.Tests
{
    public class WeaponTableTests
    {
        [Test]
        public void All_four_weapons_have_consistent_defs()
        {
            foreach (WeaponId id in System.Enum.GetValues(typeof(WeaponId)))
            {
                var d = WeaponTable.Get(id);
                Assert.That(d.Slot, Is.InRange(1, 4), $"{id}: слот");
                Assert.That(d.Sprite, Has.Length.EqualTo(4), $"{id}: 4-символьный спрайт");
                Assert.That(d.FireFrames.Length, Is.EqualTo(d.FireTics.Length), $"{id}: кадры/тики");
                foreach (int t in d.FireTics) Assert.That(t, Is.GreaterThan(0));
                Assert.That(d.CycleTics, Is.GreaterThan(0));
                if (d.FlashSprite != null)
                    Assert.That(d.FlashFrames.Length, Is.EqualTo(d.FlashTics.Length));
            }
        }

        [Test]
        public void Doom_cadence_values()
        {
            // Суммы тиков из state-таблиц p_pspr.c/info.c (linuxdoom-1.10).
            Assert.That(WeaponTable.Get(WeaponId.Fist).CycleTics, Is.EqualTo(22));     // 4+4+5+4+5
            Assert.That(WeaponTable.Get(WeaponId.Pistol).CycleTics, Is.EqualTo(19));   // 4+6+4+5
            Assert.That(WeaponTable.Get(WeaponId.Shotgun).CycleTics, Is.EqualTo(44));  // 3+7+5+5+4+5+5+3+7
            Assert.That(WeaponTable.Get(WeaponId.Chaingun).CycleTics, Is.EqualTo(4));  // 1 выстрел / 4 тика
            Assert.That(WeaponTable.Get(WeaponId.Shotgun).Pellets, Is.EqualTo(7));
            Assert.That(WeaponTable.Get(WeaponId.Fist).Melee, Is.True);
            Assert.That(WeaponTable.Get(WeaponId.Pistol).FirstShotAccurate, Is.True);
        }
    }
}
```

- [x] **Step 2: Run to verify FAIL.**

- [x] **Step 3: Implement**

`WeaponDef.cs`:
```csharp
namespace Doom.Game
{
    /// Статические данные одного оружия. Кадры — индексы в спрайте viewmodel
    /// (0='A'), тики — DOOM-тики (35/с). CycleTics = сумма FireTics = кулдаун
    /// между выстрелами. Урон наносится в момент нажатия (упрощение против
    /// DOOM, где A_Fire* срабатывает на 3–10-м тике последовательности).
    public sealed class WeaponDef
    {
        public WeaponId Id;
        public int Slot;                 // клавиши 1..4
        public AmmoType Ammo;
        public int AmmoPerShot;          // 0 для кулака
        public int Pellets;              // лучей за выстрел (дробовик 7)
        public bool Melee;               // кулак: дальность 64 юнита
        public bool FirstShotAccurate;   // пистолет: без разброса вне очереди
        public string Sprite;            // viewmodel, напр. "PISG"
        public int IdleFrame;            // кадр покоя (0)
        public int[] FireFrames;         // последовательность кадров выстрела
        public int[] FireTics;           // тики каждого кадра
        public string FlashSprite;       // null — без вспышки (кулак)
        public int[] FlashFrames;
        public int[] FlashTics;

        public int CycleTics
        {
            get { int s = 0; foreach (int t in FireTics) s += t; return s; }
        }
    }
}
```

`WeaponTable.cs` (данные из info.c: S_PUNCH1..5, S_PISTOL1..4, S_SGUN1..9, S_CHAIN1..3 + вспышки):
```csharp
namespace Doom.Game
{
    public static class WeaponTable
    {
        static readonly WeaponDef Fist = new WeaponDef
        {
            Id = WeaponId.Fist, Slot = 1, Ammo = AmmoType.None, AmmoPerShot = 0,
            Pellets = 1, Melee = true, FirstShotAccurate = false,
            Sprite = "PUNG", IdleFrame = 0,
            FireFrames = new[] { 1, 2, 3, 2, 1 }, FireTics = new[] { 4, 4, 5, 4, 5 },
            FlashSprite = null, FlashFrames = System.Array.Empty<int>(),
            FlashTics = System.Array.Empty<int>(),
        };

        static readonly WeaponDef Pistol = new WeaponDef
        {
            Id = WeaponId.Pistol, Slot = 2, Ammo = AmmoType.Bullets, AmmoPerShot = 1,
            Pellets = 1, Melee = false, FirstShotAccurate = true,
            Sprite = "PISG", IdleFrame = 0,
            FireFrames = new[] { 0, 1, 2, 1 }, FireTics = new[] { 4, 6, 4, 5 },
            FlashSprite = "PISF", FlashFrames = new[] { 0 }, FlashTics = new[] { 7 },
        };

        static readonly WeaponDef Shotgun = new WeaponDef
        {
            Id = WeaponId.Shotgun, Slot = 3, Ammo = AmmoType.Shells, AmmoPerShot = 1,
            Pellets = 7, Melee = false, FirstShotAccurate = false,
            Sprite = "SHTG", IdleFrame = 0,
            FireFrames = new[] { 0, 0, 1, 2, 3, 2, 1, 0, 0 },
            FireTics = new[] { 3, 7, 5, 5, 4, 5, 5, 3, 7 },
            FlashSprite = "SHTF", FlashFrames = new[] { 0, 1 }, FlashTics = new[] { 4, 3 },
        };

        // DOOM: 2 выстрела за 8 тиков (S_CHAIN1 A 4 + S_CHAIN2 B 4). Моделируем
        // как 1 выстрел / 4 тика с чередованием кадра — та же скорострельность.
        static readonly WeaponDef Chaingun = new WeaponDef
        {
            Id = WeaponId.Chaingun, Slot = 4, Ammo = AmmoType.Bullets, AmmoPerShot = 1,
            Pellets = 1, Melee = false, FirstShotAccurate = false,
            Sprite = "CHGG", IdleFrame = 0,
            FireFrames = new[] { 1 }, FireTics = new[] { 4 },
            FlashSprite = "CHGF", FlashFrames = new[] { 0 }, FlashTics = new[] { 5 },
        };

        public static WeaponDef Get(WeaponId id) => id switch
        {
            WeaponId.Fist => Fist,
            WeaponId.Pistol => Pistol,
            WeaponId.Shotgun => Shotgun,
            _ => Chaingun,
        };
    }
}
```

- [x] **Step 4: Run to verify PASS.**
- [x] **Step 5: Commit** — `git commit -m "Stage 6c: WeaponDef/WeaponTable with p_pspr.c timings"`.

---

### Task 4: `WeaponLoadout`

**Files:**
- Create: `Assets/Scripts/Game/WeaponLoadout.cs`
- Test: `Assets/Tests/EditMode/Game/WeaponLoadoutTests.cs`

- [x] **Step 1: Write the failing test**

```csharp
using NUnit.Framework;
using Doom.Game;

namespace Doom.Game.Tests
{
    public class WeaponLoadoutTests
    {
        [Test]
        public void Starts_with_fist_and_pistol_current_pistol()
        {
            var l = new WeaponLoadout();
            Assert.That(l.Has(WeaponId.Fist), Is.True);
            Assert.That(l.Has(WeaponId.Pistol), Is.True);
            Assert.That(l.Has(WeaponId.Shotgun), Is.False);
            Assert.That(l.Current, Is.EqualTo(WeaponId.Pistol));
        }

        [Test]
        public void Give_new_weapon_returns_true_and_autoselects()
        {
            var l = new WeaponLoadout();
            Assert.That(l.Give(WeaponId.Shotgun), Is.True);
            Assert.That(l.Current, Is.EqualTo(WeaponId.Shotgun));
            Assert.That(l.Give(WeaponId.Shotgun), Is.False, "повторная выдача — false");
        }

        [Test]
        public void TrySelect_only_owned()
        {
            var l = new WeaponLoadout();
            Assert.That(l.TrySelect(WeaponId.Chaingun), Is.False);
            Assert.That(l.TrySelect(WeaponId.Fist), Is.True);
            Assert.That(l.Current, Is.EqualTo(WeaponId.Fist));
        }

        [Test]
        public void BestAvailable_follows_p_checkammo_order()
        {
            // Порядок P_CheckAmmo: chaingun → shotgun → pistol → fist.
            var l = new WeaponLoadout();
            var ammo = new AmmoModel();          // 50 пуль, 0 дроби
            l.Give(WeaponId.Shotgun); l.Give(WeaponId.Chaingun);
            Assert.That(l.BestAvailable(ammo), Is.EqualTo(WeaponId.Chaingun));
            while (ammo.TryConsume(AmmoType.Bullets, 1)) { }
            Assert.That(l.BestAvailable(ammo), Is.EqualTo(WeaponId.Fist),
                "пули кончились, дроби не было — вниз до кулака");
            ammo.Add(AmmoType.Shells, 4);
            Assert.That(l.BestAvailable(ammo), Is.EqualTo(WeaponId.Shotgun));
        }

        [Test]
        public void Reset_restores_start()
        {
            var l = new WeaponLoadout();
            l.Give(WeaponId.Chaingun);
            l.Reset();
            Assert.That(l.Has(WeaponId.Chaingun), Is.False);
            Assert.That(l.Current, Is.EqualTo(WeaponId.Pistol));
        }
    }
}
```

- [x] **Step 2: Run to verify FAIL.**

- [x] **Step 3: Implement**

```csharp
namespace Doom.Game
{
    /// Арсенал игрока: чем владеет, что в руках. Старт DOOM — кулак+пистолет.
    public sealed class WeaponLoadout
    {
        readonly bool[] owned = new bool[4];
        public WeaponId Current { get; private set; }

        public WeaponLoadout() { Reset(); }

        public bool Has(WeaponId id) => owned[(int)id];

        /// true если оружие новое (и тогда авто-переключение на него, как в DOOM).
        public bool Give(WeaponId id)
        {
            if (owned[(int)id]) return false;
            owned[(int)id] = true;
            Current = id;
            return true;
        }

        public bool TrySelect(WeaponId id)
        {
            if (!owned[(int)id]) return false;
            Current = id;
            return true;
        }

        /// Лучшее оружие, на которое хватает патронов (порядок P_CheckAmmo).
        public WeaponId BestAvailable(AmmoModel ammo)
        {
            foreach (var id in new[] { WeaponId.Chaingun, WeaponId.Shotgun, WeaponId.Pistol })
            {
                var def = WeaponTable.Get(id);
                if (owned[(int)id] && ammo.Get(def.Ammo) >= def.AmmoPerShot) return id;
            }
            return WeaponId.Fist;
        }

        public void Reset()
        {
            for (int i = 0; i < owned.Length; i++) owned[i] = false;
            owned[(int)WeaponId.Fist] = true;
            owned[(int)WeaponId.Pistol] = true;
            Current = WeaponId.Pistol;
        }
    }
}
```

- [x] **Step 4: Run to verify PASS.**
- [x] **Step 5: Commit** — `git commit -m "Stage 6c: WeaponLoadout - ownership, select, P_CheckAmmo order"`.

---

### Task 5: `HitscanRules`

**Files:**
- Create: `Assets/Scripts/Game/HitscanRules.cs`
- Test: `Assets/Tests/EditMode/Game/HitscanRulesTests.cs`

- [x] **Step 1: Write the failing test**

```csharp
using System.Collections.Generic;
using NUnit.Framework;
using Doom.Game;

namespace Doom.Game.Tests
{
    public class HitscanRulesTests
    {
        [Test]
        public void GunShot_damage_is_5_10_or_15()
        {
            var r = new DoomRandom();
            var seen = new HashSet<int>();
            for (int i = 0; i < 300; i++) seen.Add(HitscanRules.GunShotDamage(r));
            Assert.That(seen, Is.EquivalentTo(new[] { 5, 10, 15 }));
        }

        [Test]
        public void Punch_damage_is_even_2_to_20()
        {
            var r = new DoomRandom();
            for (int i = 0; i < 300; i++)
            {
                int d = HitscanRules.PunchDamage(r);
                Assert.That(d, Is.InRange(2, 20));
                Assert.That(d % 2, Is.EqualTo(0));
            }
        }

        [Test]
        public void Shotgun_volley_is_7_pellets_within_spread()
        {
            var r = new DoomRandom();
            var shots = new List<HitscanShot>();
            HitscanRules.FireVolley(WeaponTable.Get(WeaponId.Shotgun), refire: false, r, shots);
            Assert.That(shots.Count, Is.EqualTo(7));
            foreach (var s in shots)
            {
                Assert.That(s.YawOffsetDeg, Is.InRange(-5.61f, 5.61f));
                Assert.That(s.Damage, Is.AnyOf(5, 10, 15));
            }
        }

        [Test]
        public void Pistol_first_shot_accurate_refire_spreads()
        {
            var r = new DoomRandom();
            var shots = new List<HitscanShot>();
            HitscanRules.FireVolley(WeaponTable.Get(WeaponId.Pistol), refire: false, r, shots);
            Assert.That(shots.Count, Is.EqualTo(1));
            Assert.That(shots[0].YawOffsetDeg, Is.EqualTo(0f), "первый выстрел точный");

            bool anySpread = false;
            for (int i = 0; i < 50; i++)
            {
                shots.Clear();
                HitscanRules.FireVolley(WeaponTable.Get(WeaponId.Pistol), refire: true, r, shots);
                if (shots[0].YawOffsetDeg != 0f) anySpread = true;
            }
            Assert.That(anySpread, Is.True, "очередь — с разбросом");
        }
    }
}
```

- [x] **Step 2: Run to verify FAIL.**

- [x] **Step 3: Implement**

```csharp
using System.Collections.Generic;

namespace Doom.Game
{
    public readonly struct HitscanShot
    {
        public readonly float YawOffsetDeg;
        public readonly int Damage;
        public HitscanShot(float yaw, int damage) { YawOffsetDeg = yaw; Damage = damage; }
    }

    /// Формулы hitscan DOOM (p_pspr.c / p_map.c), поверх DoomRandom.
    public static class HitscanRules
    {
        public const float HitscanRangeDoom = 2048f;  // MISSILERANGE
        public const float MeleeRangeDoom = 64f;      // MELEERANGE
        // (P_Random()-P_Random())<<18 в BAM: 1 ед. = 360/16384 ≈ 0.022°, max ±5.6°.
        const float SpreadUnitDeg = 360f / 16384f;

        public static int GunShotDamage(DoomRandom r) => 5 * (r.Next() % 3 + 1);   // P_GunShot
        public static int PunchDamage(DoomRandom r) => (r.Next() % 10 + 1) * 2;    // A_Punch

        public static float SpreadOffsetDeg(DoomRandom r)
            => (r.Next() - r.Next()) * SpreadUnitDeg;

        /// Залп одного нажатия: (смещение по yaw, урон) на каждый луч.
        public static void FireVolley(WeaponDef def, bool refire, DoomRandom r,
                                      List<HitscanShot> outShots)
        {
            for (int i = 0; i < def.Pellets; i++)
            {
                int damage = def.Melee ? PunchDamage(r) : GunShotDamage(r);
                bool accurate = def.FirstShotAccurate && !refire && !def.Melee;
                // Дробовик и пулемёт всегда с разбросом; кулак в DOOM тоже
                // рыскает на ±5.6° (A_Punch).
                float yaw = (accurate && def.Pellets == 1) ? 0f : SpreadOffsetDeg(r);
                outShots.Add(new HitscanShot(yaw, damage));
            }
        }
    }
}
```

- [x] **Step 4: Run to verify PASS** (filter `Doom.Game.Tests` — заодно все Game-тесты).
- [x] **Step 5: Commit** — `git commit -m "Stage 6c: HitscanRules - DOOM damage rolls and spread"`.

---

### Task 6: `ThingDef.Health/CorpseFrame` + данные монстров

**Files:**
- Modify: `Assets/Scripts/Things/ThingDef.cs` (двa новых поля конструктора)
- Modify: `Assets/Scripts/Things/ThingTable.cs` (строки монстров)
- Test: `Assets/Tests/EditMode/Things/MonsterDataTests.cs` (create)

**ВНИМАНИЕ:** у `ThingDef` есть другие вызовы конструктора в `ThingTable` — добавляй новые параметры С ДЕФОЛТАМИ (`int health = 0, int corpseFrame = -1`), чтобы не трогать все строки таблицы.

- [x] **Step 1: Write the failing test**

```csharp
using System.IO;
using NUnit.Framework;
using UnityEngine;
using Doom.Things;
using Doom.Wad;
using Doom.Graphics;

namespace Doom.Things.Tests
{
    public class MonsterDataTests
    {
        // info.c: MT_POSSESSED/MT_SHOTGUY/MT_TROOP/MT_SERGEANT.
        // Кадр трупа = кадр последнего state смерти (tics -1).
        static readonly (int ed, int hp, int corpse)[] Monsters =
        {
            (3004, 20, 11),  // POSS: S_POSS_DIE5, frame 11 = 'L'
            (9,    30, 11),  // SPOS: S_SPOS_DIE5, frame 11 = 'L'
            (3001, 60, 12),  // TROO: S_TROO_DIE5, frame 12 = 'M'
            (3002, 150, 13), // SARG: S_SARG_DIE6, frame 13 = 'N'
        };

        [Test]
        public void E1_monsters_have_health_and_corpse_frame()
        {
            foreach (var (ed, hp, corpse) in Monsters)
            {
                Assert.That(ThingTable.TryGet(ed, out var def), Is.True, $"doomednum {ed}");
                Assert.That(def.Health, Is.EqualTo(hp), $"{ed} health");
                Assert.That(def.CorpseFrame, Is.EqualTo(corpse), $"{ed} corpse frame");
                Assert.That(def.Has(ThingFlags.Shootable), Is.True, $"{ed} shootable");
            }
        }

        [Test]
        public void Corpse_sprites_resolve_in_freedoom()
        {
            string path = Path.Combine(Application.streamingAssetsPath, "wads", "freedoom1.wad");
            using var wad = WadFile.Open(path);
            var sprites = SpriteSet.Load(wad);
            foreach (var (ed, _, corpse) in Monsters)
            {
                ThingTable.TryGet(ed, out var def);
                // Кадры смерти без ротаций → ищем ротацию 0. API поиска — тот же,
                // что в существующем SpriteSetFreedoomTests (см. файл рядом).
                Assert.That(sprites.TryGet(def.Sprite, corpse, 0, out _), Is.True,
                    $"{def.Sprite} кадр {corpse} должен существовать в freedoom1");
            }
        }
    }
}
```

Если сигнатура `SpriteSet` отличается (например, `Get`/`Resolve` вместо `TryGet`) — подстрой вызов под фактический API, посмотрев `Assets/Tests/EditMode/Graphics/SpriteSetFreedoomTests.cs`; смысл ассерта не менять.

- [x] **Step 2: Run to verify FAIL** (filter `Doom.Things.Tests.MonsterDataTests`): нет полей `Health`/`CorpseFrame`.

- [x] **Step 3: Implement**

`ThingDef.cs` — добавить поля и параметры с дефолтами:

```csharp
public readonly int Health;       // 0 = неубиваемое (декорации, предметы)
public readonly int CorpseFrame;  // финальный кадр смерти (info.c), -1 = нет

public ThingDef(int doomEdNum, string sprite, int frame,
                int radius, int height, ThingFlags flags,
                int health = 0, int corpseFrame = -1)
{
    // ... существующие присваивания ...
    Health = health;
    CorpseFrame = corpseFrame;
}
```

`ThingTable.cs` — в строках четырёх монстров дописать два аргумента
(`health:`/`corpseFrame:` из теста выше). Пример для POSS (найди строку с
doomednum 3004 и добавь):

```csharp
// было: new ThingDef(3004, "POSS", 0, 20, 56, ThingFlags.Solid | ThingFlags.Shootable | ThingFlags.CountKill)
new ThingDef(3004, "POSS", 0, 20, 56,
    ThingFlags.Solid | ThingFlags.Shootable | ThingFlags.CountKill,
    health: 20, corpseFrame: 11)
```

Аналогично: 9/SPOS → 30/11; 3001/TROO → 60/12; 3002/SARG → 150/13.

- [x] **Step 4: Run to verify PASS** (оба теста; второй — интеграция с WAD).
- [x] **Step 5: Run** полный EditMode без фильтра — существующие Things-тесты не сломаны.
- [x] **Step 6: Commit** — `git commit -m "Stage 6c: ThingDef health/corpse frame for E1 monsters"`.

---

### Task 7: `EnemyHealth` + труп в `SpriteBillboard` + навеска в `ThingSpawner`

**Files:**
- Create: `Assets/Scripts/MapBuild/EnemyHealth.cs`
- Modify: `Assets/Scripts/MapBuild/SpriteBillboard.cs` (метод `SetStaticFrame`)
- Modify: `Assets/Scripts/MapBuild/ThingSpawner.cs` (навеска + прогрев кадра трупа)

PlayMode-проверка этого куска — в Task 12 (одним заходом со стрельбой); здесь только компиляция + ручная навеска. Это исключение из TDD-каденции: у чистой логики теста нет, потому что вся логика — Unity-side.

- [x] **Step 1: `SpriteBillboard.SetStaticFrame`**

Добавить поле и метод (после `Init`):

```csharp
bool lockRotation;

/// Переключить билборд на статичный кадр без выбора ротаций (труп: у кадров
/// смерти DOOM ротаций нет — всегда ротация 0).
public void SetStaticFrame(int newFrame)
{
    frame = newFrame;
    lockRotation = true;
}
```

В `LateUpdate` заменить вычисление `rotIndex`:

```csharp
int rotIndex = 0;
if (!lockRotation)
{
    float angToCam = Mathf.Atan2(to.z, to.x) * Mathf.Rad2Deg;
    float diff = angToCam - doomAngleDeg + 202.5f;
    rotIndex = Mathf.FloorToInt(Mod360(diff) / 45f) & 7;
}
```

- [x] **Step 2: `EnemyHealth.cs`**

```csharp
using UnityEngine;

namespace Doom.MapBuild
{
    /// HP врага (Stage 6c). Без ИИ и боли: копит урон, при нуле — труп-кадр,
    /// коллайдер выключается, дальнейший урон игнорируется.
    public sealed class EnemyHealth : MonoBehaviour
    {
        int hp;
        int corpseFrame;
        SpriteBillboard billboard;   // может быть null в синтетических тестах
        CapsuleCollider capsule;

        public bool IsDead => hp <= 0;

        public void Init(int health, int corpseFrame,
                         SpriteBillboard billboard, CapsuleCollider capsule)
        {
            hp = health;
            this.corpseFrame = corpseFrame;
            this.billboard = billboard;
            this.capsule = capsule;
        }

        public void TakeDamage(int damage)
        {
            if (IsDead) return;
            hp -= damage;
            if (hp <= 0) Die();
        }

        void Die()
        {
            hp = 0;
            if (billboard != null && corpseFrame >= 0)
                billboard.SetStaticFrame(corpseFrame);
            if (capsule != null) capsule.enabled = false;
        }
    }
}
```

- [x] **Step 3: навеска в `ThingSpawner.SpawnAll`**

После блока `if (def.Has(ThingFlags.Solid)) { ... }` (переменная коллайдера
нужна — вынеси `CapsuleCollider col = null;` перед блоком и присвой внутри):

```csharp
if (def.Has(ThingFlags.Shootable) && def.Health > 0)
{
    var eh = go.AddComponent<EnemyHealth>();
    eh.Init(def.Health, def.CorpseFrame, bb, col);
    if (def.CorpseFrame >= 0)
        cache.Get(def.Sprite, def.CorpseFrame, 0); // прогрев, пока WAD открыт
}
```

- [x] **Step 4: Компиляция** — прогнать EditMode полный (быстрый компайл-гейт): 0 ошибок, все тесты зелёные.
- [x] **Step 5: Commit** — `git commit -m "Stage 6c: EnemyHealth + corpse frame on billboards"`.

---

### Task 8: эффекты попаданий (PUFF/BLUD)

**Files:**
- Create: `Assets/Scripts/MapBuild/HitEffect.cs`

- [x] **Step 1: Implement** (логика тривиальна и Unity-only; проверяется PlayMode-тестом Task 12 косвенно и глазами)

```csharp
using UnityEngine;

namespace Doom.MapBuild
{
    /// Короткоживущий билборд-эффект: последовательность кадров одного спрайта
    /// (PUFF A→D, BLUD C→A), затем Destroy. Кадры/тики — info.c.
    public sealed class HitEffect : MonoBehaviour
    {
        SpriteBillboard billboard;
        int[] frames;
        float[] secs;
        int idx;
        float left;

        public static void SpawnPuff(SpriteCache cache, float worldScale, Vector3 pos, Vector3 normal)
        {
            // Чуть от поверхности к стрелку, чтобы не тонуло в стене.
            Spawn(cache, worldScale, pos + normal * 0.05f, "PUFF",
                  new[] { 0, 1, 2, 3 }, new[] { 4, 4, 4, 4 });
        }

        public static void SpawnBlood(SpriteCache cache, float worldScale, Vector3 pos)
        {
            Spawn(cache, worldScale, pos, "BLUD",
                  new[] { 2, 1, 0 }, new[] { 8, 8, 8 });
        }

        static void Spawn(SpriteCache cache, float worldScale, Vector3 pos,
                          string sprite, int[] frames, int[] tics)
        {
            var go = new GameObject($"FX_{sprite}", typeof(MeshFilter), typeof(MeshRenderer));
            go.transform.position = pos;
            var bb = go.AddComponent<SpriteBillboard>();
            bb.Init(cache, sprite, frames[0], worldScale,
                    doomAngleDeg: 0f, spawnCeiling: false, ceilingY: 0f);
            bb.SetStaticFrame(frames[0]); // без ротаций

            var fx = go.AddComponent<HitEffect>();
            fx.billboard = bb;
            fx.frames = frames;
            fx.secs = new float[tics.Length];
            for (int i = 0; i < tics.Length; i++) fx.secs[i] = tics[i] / 35f;
            fx.idx = 0;
            fx.left = fx.secs[0];
        }

        void Update()
        {
            left -= Time.deltaTime;
            if (left > 0f) return;
            idx++;
            if (idx >= frames.Length) { Destroy(gameObject); return; }
            billboard.SetStaticFrame(frames[idx]);
            left = secs[idx];
        }
    }
}
```

Замечание: `SpriteBillboard` якорит НИЗ спрайта на позицию GO — для эффектов
приемлемо (PUFF/BLUD мелкие); при желании центрирования опусти `pos` на
`0.5f * высота` — не обязательно, решается глазами при финальной проверке.

- [x] **Step 2: Компиляция** — EditMode полный прогон, 0 ошибок.
- [x] **Step 3: Commit** — `git commit -m "Stage 6c: HitEffect - PUFF/BLUD short-lived billboards"`.

---

### Task 9: `PlayerWeapons` — ввод, залпы, урон

**Files:**
- Create: `Assets/Scripts/MapBuild/PlayerWeapons.cs`

Паттерн ввода — как `BuildInputActions` в `PlayerController.cs` (hand-built
`InputActionMap`, см. строки 64–86).

- [x] **Step 1: Implement**

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;
using Doom.Game;

namespace Doom.MapBuild
{
    /// Оружие игрока: ввод (ЛКМ, 1-4), кулдауны в тиках, hitscan-лучи, урон,
    /// эффекты. Правила — в Doom.Game; здесь только Unity-обвязка.
    public sealed class PlayerWeapons : MonoBehaviour
    {
        public AmmoModel Ammo { get; } = new AmmoModel();
        public WeaponLoadout Loadout { get; } = new WeaponLoadout();

        /// Выстрел произошёл — WeaponView запускает кадры выстрела и вспышку.
        public event Action<WeaponDef> Fired;

        SpriteCache cache;
        float worldScale;
        Transform cam;
        readonly DoomRandom rng = new DoomRandom();
        readonly List<HitscanShot> volley = new List<HitscanShot>();

        InputActionMap map;
        InputAction fireAction;
        float cooldown;       // сек до следующего выстрела
        bool refire;          // ЛКМ удерживается со прошлого выстрела

        public void Init(SpriteCache cache, float worldScale, Transform cameraTransform)
        {
            this.cache = cache;
            this.worldScale = worldScale;
            cam = cameraTransform;

            map = new InputActionMap("Weapons");
            fireAction = map.AddAction("fire", InputActionType.Button, "<Mouse>/leftButton");
            for (int slot = 1; slot <= 4; slot++)
            {
                int s = slot;
                var a = map.AddAction($"slot{s}", InputActionType.Button, $"<Keyboard>/{s}");
                a.performed += _ => SelectSlot(s);
            }
            map.Enable();
        }

        void OnDestroy() => map?.Dispose();

        void SelectSlot(int slot)
        {
            if (cooldown > 0f) return; // нельзя менять посреди выстрела
            foreach (WeaponId id in Enum.GetValues(typeof(WeaponId)))
                if (WeaponTable.Get(id).Slot == slot) { Loadout.TrySelect(id); return; }
        }

        void Update()
        {
            cooldown -= Time.deltaTime;
            bool held = fireAction != null && fireAction.IsPressed();
            if (!held) refire = false;
            if (held && cooldown <= 0f) FireCurrent();
        }

        void FireCurrent()
        {
            var def = WeaponTable.Get(Loadout.Current);
            if (!Ammo.TryConsume(def.Ammo, def.AmmoPerShot))
            {
                Loadout.TrySelect(Loadout.BestAvailable(Ammo)); // авто-даунгрейд
                return;
            }

            volley.Clear();
            HitscanRules.FireVolley(def, refire, rng, volley);
            float rangeDoom = def.Melee ? HitscanRules.MeleeRangeDoom
                                        : HitscanRules.HitscanRangeDoom;
            float range = rangeDoom * worldScale;

            foreach (var shot in volley)
            {
                var dir = Quaternion.AngleAxis(shot.YawOffsetDeg, Vector3.up) * cam.forward;
                // Старт чуть впереди, чтобы не поймать собственную капсулу (r=0.5м).
                var origin = cam.position + dir * 0.6f;
                if (!Physics.Raycast(origin, dir, out var hit, range,
                                     ~0, QueryTriggerInteraction.Ignore)) continue;

                var enemy = hit.collider.GetComponent<EnemyHealth>();
                if (enemy != null && !enemy.IsDead)
                {
                    enemy.TakeDamage(shot.Damage);
                    HitEffect.SpawnBlood(cache, worldScale, hit.point);
                }
                else if (enemy == null)
                {
                    HitEffect.SpawnPuff(cache, worldScale, hit.point, hit.normal);
                }
            }

            cooldown = def.CycleTics / 35f;
            refire = true;
            Fired?.Invoke(def);
        }

        /// Подбор оружейной вещи. true — вещь поглощена (уничтожить GO).
        public bool Pickup(int doomedNum)
        {
            switch (doomedNum)
            {
                case 2001: return PickWeapon(WeaponId.Shotgun, AmmoType.Shells, 8);
                case 2002: return PickWeapon(WeaponId.Chaingun, AmmoType.Bullets, 20);
                case 2007: return Ammo.Add(AmmoType.Bullets, 10);
                case 2048: return Ammo.Add(AmmoType.Bullets, 50);
                case 2008: return Ammo.Add(AmmoType.Shells, 4);
                case 2049: return Ammo.Add(AmmoType.Shells, 20);
                default: return false;
            }
        }

        bool PickWeapon(WeaponId id, AmmoType ammo, int give)
        {
            bool gotWeapon = Loadout.Give(id);      // авто-select если новое
            bool gotAmmo = Ammo.Add(ammo, give);
            return gotWeapon || gotAmmo;            // есть оружие И полно — лежит
        }

        public void ResetToStart() { Ammo.Reset(); Loadout.Reset(); cooldown = 0f; refire = false; }

        // ── для PlayMode-тестов ────────────────────────────────────────────────
        public void FireOnceForTest() { if (cooldown <= 0f) FireCurrent(); }
        public float CooldownForTest => cooldown;
    }
}
```

- [x] **Step 2: Компиляция** — EditMode полный, 0 ошибок.
- [x] **Step 3: Commit** — `git commit -m "Stage 6c: PlayerWeapons - input, hitscan volleys, pickups"`.

---

### Task 10: `WeaponView` — viewmodel с кадрами и bob

**Files:**
- Create: `Assets/Scripts/MapBuild/WeaponView.cs`

- [x] **Step 1: Implement**

```csharp
using UnityEngine;
using Doom.Game;

namespace Doom.MapBuild
{
    /// Спрайт оружия от первого лица: OnGUI-отрисовка в виртуальном экране
    /// 320x200 (масштаб по высоте, якорь по центру), кадры выстрела + вспышка
    /// по событию Fired, DOOM-bob при движении.
    ///
    /// Формула размещения — R_DrawPSprite: спрайт рисуется в 320x200 так, что
    /// левый край = sx - leftOffset (от центра 160), верх = sy - topOffset,
    /// где покой sx=1, sy=WEAPONTOP=32. Патчи оружия авторски смещены под эти
    /// координаты. Проверяется глазами (Step 2).
    public sealed class WeaponView : MonoBehaviour
    {
        const float WeaponTopPx = 32f;
        const float MaxBobPx = 16f;

        PlayerWeapons weapons;
        SpriteCache cache;
        float worldScale;
        CharacterController cc;

        WeaponDef anim;      // активная последовательность выстрела (null = покой)
        int animIdx;
        float animLeft;
        float flashLeft;
        int flashIdx;

        public void Init(PlayerWeapons weapons, SpriteCache cache, float worldScale,
                         CharacterController controller)
        {
            this.weapons = weapons;
            this.cache = cache;
            this.worldScale = worldScale;
            cc = controller;
            weapons.Fired += OnFired;
        }

        void OnDestroy() { if (weapons != null) weapons.Fired -= OnFired; }

        void OnFired(WeaponDef def)
        {
            anim = def;
            animIdx = 0;
            animLeft = def.FireTics[0] / 35f;
            if (def.FlashSprite != null && def.FlashTics.Length > 0)
            {
                flashIdx = 0;
                flashLeft = def.FlashTics[0] / 35f;
            }
        }

        void Update()
        {
            if (anim != null)
            {
                animLeft -= Time.deltaTime;
                if (animLeft <= 0f)
                {
                    animIdx++;
                    if (animIdx >= anim.FireFrames.Length) anim = null;
                    else animLeft = anim.FireTics[animIdx] / 35f;
                }
            }
            if (flashLeft > 0f)
            {
                flashLeft -= Time.deltaTime;
                if (flashLeft <= 0f && anim != null && flashIdx + 1 < anim.FlashTics.Length)
                {
                    flashIdx++;
                    flashLeft = anim.FlashTics[flashIdx] / 35f;
                }
            }
        }

        void OnGUI()
        {
            if (weapons == null || Event.current.type != EventType.Repaint) return;
            var def = WeaponTable.Get(weapons.Loadout.Current);

            // Bob (A_WeaponReady): angle крутится на 128 ед/тик из 8192 на круг.
            float bob = BobAmplitudePx();
            float phase = Time.time * 35f * 128f / 8192f * 2f * Mathf.PI;
            float sx = 1f + bob * Mathf.Cos(phase);
            // DOOM маскирует угол половиной круга: sy ходит по |sin| той же частоты.
            float sy = WeaponTopPx + bob * Mathf.Abs(Mathf.Sin(phase));
            if (anim != null) { sx = 1f; sy = WeaponTopPx; } // при выстреле — без bob

            int frame = anim != null ? anim.FireFrames[animIdx] : def.IdleFrame;
            DrawPatch(def.Sprite, frame, sx, sy);
            if (anim != null && flashLeft > 0f && anim.FlashSprite != null)
                DrawPatch(anim.FlashSprite, anim.FlashFrames[flashIdx], sx, sy);
        }

        float BobAmplitudePx()
        {
            if (cc == null) return 0f;
            var v = cc.velocity; v.y = 0f;
            float unitsPerTic = v.magnitude / worldScale / 35f;   // DOOM momx
            return Mathf.Min(MaxBobPx, unitsPerTic * unitsPerTic * 0.25f);
        }

        void DrawPatch(string sprite, int frame, float sx, float sy)
        {
            var sm = cache.Get(sprite, frame, 0);
            if (!sm.IsValid) return;
            var tex = sm.Material != null ? sm.Material.mainTexture : null;
            if (tex == null) return;

            float scale = Screen.height / 200f;
            float left = 160f + sx - sm.LeftOffset;               // виртуальные px
            float top = sy - sm.TopOffset;
            var r = new Rect(
                Screen.width * 0.5f + (left - 160f) * scale,
                top * scale,
                sm.Width * scale,
                sm.Height * scale);
            GUI.DrawTexture(r, tex);
        }
    }
}
```

- [x] **Step 2: Глазная проверка формулы размещения** — после Task 11 (когда всё
  провязано) запусти capture-харнесс `StairCaptureTests` и посмотри
  `Logs/stair-spawn.png`: пистолет должен сидеть по центру у нижней кромки, как
  в DOOM. Если спрайт улетел — проверь знаки `LeftOffset`/`TopOffset` у
  `SpriteMaterial` (viewmodel-патчи имеют большие отрицательные topOffset) и
  подгони формулу `top`/`left` (единственные две строки).
- [x] **Step 3: Компиляция** — EditMode полный, 0 ошибок.
- [x] **Step 4: Commit** — `git commit -m "Stage 6c: WeaponView - viewmodel frames, flash, bob"`.

---

### Task 11: `ThingPickup` + провязка в `MapLoader` (+ HUD AMMO, + сброс при респавне)

**Files:**
- Create: `Assets/Scripts/MapBuild/ThingPickup.cs`
- Modify: `Assets/Scripts/MapBuild/ThingSpawner.cs` (навеска ThingPickup)
- Modify: `Assets/Scripts/MapBuild/MapLoader.cs` (`SpawnPlayer`: PlayerWeapons + WeaponView + прогрев viewmodel-спрайтов)
- Modify: `Assets/Scripts/MapBuild/PlayerHud.cs` (строка AMMO)
- Modify: `Assets/Scripts/MapBuild/PlayerDeathHandler.cs` (событие Respawned)

- [x] **Step 1: `ThingPickup.cs`**

```csharp
using UnityEngine;

namespace Doom.MapBuild
{
    /// Триггер подбора оружейной вещи (Stage 6c: только оружие и патроны).
    /// Радиус касания в духе DOOM (16 юнитов вещи + 16 радиуса игрока).
    public sealed class ThingPickup : MonoBehaviour
    {
        int doomedNum;

        public void Init(int doomedNum, float worldScale)
        {
            this.doomedNum = doomedNum;
            var trig = gameObject.AddComponent<SphereCollider>();
            trig.isTrigger = true;
            trig.radius = 32f * worldScale;
            trig.center = new Vector3(0f, 32f * worldScale, 0f);
        }

        void OnTriggerEnter(Collider other)
        {
            var weapons = other.GetComponent<PlayerWeapons>();
            if (weapons == null) return;
            if (weapons.Pickup(doomedNum))
                Destroy(gameObject);
        }
    }
}
```

- [x] **Step 2: навеска в `ThingSpawner.SpawnAll`** (после блока EnemyHealth):

```csharp
switch (t.Type)
{
    case 2001: case 2002: case 2007: case 2008: case 2048: case 2049:
        go.AddComponent<ThingPickup>().Init(t.Type, worldScale);
        break;
}
```

- [x] **Step 3: `MapLoader.SpawnPlayer`** — после блока `rider` добавить:

```csharp
// Оружие и стрельба (Stage 6c).
var weapons = player.AddComponent<PlayerWeapons>();
weapons.Init(spriteCacheForPlayer, worldScale, cameraGO.transform);
var weaponView = cameraGO.AddComponent<WeaponView>();
weaponView.Init(weapons, spriteCacheForPlayer, worldScale, cc);
death.Respawned += weapons.ResetToStart;
hud.SetWeapons(weapons);
```

`spriteCacheForPlayer`: `SpawnPlayer` сейчас не видит `SpriteCache` (он
создаётся ПОСЛЕ `SpawnPlayer` в `Build`). Переставь: в `Build()` создай
`spriteSet`/`spriteCache` ДО `SpawnPlayer(map, bounds)` и передай кэш
параметром: `SpawnPlayer(map, bounds, spriteCache)`. ThingSpawner использует
тот же экземпляр.

Там же, пока WAD открыт, прогрей viewmodel/эффект-спрайты (после создания
`spriteCache`):

```csharp
// Прогрев спрайтов оружия/вспышек/эффектов: WAD закроется в конце Build.
foreach (var (spr, frames) in new (string, int[])[]
{
    ("PUNG", new[] { 0, 1, 2, 3 }), ("PISG", new[] { 0, 1, 2 }), ("PISF", new[] { 0 }),
    ("SHTG", new[] { 0, 1, 2, 3 }), ("SHTF", new[] { 0, 1 }),
    ("CHGG", new[] { 0, 1 }), ("CHGF", new[] { 0, 1 }),
    ("PUFF", new[] { 0, 1, 2, 3 }), ("BLUD", new[] { 0, 1, 2 }),
})
    foreach (int f in frames) spriteCache.Get(spr, f, 0);
```

- [x] **Step 4: `PlayerHud`** — поле `PlayerWeapons weapons` + `public void
  SetWeapons(PlayerWeapons w) => weapons = w;` и в `OnGUI` после ARMOR:

```csharp
if (weapons != null)
{
    var def = Doom.Game.WeaponTable.Get(weapons.Loadout.Current);
    string ammo = def.Ammo == Doom.Game.AmmoType.None
        ? "-" : weapons.Ammo.Get(def.Ammo).ToString();
    GUI.Label(new Rect(12f, 56f, 300f, 28f), $"AMMO {ammo}", style);
}
```

- [x] **Step 5: `PlayerDeathHandler`** — добавить `public event System.Action
  Respawned;` и вызвать `Respawned?.Invoke();` в конце обработки клавиши R
  (после реактивации компонентов; найди блок респавна по строке с телепортом).

- [x] **Step 6: Компиляция + полный EditMode** — 0 ошибок, все зелёные.
- [x] **Step 7: Commit** — `git commit -m "Stage 6c: pickups, MapLoader wiring, HUD ammo, respawn reset"`.

---

### Task 12: PlayMode-тесты

**Files:**
- Create: `Assets/Tests/PlayMode/WeaponPlayTests.cs`

Паттерн загрузки сцены/ожидания — скопируй из `PlayerDamagePlayTests.cs`
(LoadScene "Stage2_MapPreview", captureDeltaTime, ожидание MapLoader.Build,
`LogAssert.ignoreFailingMessages = true` из-за PhysX-warning'ов).

- [x] **Step 1: Write the tests**

```csharp
using System.Collections;
using System.Linq;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.TestTools;
using Doom.Game;
using Doom.MapBuild;

namespace Doom.Stage3.PlayTests
{
    public class WeaponPlayTests
    {
        [TearDown]
        public void TearDown()
        {
            Time.captureDeltaTime = 0f;
            LogAssert.ignoreFailingMessages = false;
        }

        static IEnumerator LoadLevel()
        {
            LogAssert.ignoreFailingMessages = true;
            Time.captureDeltaTime = 1f / 60f;
            SceneManager.LoadScene("Stage2_MapPreview", LoadSceneMode.Single);
            yield return null; yield return null;
            for (int i = 0; i < 90; i++) yield return null;
        }

        [UnityTest]
        public IEnumerator Pistol_kills_synthetic_enemy_and_drains_ammo()
        {
            yield return LoadLevel();
            var player = GameObject.Find("Player");
            var weapons = player.GetComponent<PlayerWeapons>();
            var cam = Camera.main.transform;

            // Синтетическая цель прямо перед камерой: капсула + EnemyHealth 20 HP.
            var target = new GameObject("TestTarget");
            target.transform.position = cam.position + cam.forward * 3f - Vector3.up * 0.8f;
            var col = target.AddComponent<CapsuleCollider>();
            col.height = 1.75f; col.radius = 0.5f;
            col.center = new Vector3(0f, 0.875f, 0f);
            var eh = target.AddComponent<EnemyHealth>();
            eh.Init(20, corpseFrame: -1, billboard: null, capsule: col);

            int startAmmo = weapons.Ammo.Get(AmmoType.Bullets);
            // 20 HP / min 5 урона = максимум 4 выстрела, стреляем с запасом.
            for (int i = 0; i < 8 && !eh.IsDead; i++)
            {
                weapons.FireOnceForTest();
                // подождать конец кулдауна (19 тиков ≈ 0.54 c при dt=1/60)
                for (int f = 0; f < 40; f++) { if (weapons.CooldownForTest <= 0f) break; yield return null; }
            }

            Assert.That(eh.IsDead, Is.True, "враг умер от пистолета");
            Assert.That(col.enabled, Is.False, "коллайдер трупа выключен");
            Assert.That(weapons.Ammo.Get(AmmoType.Bullets), Is.LessThan(startAmmo),
                "патроны потрачены");
        }

        [UnityTest]
        public IEnumerator Empty_ammo_autoswitches_to_fist()
        {
            yield return LoadLevel();
            var weapons = GameObject.Find("Player").GetComponent<PlayerWeapons>();

            while (weapons.Ammo.TryConsume(AmmoType.Bullets, 1)) { }
            Assert.That(weapons.Loadout.Current, Is.EqualTo(WeaponId.Pistol));
            weapons.FireOnceForTest(); // патронов нет → авто-переключение
            Assert.That(weapons.Loadout.Current, Is.EqualTo(WeaponId.Fist));
        }

        [UnityTest]
        public IEnumerator Shotgun_pickup_gives_weapon_and_shells()
        {
            yield return LoadLevel();
            var player = GameObject.Find("Player");
            var weapons = player.GetComponent<PlayerWeapons>();
            var cc = player.GetComponent<CharacterController>();

            // Найти шотган (Thing_2001_*) на карте; если на E1M1 его нет — создать.
            var pickupGo = GameObject.FindObjectsByType<ThingPickup>(FindObjectsSortMode.None)
                .Select(p => p.gameObject)
                .FirstOrDefault(g => g.name.StartsWith("Thing_2001"));
            if (pickupGo == null)
            {
                pickupGo = new GameObject("Thing_2001_SHOT");
                pickupGo.transform.position = player.transform.position;
                pickupGo.AddComponent<ThingPickup>().Init(2001, 1f / 32f);
            }

            // Телепорт к вещи + микродвижение, чтобы CharacterController
            // сгенерировал OnTriggerEnter.
            cc.enabled = false;
            player.transform.position = pickupGo.transform.position;
            cc.enabled = true;
            for (int i = 0; i < 10; i++) { cc.Move(new Vector3(0.01f, 0f, 0f)); yield return null; }

            Assert.That(weapons.Loadout.Has(WeaponId.Shotgun), Is.True, "дробовик подобран");
            Assert.That(weapons.Ammo.Get(AmmoType.Shells), Is.EqualTo(8), "+8 дроби");
            Assert.That(pickupGo == null || !pickupGo, Is.True, "GO вещи уничтожен");
        }
    }
}
```

- [x] **Step 2: Run PlayMode** (filter `Doom.Stage3.PlayTests.WeaponPlayTests`) — 3/3 PASS. Если pickup-тест падает на триггере (CC не генерирует OnTriggerEnter) — план Б: в `ThingPickup` заменить триггер на дистанционную проверку в `Update` (расстояние до игрока < radius), это тоже DOOM-way.
- [x] **Step 3: Run ВСЁ** — полный EditMode и полный PlayMode без фильтров: старые тесты не сломаны.
- [x] **Step 4: Commit** — `git commit -m "Stage 6c: PlayMode tests - kill, ammo autoswitch, pickup"`.

---

### Task 13: визуальная проверка + докуменация + финал

**Files:**
- Modify: `CLAUDE.md` (статус 6c, счётчики тестов)
- Modify: `docs/doom-unity-remake-plan.md` (галочка «Оружие и стрельба», абзац про 6c)
- Modify: `docs/superpowers/plans/2026-07-02-weapons.md` (все чекбоксы)

- [x] **Step 1: Глазная проверка** — прогнать PlayMode `StairCaptureTests`, открыть `Logs/stair-spawn.png`: viewmodel пистолета по центру снизу, HUD показывает `AMMO 50`. Если оружие съехало — см. Task 10 Step 2. Затем интерактивно (или попросить пользователя): выстрелить в зомби, увидеть кровь/труп, подобрать дробовик.
- [x] **Step 2: `CLAUDE.md`** — в списке под-этапов добавить абзац Stage 6c (по образцу 6b: сборки, компоненты, что отложено); обновить «Project status» (6c done) и счётчики тестов на фактические из финального прогона (ожидаемо ~159 EditMode / ~15 PlayMode — сверить с XML).
- [x] **Step 3: `docs/doom-unity-remake-plan.md`** — пометить «Оружие и стрельба ✅ (под-этап 6c)» и добавить абзац «Под-этап 6c ✅» по образцу 6a/6b со ссылками на спеку/план.
- [x] **Step 4: Отметить все чекбоксы в этом плане.**
- [x] **Step 5: Финальный прогон** полного EditMode + PlayMode, оба зелёные (числа для CLAUDE.md берутся отсюда).
- [x] **Step 6: Commit** — `git commit -m "Stage 6c done: weapons & shooting - docs, plan checkboxes"`.

---

## Порядок и зависимости

Task 1–5 — чистый C#, независимы от Unity-сцены, идут строго по TDD.
Task 6 — данные + WAD-интеграция. Task 7–11 — Unity-глю (тестируется
компиляцией + PlayMode в Task 12). Task 12 — PlayMode-верификация всего.
Task 13 — глаза + документация. Выполнять по порядку; каждый Task — отдельный
коммит (или несколько, как размечено).

## Известные упрощения (зафиксированы в спеке)

- Урон в момент нажатия, а не на N-м тике state-последовательности.
- Пулемёт: 1 выстрел/4 тика вместо пары за 8 (та же скорострельность).
- Вспышка рисуется той же parenт-позицией, без отдельного psprite-слоя.
- Трупы не анимируются (один финальный кадр) и не убираются.

Добавлено по итогам ревью-циклов реализации:

- Бочки (2035) стреляются, но без HP/взрыва — отложено (Health=0 у не-монстров).
- Viewmodel продолжает рисоваться поверх оверлея смерти (DOOM опускает оружие) — отложено.
- После смерти/респавна подобранное оружие теряется навсегда (вещи уничтожены; DOOM перезапускает уровень) — отложено.
- Пулемёт в DOOM точен на первом выстреле (A_FireCGun с !refire); у нас FirstShotAccurate=false — сознательное упрощение таблицы.
- Смена оружия во время кулдауна теряется (DOOM ставит pendingweapon в очередь) — отложено.
- Глазная проверка формулы viewmodel через capture-харнесс невозможна: `StairCaptureTests` рендерит через `Camera.Render()`, который не включает OnGUI/IMGUI — оружие и HUD в PNG не попадают. Формула следует R_DrawPSprite; мир на снимках (спавн, ящики со спрайтами и подбираемым пулемётом) рендерится без регрессий.
- Глазная проверка выполнена интерактивно в редакторе (после Task 13): она поймала горизонтальный сдвиг viewmodel — в `DrawPatch` терялся член `-160` из формулы R_DrawPSprite (экранный X = центр + (sx − 160 − leftOffset)·scale), из-за чего оружие уезжало вправо на полэкрана. Исправлено; пистолет/HUD подтверждены визуально.
